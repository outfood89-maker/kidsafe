코드 수정 후 GitHub 업로드 순서

git add .

변경된 파일 전부 추가

git commit -m "작업 내용 간단히 설명"

현재 상태 저장 (사진 찍기)
git push origin master


GitHub에 업로드


----------------------------------------------
예시
오늘 부모 대시보드 UI 만들었으면 이렇게 해요.
git add .
git commit -m "부모 대시보드 UI 추가"
git push origin master