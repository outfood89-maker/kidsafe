# [마이크로 브리프] Q-2 — 임베드 불가 영상 원천 차단 + embedError 탈출구 제거 (팀장 결정, 동작 변경)

> **팀장 결정 (2026-07-02, 업그레이드):** ① 검색에 `videoEmbeddable=true` — 임베드 막힌 영상이 애초에 결과에 안 들어오게 (유튜브 공식 필터라 규정 논란 0. RMF 확인 결과 "재생 불가 영상에 유튜브 링크 제공 의무"는 없음 — 안 보여주는 건 자유). ② embedError 화면은 **폴백으로 유지하되** "YouTube에서 보기" 버튼 제거 — 필터가 100%는 아니라서(지역·연령 제한으로 재생 시점에 막히는 케이스, 필터 전에 찜·캐시·**재생목록 경유**로 들어온 영상). 그 버튼은 아이를 유튜브 본진(무한 알고리즘·쇼츠)으로 내보내는 탈출구 — 서사의 정중앙을 뚫는 구멍.
> **카피 확정 (verbatim, 임의 수정 금지).**

## 수정 1 — [search.py:68-81](../server/routers/search.py#L68-L81) 검색 파라미터 (한 줄)

`search_youtube`의 params에 추가 (이미 `type: "video"`라 파라미터 조건 충족):
```python
"videoEmbeddable": "true",   # 임베드 가능 영상만 — 임베드 불가 영상 원천 차단 (Q-2, 팀장 결정)
```
- ⚠️ 재생목록 검색(:129 `type: "playlist"`)과 playlistItems(:253)에는 **적용 불가**(API가 지원 안 함) → 그 경로는 아래 폴백이 담당. 다른 곳 수정 금지.

## 수정 2 — [VideoPlayer.jsx:590-601](../client/src/components/VideoPlayer.jsx#L590-L601) embedError 분기 (폴백)

1. **문구 교체** (:592-593 두 줄 → 확정 카피 한 줄):
```
이 영상은 키디에서 볼 수 없어. 다른 재밌는 거 찾으러 가자! 🦕
```
2. **버튼 교체:** "YouTube에서 보기"(`window.open` 유튜브 새 탭)는 **삭제 말고 주석처리 보존** (CLAUDE.md 규칙). 같은 스타일로 새 버튼:
   - 라벨: **"다른 영상 보러 가기"**
   - 동작: `requestClose()` (:606과 동일 — 플레이어 닫기, 새 탭·외부 이동 없음)

## 검증
- 검색 결과 정상 반환(개수 큰 변화 없는지 육안) — videoEmbeddable 추가 후에도 뽀로로 등 일반 검색 정상.
- 임베드 제한 영상 재생 시도(재생목록 경유 등) → 키디(sad) + 새 카피 + 버튼 클릭 → 플레이어 닫히고 목록 복귀. 유튜브로 나가는 경로 0.
- 정상 영상 재생 회귀 무변화 / build ✓

## 하지 말 것
- embedError 판정 로직·onError 핸들러 변경 금지 (버튼·문구만)
- 부모용 화면·다른 화면의 유튜브 링크는 이번 스코프 아님
