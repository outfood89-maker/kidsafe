# KidSafe 프로젝트 하네스

## 프로젝트 개요
- AI 기반 어린이 미디어 안전 및 추천 플랫폼
- 개발자: Freddie (초급 프론트엔드, 포트폴리오 목적)
- 현황: KT 공모전 본상 후보 — 시상식 2026-08-14(금) / 고도화 착수 예정 2026-08-17(월)
- GitHub: https://github.com/outfood89-maker/kidsafe
- **정체성(현재 방향, UPDATE_1 기준):** '영상 검수 도구'가 아니라 **'아이의 첫 미디어 경험 공간'**.
  - 정체성(B) = 키디와 함께하는 첫 미디어 경험 / 해자·증거(A) = 대단한 검수.
  - **B가 부모 가슴을 치고, A가 그것을 믿게 만든다.** 검수는 잘하는 기본기, 관계가 방어 가능한 정체성.
  - 데모의 심장: **키디가 묻는다 → 아이가 답한다 → 부모가 리포트를 열고 뭉클해진다.** 모든 설계는 이 순간으로 수렴.
  - ⚠️ 윤리 선: 키디는 아이의 '비밀 친구'. 부모에겐 **아이가 공유 선택한 것 + 감정 흐름 요약만** 전달(원본 대화 노출 금지).
  - 상세: `UPDATE_1/KidSafe_정체성전환_전략.md`(전략) / `UPDATE_1/KidSafe_ClaudeCode_작업지시서.md`(구현 명세)

## 🧭 협업 체제 (4인) — `prompts/`
- Claude Code 창 3개로 나눠 돌린다: **팀장**(전략·아동 카피 게이트) / **컨트롤타워**(설계·브리프·리뷰·커밋) / **작업자**(구현). 오너(Freddie)가 복사-붙여넣기로 중계. 커밋 규칙은 아래 **커밋 게이트** 참조.
- 시스템 프롬프트 3종 + 사용법: **`prompts/README.md`** 부터 읽을 것.
- ⚠️ 팀장 프롬프트는 원래 git에 없어 유실 직전이었음(2026-08-01 백업 폴더에서 복구). **`prompts/`는 반드시 커밋 유지.**
- **기록 문서 2종 — 성격이 다르니 섞지 말 것**
  | 문서 | 묻는 것 | 독자 | 작성 |
  |---|---|---|---|
  | `WORKLOG.md` | *"어디까지 했지?"* — 진행상황·지뢰 목록 | 다음 세션 | 작업 주체 |
  | **`DECISIONS.md`** | ***"우리가 뭘 배웠지?"*** — 결정·이유·배움 | 6개월 뒤·새 합류자·심사위원 | **팀장** |
  → `DECISIONS.md`는 **브리프 → 작업 → 보고 → 팀장 검수**가 완결될 때마다 팀장이 5항목(결정/왜/배운 것/자산/되돌리려면)으로 기록한다. 새 안건 시작 전 **여기부터 읽어** 같은 논의를 반복하지 않는다.

## 기술 스택
- 프론트: React 19, React Router v7, Tailwind CSS, Axios, Recharts, react-icons
- 백엔드: FastAPI (Python) — `server/` 폴더, uvicorn으로 실행 (초기 Express 구현은 폐기됨)
- AI: Anthropic API — 실시간 상호작용(챗봇·체크인 반응/인사·스케줄)은 `claude-haiku-4-5-20251001`, **주간 리포트·AI 코치는 Sonnet 승격**(`reports.py`의 `REPORT_MODEL` 환경변수, 기본 `claude-sonnet-5`)
- 영상: YouTube Data API v3
- 아바타: 로컬 PNG (`client/public/images/avatars/avatar_01~08.png`)
- 배포: Vercel (프론트 `https://kidsafe-eight.vercel.app`) + Railway (백엔드 `https://kidsafe-production.up.railway.app`)

## 남은 작업 우선순위
- ✅ **검수 고도화 (Tier0~2): 사실상 완료** — 자막+썸네일 Vision+Claude 정밀분석·캐싱·채널학습까지 구현됨. (설계: `문서/설계/KidSafe_검수아키텍처_핵심설계.md`) 남은 건 정확도 튜닝뿐.
- 🟢 **현재 무게중심 = 정체성 전환 P0** (`UPDATE_1/KidSafe_ClaudeCode_작업지시서.md` TASK 순서):
  - F0 관심사 씨앗(InterestSeed) / F1 키디 환영+체크인(DailyCheckin) / F2 부모 리포트(KiddyReportCard) — 데모의 심장. **✅ 3건 모두 완료 확정**(2026-08-02 코드 전수 검증 + 적대 검증, 근거는 `WORKLOG.md` 세션 로그).
  - F3 리터러시 한 스푼은 **보류 확정**(2026-08-02, 근거: `kiddy_voice/F3-리터러시한스푼-판단자료.md`). 7세+ 자유 대화는 '다음' 단계.
  - ✅ 지뢰 #6(DB 스키마 레포 미보관) **해소 완료** — `server/sql/004_identity_p0_tables.sql` 박제 + 실 DB 실행 검증(2026-08-02).
- 🔵 **다음 무게중심 = 고도화 (2026-08-17 착수)** — 전체 지도·체크리스트 36항목은 `고도화/README.md`. 최대 관문은 **그림일기 서버 이전**(현재 브라우저 로컬 저장이라 TV·앨범·타임캡슐이 전부 여기 막혀 있음).

## ⚠️ FastAPI 핵심 주의사항
- Railway 재시작 시 JSON 초기화됨 → `main.py`의 `ensure_data_files()`로 해결
- JSON 읽기/쓰기 시 반드시 `encoding="utf-8"` 명시
- FastAPI에서 Anthropic은 반드시 `AsyncAnthropic` 사용 (sync 쓰면 블로킹)
- API 엔드포인트 주소 동일하게 유지 → 프론트 코드 수정 불필요
- **라우터 추가 시 반드시 `main.py`에 import + `include_router` 등록할 것** — 누락 시 404 (chat 라우터 누락 사고 발생)
- **응답 JSON에 필드를 추가할 때 키 충돌 주의** — 프론트가 `{ ...video, ...safety }` 패턴으로 spread하므로, 같은 키가 있으면 **원본 데이터를 덮어쓴다** (videoId 덮어쓰기 사고 발생)
- **외부 API(YouTube 등) 응답 dict 접근은 반드시 `.get()`으로** — JS는 undefined로 넘어가지만 Python은 KeyError로 500 터짐 (예: `item["id"]["videoId"]` → `item.get("id", {}).get("videoId")`)
- **Pydantic 모델의 Optional 필드는 `Optional[str]` 등으로 선언** — 프론트가 `null`을 명시적으로 보낼 경우 `str` 타입이면 422 Unprocessable Entity 발생 (chat profileName/profileAge 사고 발생)
- 🔴 **`except Exception` 이 `HTTPException` 을 삼킨다 — 401·404·429 가 조용히 '성공'으로 둔갑** (2026-08-06 차단시험으로 실증)
  - `HTTPException` 도 `Exception` 의 하위다. 실패를 `{ ok: False }` 로 감싸는 라우터(`diary_image.py` 계열)에서
    **인증·인가·한도 검사를 `try` 안에 두면** 429 가 `{ok:false}` 로 바뀌어 나간다. **에러도 안 나고 화면도 멀쩡하다.**
  - **규칙: 검사(Depends 외의 소유권·한도 확인)는 `try` 바깥에 둔다.** 부득이 안에 둬야 하면
    `except HTTPException: raise` 를 **일반 `except` 보다 먼저** 놓는다 (`db.py`·`analyze.py:analyze_deep` 이 그 패턴).
  - 검증할 때 **상태코드만 보지 말고 응답 본문에 `ok` 키가 섞이지 않았는지**까지 확인할 것.

## 중요 설정

### 안전도 분석 (`server/routers/analyze.py`)
- **2층 구조** — 기본 채점은 **키워드 기반**(크레딧 절약), 그 위에 **Tier1~2 정밀검수**(자막·썸네일 Vision + Claude)가 얹힌다. 대체가 아니라 층위다.
- Anthropic 모델 배치: `chat.py`·`checkins.py`(반응/인사)·`kiddy_greeting.py`·`schedules.py`는 Haiku, `reports.py`(리포트·코치)는 Sonnet(`REPORT_MODEL`), `analyze.py` Tier2 정밀검수는 Haiku Vision. (안전도 기본 채점은 여전히 키워드 기반)

### 모바일 테스트
- `client/.env`의 **`VITE_API_URL`** 에 PC 로컬 IP를 넣는다 (예: `http://172.30.1.56:3000`).
- ⚠️ **`api.js` 코드를 직접 고치지 말 것** — 기본값 `http://localhost:3000`을 환경변수로 덮는 구조다(`api.js:4`).
- ⚠️ 배포 전 `.env`에 로컬 IP가 남아있지 않은지 반드시 확인 (`WORKLOG.md` 지뢰 #2)

### 🔒 pre-commit 훅 — 규칙을 '장치'로 (2026-08-05 신설)

**설치 — 자동이다. 손댈 것 없음.**
`client` 에서 `npm install` 하면 `postinstall` → `scripts/setup-hooks.mjs` 가 훅을 활성화한다.
새 기기 세팅은 반드시 `npm install` 을 거치므로 **잊을 수가 없다.**

> ⚠️ **왜 자동이어야 했나:** 훅은 `.git/hooks` 에 살아서 **git 으로 전파되지 않는다.**
> 새 기기에서는 훅 없이 커밋이 **아무 경고 없이** 그냥 된다. CLAUDE.md 에 "설치하세요"라고
> 적어두는 건 **'규칙'이지 '장치'가 아니다**(읽고 기억해서 실행해야 함).
> 안전장치: git 저장소가 아니거나 훅 파일이 없어도 **npm install 을 절대 실패시키지 않는다.**

수동으로 켜야 할 때(드묾): `git config core.hooksPath scripts/hooks`

`scripts/hooks/pre-commit` 이 **바뀐 영역만** 검사한다. 문서만 고쳤으면 **0.4초에 통과**한다.

| 바뀐 영역 | 검사 | 시간 |
|---|---|---|
| 문서·설정만 | 건너뜀 | ~0.4초 |
| `server/**.py` | `_verify_all.py` — **4종** (아래) | ~3초 |
| `client/src/**` | `vitest run` + `npm run build` | ~8초 |

**백엔드 검사 5종** (손으로 보려면 `cd server && ./venv/bin/python _verify_all.py`)

| # | 검사 | 막는 것 |
|---|---|---|
| 1 | 서버 임포트 | 문법 오류·순환 import |
| 2 | 🔴 **인증 기준선** | 미인증이 `GET /` 하나를 넘으면 차단. 새 라우트에 `Depends` 를 빠뜨리는 실수 |
| 3 | 🔴 **인가(소유권)** — `_verify_authz.py` | *"자물쇠는 달렸는데 남의 집 문이 열리는"* 경우. 프로필을 다루는 라우트에 `get_owned_profile`·`user_id` 매칭이 없으면 차단 |
| 4 | 🐤 **윤리선 카나리아** — `_verify_canary.py` | **아이의 비공유 이야기가 부모 화면으로 새는 것** |
| 5 | 💸 **비용 상한** — `_verify_quota.py` | **한도가 조용히 사라지는 것.** 한도는 지워도 에러가 안 나고 화면도 멀쩡하다 — 카나리아와 같은 종류의 사고 |

> 💸 **비용 상한 검사가 특히 조심하는 것 — "막히는 것만 보면 절반이다."**
> "6번째가 막혔다"는 쉽게 확인되지만, **"5번째까지는 안 막힌다"** 를 같이 안 보면
> 통과 보고를 믿는 사이 **아이가 그림을 못 그린다.** 그래서 모든 한도 검사에 **대조군**을 붙인다.
> 그리고 진짜 목적은 '차단'이 아니라 **'돈이 안 나가는 것'** 이다 → 막힌 뒤 외부 호출이 **0번**인지 반드시 확인.

> 🐤 **카나리아가 왜 필요한가:** 윤리선이 뚫려도 **에러가 안 나고 화면은 예쁘게 나온다.** 아무도 신고하지 않는다 — 조용히 배포되고 조용히 신뢰를 깎는다. 그래서 사람이 아니라 **기계가 지킨다.**
> 방법: 비공유 체크인에 표식을 심고 부모용 API 3종을 호출해 **응답 어디에도 없어야** 통과. 대조군(공유 답변)이 도달하는지도 함께 확인한다 — 그것마저 없으면 "유출이 없는" 게 아니라 **검사가 아무것도 안 한 것**이다.
> ⚠️ 실증: `reports.py:397` 의 `share_with_parent` 필터 한 줄을 지우자 **즉시 차단**됐다(2026-08-05 시험).

- 새 엔드포인트를 **공개로 두어야 한다면**: 사유를 GD-S0 §2 에 기록하고 훅의 허용 목록을 갱신할 것(그냥 우회 금지).
- 긴급 우회: `git commit --no-verify` — ⚠️ 단 **커밋 게이트 규칙상 검증 안 된 코드의 푸시는 여전히 금지.**
- 프론트 테스트가 **전부 `Test timed out`** 이면 로직이 아니라 **무거워진 것**이다(위 「성능 회귀」·「디버깅」 절).

### Git 커밋 방식 (Freddie 선호)
```bash
git add .
git commit -m "feat: 작업 내용"
git push origin master
```

### 🔴 커밋 게이트 (2026-08-04 오너 확정 — 반드시 지킬 것)

**`master` = 배포다.** Vercel(프론트)·Railway(백엔드)가 master를 보고 자동 배포하므로, 검증 안 된 코드를 올리면 **실제 서비스가 깨진다.**

| 대상 | 커밋·푸시 |
|---|---|
| **문서** (md·조사자료·브리프·계획서·설계문서) | ✅ 자유롭게 즉시 |
| **기능 코드** (client/·server/) | 🔴 **①검증 통과 + ②오너 시범 테스트 완료** 둘 다 끝난 뒤에만 |

- 작업자·컨트롤타워는 코드 작업을 끝내도 **스스로 push하지 않는다.** 검증 결과를 보고하고 **오너의 "올려도 된다"를 받은 뒤** 푸시한다.
- 브리프의 `보고 양식`에 이 게이트를 명시할 것.
- ⚠️ **유실 방지와의 균형:** 7/12 코드 유실 사고(git 미커밋 상태로 맥 이전)를 반복하면 안 된다. 장시간 작업은 **로컬 커밋은 하되 push만 보류**하는 방식을 기본으로 한다. (작업 브랜치 사용 여부는 오너 확정 대기 — 이 레포는 master 직접 커밋 관례)

## 코드 규칙 (필수)
- 모든 주석과 답변은 한국어
- 함수형 컴포넌트만 사용 (class형 금지)
- Axios만 사용 (fetch 금지)
- Tailwind CSS만 사용 (인라인 style은 불가피한 경우만)
- try-catch 필수
- 백엔드(FastAPI)는 Python — `import/from` 방식, `require()` 없음
- 백엔드 라우터 추가 시 반드시 `server/main.py`에 import + `include_router` 등록

## 🧪 검증 고정 축 4개 (2026-08-05 오너 확정 — 매 작업 필수)

> **왜 고정인가:** 검증 항목을 매번 즉흥으로 정하면 **축이 통째로 빠진다.** 아래 사고들이 전부 그렇게 났다 — 기능은 동작했고 문법도 맞았다. **아무도 그 질문을 던지지 않았을 뿐이다.**
> **틀린 답은 시끄러워서 결국 잡히고, 안 던진 질문은 조용히 배포된다.**

작업을 끝내기 전 **아래 4개를 항상 자문한다. 매번 새로 짜지 말 것.**

| # | 질문 | 이 축이 빠져서 난 사고 |
|---|---|---|
| 1 | **기존 동작이 보존됐나?** | `efadd94`(키디 검색 포즈 삭제) · KidHome 로딩 게이트(검색바 실종) · `videoId` 덮어쓰기 |
| 2 | **무거워지지 않았나?** | `react-icons/fa` 배럴 import — 아이콘 2개 쓰려다 **1,611개** 로드, 테스트 import 9.45s→**120s** (2026-08-05) |
| 3 | **데이터가 새지 않나? (윤리선)** | ⚠️ **아직 안 터짐 — 터지면 제일 크다.** 키디는 아이의 **비밀 친구**다. 부모에게 가는 건 *아이가 공유 선택한 것 + 감정 흐름 요약*뿐. 새 기능마다 **"이 데이터가 부모 화면으로 새는 경로가 있나?"** 를 반드시 물을 것 |
| 4 | **사실이 왜곡되지 않나?** | 키디 스케줄 인사(부모 메모 재해석) · 볼 것 "동화 **봤구나**"(미래→과거) |

⚠️ **2번은 "테스트가 통과했다"로 대체되지 않는다.** 실행 시간·번들 크기를 **작업 전후 비교값으로** 확인할 것. 이번엔 우연히 타임아웃이 나서 걸렸을 뿐, 조금만 가벼웠으면 그대로 배포됐다.

### ⚠️ 가짜(mock) 주입 3원칙 — **하루에 3번 반복된 사고** (2026-08-05)

검증하려고 끼운 가짜가 **실제 실행 경로에 제대로 꽂혔는지 확인하지 않아** 세 번 틀렸다.

| # | 무엇을 했나 | 방향 | 결과 |
|---|---|---|---|
| 1 | `write_audit` 을 **통째로** 교체 — 내부에서 예외를 삼키도록 설계된 함수였다 | 거짓 실패 | 없는 버그(500)를 만들어 "실패"로 보고 |
| 2 | `load_dotenv` 를 안 막음 — 로컬 `.env` 가 내 환경변수를 덮어씀 | 거짓 실패 | 배포 상황 재현 실패 → 7건 오탐 |
| 3 | 가짜를 **정의만 하고 안 끼움** | 🔴 **거짓 통과** | 진짜 YouTube 호출 → **검증 안 된 채 통과** + 쿼터 300유닛 |

> 🔴 **3번이 가장 위험하다 — 통과로 나오니 아무도 다시 안 본다.** 파이썬은 오타 난 이름에도 새 속성을 조용히 만들어준다.

**3원칙 (도구: [`server/_verify_fakes.py`](server/_verify_fakes.py) — 매번 새로 짜지 말고 이걸 쓸 것)**

1. **경계에 끼운다** — 우리 코드가 외부와 만나는 마지막 지점. 그보다 바깥이면 중간 층(방어막·덮어쓰기)을 건너뛴다.

   | 경계 | 지점 | 도구 |
   |---|---|---|
   | DB | `db.sb_select`·`sb_insert`·`sb_update`·`sb_delete` | `FakeDB` |
   | LLM | `anthropic.AsyncAnthropic` | `FakeAnthropic` |
   | 외부 HTTP | `httpx` (YouTube·CLOVA) | `FakeHTTP` |
   | 환경 | `dotenv.load_dotenv` | `block_dotenv()` · `reload_app()` |

2. **가짜는 호출 기록을 남기고, 테스트가 그 기록을 검사한다.** `Recorder.assert_called()` / **`assert_not_called()`** — **0번이어야 하는 것도 반드시 검사**(예: 무토큰 요청의 외부 호출 0). 사고 3은 이 한 줄로 잡혔다.
3. **결과가 이상하면 코드 말고 배선부터 의심한다.**
   - **실패**가 났다 → "버그다" 결론 전에 가짜가 제대로 꽂혔는지
   - **통과**가 났다 → 진짜 검증된 건지. 외부 호출 흔적이 예상과 맞는지

⚠️ `patch()` 는 **없는 이름에 주입하면 즉시 `RuntimeError`** 를 낸다. 오타로 조용히 새는 것을 막는 장치이므로 `setattr` 로 직접 바꾸지 말 것.
🔬 도구가 위 3건을 실제로 잡는지는 [`고도화/기능별/GD-S_보안축/verify_fakes_catches.py`](고도화/기능별/GD-S_보안축/verify_fakes_catches.py) 로 재현 검증돼 있다(6/0 통과).

### 🔁 오너의 "다시 한번 검증해봐" — 팀장 대응 규칙 (2026-08-05 오너 확정)

기계는 **"안 던진 질문"** 을 대신 물어줄 수 없다. 그래서 오너가 **딱 한 문장**으로 그 역할을 맡는다:

> 팀장이 *"검증 완료"* 라고 하면 → 오너: **"다시 한번 검증해봐."**

⚠️ **이때 같은 검사를 반복하면 아무 의미가 없다. 아직 안 본 '축'을 찾아야 한다.**

**실증 (2026-08-05, 두 번 다 새로운 걸 찾았다):**

| 차수 | 무엇을 봤나 | 그래서 나온 것 |
|---|---|---|
| 1차 | "토큰 없으면 막히나" | 통과 — **하지만 절반만 본 것이었다** |
| 2차 | **"회원은? 관리자는?"** | 자물쇠는 달았는데 **열쇠가 맞는지 안 봤다**는 걸 발견 |
| 3차 | **"파라미터 오염? 응답 형태 불변? 삭제된 줄?"** | 새 축 3개. 전부 통과했지만 **안 봤으면 몰랐을 것** |

**대응 규칙:**
1. **새 각도를 찾는다.** 답변에 *"이번엔 ○○를 새로 봤습니다"* 를 **반드시 명시**한다.
2. **방어하지 않는다.** *"이미 다 봤습니다"* 는 금지. 오너가 그 말을 한 순간 **아직 안 본 게 있다고 가정**한다.
3. **정말 없으면 정직하게** — *"더 볼 축이 없다, 이유는 X"*. 억지로 만들어내지 않는다.
4. **트리거는 "검증 완료"만이 아니다** — *"끝났습니다 · 완료했습니다 · 문제없습니다 · 전부 통과"* 전부 해당. 그런 말을 할 때는 **먼저** 「검증 고정 축 4개」를 다시 훑을 것.

### 🔴 오너가 원하는 방향의 결론일수록 **더** 의심할 것 (실사고: 2026-08-06)

**가장 위험한 순간은 오너가 바라는 답을 찾아드리려 할 때다.**

| 무슨 일이 있었나 |
|---|
| 오너: *"영상 검수는 기본 기능으로 가고 싶다"* → 측정 결과도 *"생각보다 싸다"* 로 나옴 |
| → 팀장이 **"한도를 풀자"로 먼저 기울고**, 그 뒤에 근거를 찾았다 (*"분당 제한이 있으니 괜찮다"*) |
| → 숫자를 넣어보니 분당 3회만으로 **하루 4,320회 · 계정 1개 월 200만원**이 통과. 완전히 틀렸다 |
| → 같은 날 오전 `quota.py` 에 *"분당=얼마나 빨리 / 하루=얼마나 많이"* 라고 **직접 써놓고** 어겼다 |

**핵심: 계산을 빠뜨린 게 아니라 계산할 생각이 안 들었다.** 이미 답을 안다고 느꼈기 때문이다.
이런 실수는 *"다음엔 계산하겠다"* 는 다짐으로 막히지 않는다 — **구조가 같으면 또 난다.**

**신호 3개 — 하나라도 켜지면 멈추고 숫자부터 넣을 것**
1. 오너의 희망과 내 결론이 **같은 방향**이다
2. 유리한 측정치가 방금 나왔다 (예: *"캐시 적중률 97%"*)
3. 내가 만든 안전장치를 **내가 완화하자고** 하고 있다 ← 가장 강한 신호

**⚠️ 평균으로 최악을 재지 말 것.** 캐시 적중률 97% 는 **정상 사용자** 값이다.
비용을 태우려는 쪽은 매번 새 ID 를 던져 **캐시를 일부러 피한다**(적중률 0%).
*"평소엔 싸다"* 와 *"최악에도 안전하다"* 는 다른 질문이다.

**장치(2026-08-06 신설):** `server/quota.py` 의 `_validate_limits()` 가 **하루 한도 없는 종류를 아예 못 만들게** 한다.
`_verify_quota.py` 는 매 실행마다 **"계정 1개가 하루에 태울 수 있는 최대 금액"** 을 찍는다 — 안 보고 지나갈 수 없게.

### 🔬 백엔드는 안전망이 없다 — 검증 스크립트를 남길 것

**프론트 테스트 27파일 208개 / 백엔드 테스트 0개** (2026-08-05 실측).
→ 위 실수 2건이 잡힌 건 **프론트에 테스트가 있었기 때문**이다. 같은 실수를 백엔드에서 하면 **아무도 모른다.**
→ 남은 큰 작업(GD-8 서버이전·GiftGate·레이트리밋·비용상한)은 **거의 전부 백엔드**다.
→ **백엔드 작업은 검증 스크립트를 레포에 남긴다.** 선례: `고도화/기능별/GD-S_보안축/verify_s1_*.py`(무토큰 401 / 회원 403 / 관리자 통과 / 응답형태 불변 / 삭제줄 전수).

## ⚠️ 과거 실수 — 반드시 지킬 것

### ⚠️ 성능 회귀 — 모듈 최상단 부담 금지 (실사고: 2026-08-05, 테스트 5초→147초)

동작은 멀쩡한데 **앱이 무거워지는** 실수다. 테스트 타임아웃으로 겨우 드러났다.

| 금지 | 이유 | 대신 |
|---|---|---|
| **배럴 import** (`from "react-icons/fa"` 등) — 특히 `App.jsx`가 직접 import 하는 컴포넌트에서 | 아이콘 **1,611개**가 통째로 딸려온다. `App`을 쓰는 **모든 테스트·화면**이 매번 로드 → `import` 9.45s→**120.04s**, 37개 타임아웃 | **인라인 SVG로 직접 그린다.** 선례: `ProtectedRoute`·`AdminRoute` 둘 다 react-icons를 쓰지 않는다 |
| **모듈 최상단에서 클라이언트/무거운 객체 생성** | `utils/supabase.js`는 거의 모든 화면이 import → 앱 전역에서 초기화된다. 검증용 클라이언트를 그렇게 만들었다가 46개 타임아웃 | **지연 생성**(`getVerifyClient()`처럼 필요할 때만) |

> **새 파일을 만들기 전에 같은 폴더 형제의 `import` 목록부터 볼 것.** 형제 둘이 특정 패턴을 피하고 있으면 이유가 있다.

### ⚠️ 디버깅 — 환경 탓으로 넘기기 전에 `git stash` (실사고: 2026-08-05)

**내 변경 직후 깨졌다면 그게 가장 강한 증거다.** 그런데 실패가 흔들리면(46→7→46) 환경을 의심하게 되고, 실제로 그렇게 두 번 헛짚었다.

| 헛짚음 | 실제 |
|---|---|
| "Steam이 CPU 146% 주범" | `ps aux`의 `%CPU`는 **프로세스 생애 평균**이지 현재값이 아니다 |
| "게임 부하 때문" | **게임이 켜진 채로** 기준선 199개 전부 통과(7.32s) |

**규칙:**
1. 오류 **종류**부터 본다 — `AssertionError`(로직)인가 `timed out`(느려짐)인가. **전부 타임아웃이면 로직이 아니라 무게 문제다.**
2. **`git stash`로 내 변경만 치우고 같은 조건에서 기준선을 잰다.** 5분이면 되고, 유일하게 확실한 판별법이다. **가장 확실한 검증을 가장 늦게 하지 말 것.**
   - 일부만 치우려면 `git stash push -m "메모" -- client/` (경로 지정). **치우기 전에 변경 파일을 스크래치패드에 복사**해두면 pop 실패 위험이 0이 된다.
   - 🔴 **2026-08-06 실증:** 프론트 31개 실패 → 내 변경을 치우자 **36개로 더 늘었다.** 코드가 원인이 아님이 즉시 판명.
3. **전부 타임아웃이면 `--no-file-parallelism` 으로 갈라라** (2026-08-06 신설).
   vitest 는 워커를 코어 수만큼 띄운다. **머신이 바쁘면 워커끼리 굶어 5초 타임아웃**이 무더기로 난다 — 코드는 멀쩡한데.
   | 조건 | 결과 |
   |---|---|
   | 병렬 (게임 44% + `kernel_task` 40% 물린 상태) | 31개 실패 · `import` **183초** |
   | **순차 (`--no-file-parallelism`)** | **214개 전부 통과** · `import` **7.6초** |
   ⚠️ `kernel_task` 가 높으면 **발열로 CPU 를 강제로 늦추는 중**이다. 이때 병렬 측정값은 아무 의미가 없다.
4. 용의자를 **반으로 자른다** (테스트 제외 / 컴포넌트 제외).
5. 로그에서 **어느 단계가 느린지** 본다 (`transform` / `import` / `tests`).
6. 원인을 단정하기 전에 **"내가 이걸 어떻게 알았지?"** 를 확인한다. 근거가 약하면 "아마"라고 말한다 — 잘못된 확신은 오너의 시간을 뺏는다.

### ⚠️ 문서 검색 — "없다"고 단정하기 전에 (실사고: 2026-08-01 설계 v2 오분류)
윈도우↔맥을 오간 레포라 **파일명 검색이 두 겹으로 거짓말한다.**
1. **한글 NFC/NFD** — 맥은 NFD 저장이라 `find -name "한글*"`·`grep`이 **있는 파일을 못 찾는다.** → `python3` + `unicodedata.normalize('NFC', ...)`.
2. **윈도우 8.3 단축명 + 대문자 `.MD`** — `kiddy_voice/`의 4개(`KI009F~1` `KI7A19~1` `그림일기/KI366E~1` `그림일기/KI7155~1`)는 한글로 못 찾고 `--include="*.md"` 필터에서도 빠진다. 그중 `그림일기/KI7155~1.MD` = **그림일기 설계 v2(SoT)** — AD 브리프·`ROADMAP-고도화.md`가 verbatim 참조하는 현행 문서.
- **판정 규칙:** 못 찾으면 → `*~1*.MD` 4개 내용 확인 → `git log --all --diff-filter=D` 확인 → 그 뒤에야 "없다"고 말할 것.

### 파일 및 코드 관리
- 기존 파일 절대 삭제 금지 (VideoModal.jsx 삭제 사고 발생)
- **기존 코드(함수, state, UI 섹션 포함)를 삭제하거나 크게 변경하기 전에 반드시 Freddie에게 먼저 물어볼 것**
- 기능을 끄고 싶을 때는 삭제 말고 주석처리로 비활성화 (복구 가능하게)
- 파일 경로 항상 명시

### ⚠️ 대규모 레이아웃/리라이트 시 기존 동작 보존 (실제 사고: efadd94)
**큰 레이아웃 커밋이 잘 동작하던 조건부 로직을 조용히 지운다.** 사례: `efadd94`에서 `pose={loading ? "search" : "hello"}`(검색 중 포즈 전환)가 `pose="hello"` 하드코딩으로 **삭제** → UX 상실, 한참 뒤 발견.
1. 재작성 전에 그 안의 **조건부 동작(`loading ? ...`, `isX && ...`, 상태 분기)을 먼저 목록화**하고 새 코드에 **그대로 보존**.
2. 큰 diff는 추가(+)만 보지 말고 **삭제(−)된 줄을 반드시 검토**.
3. "리팩터/디자인" 커밋에서 **동작은 바꾸지 말 것**(색·레이아웃만). 동작 변경은 별도 커밋 + Freddie 동의.
4. 의심되면 `git log -S "<코드조각>" -- <파일>`.

### YouTube API
- 과도한 호출 시 429 오류 (할당량 초과) 주의 — 쿼터 초기화: 매일 오후 4시 (한국 기준)
- 429 발생 시 서버가 500 반환 → 프론트에서 "오늘 검색 횟수를 초과했어요" 안내 필요 (미구현)

### UI / Tailwind
- `line-clamp` 안 먹힐 경우 인라인 스타일: `style={{ display: "-webkit-box", WebkitLineClamp: 2, WebkitBoxOrient: "vertical", overflow: "hidden" }}`
- 카드 고정 크기는 인라인 `style={{ width, height }}`가 안정적
- 가로 스크롤 컨테이너는 반드시 `flex-nowrap` 추가
- CSS 애니메이션 transform이 인라인 transform을 덮어씀 → 팝업 가운데 정렬은 바깥 flex 컨테이너로 처리하고 안쪽에만 animation 적용
- 진행 점(dot)이 많을 때(10개 이상) 줄바꿈 방지: `flexWrap: "nowrap"`, `flexShrink: 0`, 크기 22~26px, gap-1

### 랜딩페이지 앱 캡쳐(PhoneShot) 크기 일관성 (캡쳐가 작아 보이는 사고)
- `width`는 **표시 폭 상한(maxWidth)**. 자리마다 매직넘버(240/320/360…)를 박아 섹션끼리 들쭉날쭉해졌고, 4칸 그리드에 맞춘 `width={240}`은 모바일(1칸)에서 폰만 작아 보이게 만든다.
- **기준:** 단독·2단 = **360**(기본값) / 3~4칸 그리드 = 폭은 셀이 정하므로 상한만 넉넉히(예: 340) + `w-full`. 모바일 1칸에서 240처럼 작아지지 않게 **360 근처**로.
- 원본 PNG는 전부 ~780px 동일 규격 → **작아 보이면 PNG가 아니라 `width` 프롭·그리드 칸 수를 의심할 것.**

### KidHome 로딩/검색 화면 구조 (사고 발생 — 반드시 분리)
**항상 보이는 chrome(검색바·칩)** 과 **로딩 중 바뀌는 콘텐츠(추천/로더/결과)** 를 한 `!loading` 게이트로 묶지 말 것. (사고1: 묶여서 검색 중 검색바까지 사라짐 / 사고2: 게이트만 풀었더니 추천 제목이 로딩 중에도 남아 로더가 동떨어져 보임)
- 검색바·칩 → **항상 렌더**
- 콘텐츠 영역은 **상호배타**: `loading`→로더 / 결과 있음→결과 / 그 외→추천
- 큐레이션 헤더(추천 제목)는 **`!loading && 결과없음`** 일 때만

### 아바타 시스템
- 경로: `client/public/images/avatars/avatar_01~08.png`
- **현행 CSS (PNG 재가공 완료 — 정사각·상반신·머리위 여백 통일):** `objectFit: cover` · `objectPosition: center top` · `transform: scale(1.04)`(테두리 흰선 방지) · `transformOrigin: center top`. 기준 구현 = ProfileSelect·KidHome·BadgeCollection·ParentDashboard·ProfileFormModal — **새 렌더 사이트도 동일 값 사용.**
- ⚠️ **옛 값 폐기:** `scale(1.35) translateY(5%)` + `objectPosition: "... 0%"` + `AVATAR_OFFSET_X{5:"43%"}` — 재가공 이미지에 쓰면 중심이 틀어진다(BadgeCollection 사고).
- 파라미터 있는 함수는 onClick에서 `() => fn()` 래퍼 필수 — 이벤트 객체가 인자로 넘어가는 버그 방지

### 안전도 점수 기준
- 90점 이상 → 안전 (green) / 70~89점 → 주의 (yellow) / 69점 이하 → 위험 (red)

### ⚠️ LLM 사용 원칙 — 사실은 코드가, 분위기만 LLM이 (사고: 키디 스케줄 인사)
**사실(시간·제목·수치·일정)이 필요한 자리에 LLM 자유생성 금지.** 사실은 코드로 렌더, LLM은 톤 한 문장만.
> 사고: 부모 메모 "도복 세탁 완료" → "도복이 준비되어 있으니 편하실 것", 식단 "저녁 카레" → "함께 즐기시면 좋겠다"는 **추천으로 왜곡**. 신뢰가 생명인 안내 기능에서 치명적.
1. **부모 내부 메모·민감 텍스트는 프롬프트에 넣지 말 것** — 재해석되어 없던 내용을 지어낸다.
2. **분위기 파악에 필요한 최소 재료만**(개수·제목) 주고 "그대로 옮기거나 지어내지 말 것" 명시.
3. 한국어는 **`max_tokens` 넉넉히**(100이면 2~3문장에서 잘림), **`temperature`는 낮게**.
4. 화면이 이미 정확히 보여주는 정보를 LLM이 또 풀어쓰게 하지 말 것(중복+왜곡).
- 적용: 키디 인사·리포트·알림 등 **사실을 전달하는 모든 LLM 출력.**

### ⚠️ LLM 반응에는 답의 '틀(시제·의미)'을 프롬프트에 못박을 것 (사고: 볼 것 "동화 봤구나")
**'한 일(과거)'인지 '보고 싶은 것(미래)'인지 LLM은 문맥만으로 헷갈린다.** 라벨만으론 부족 — 시제 틀을 명시하지 않으면 미래를 과거로 뒤집는다. (사고: 볼 것 답 '동화'를 **"동화 봤구나"**로 반응. calm 예시가 과거형뿐이라 그 시제가 오염됨)
1. 질문 유형별 **시제/틀을 프롬프트에 명시** (예: `checkins.py`의 `FRAME_HINT` — 한 일=과거 "~했구나" / 볼 것=미래 "~보고 싶구나", "절대 '봤다'로 말하지 마").
2. **few-shot 예시도 유형별로 분리** — 한 예시를 모든 유형에 재사용하면 시제가 오염된다.
3. 로컬 폴백이 항상 정확하면(예: `WATCH_REACTION`) 의심 시 **LLM 대신 폴백**을 쓰는 서버 안전망도 고려.

## 페르소나 규칙
- 사실만 말한다
- 모르면 모른다고 말한다
- 잘못된 방향이면 바로 말한다
- 실수가 발생하면 CLAUDE.md에 규칙 추가 제안
