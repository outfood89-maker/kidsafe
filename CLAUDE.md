# KidSafe 프로젝트 하네스

> ⚠️ **이 파일은 매 세션 통째로 로드된다. 200줄을 넘기지 말 것** — 길수록 지시 준수율이 떨어진다(공식 권장).
> **여기 있는 것 = `/compact` 후에도 반드시 살아야 하는 것.** 작업별 상세 규칙은 `.claude/rules/` 에 있고, **자동으로 로드되지 않는다 — 직접 열어야 한다**(2026-08-08 실측). 그래서 안전·게이트는 절대 밖으로 빼지 않는다.
> 새 규칙을 넣기 전에 **①장치로 만들 수 있나 ②`.claude/rules/` 로 갈 것인가**를 먼저 묻는다.

## 프로젝트 개요
- AI 기반 어린이 미디어 안전·추천 플랫폼 / 개발자: Freddie(초급 프론트, 포트폴리오 목적)
- 현황: **KT 고도화 국면** — 🔴 **계획서 제출 8/26(수) · 고도화 기간 ~11/20(12주)**. 계획서 정본: `고도화/계획서/KT_고도화계획서_초안_v1.md`(v18·PDF 제출 대기 — KT 답장만 반영하면 끝) / GitHub: https://github.com/outfood89-maker/kidsafe
- **정체성:** '영상 검수 도구'가 아니라 **'아이의 첫 미디어 경험 공간'**. 정체성(B)=키디와의 첫 경험 / 해자(A)=대단한 검수. **B가 부모 가슴을 치고, A가 그걸 믿게 만든다.**
- **데모의 심장:** 키디가 묻는다 → 아이가 답한다 → **부모가 리포트를 열고 뭉클해진다.** 모든 설계는 이 순간으로 수렴.
- 🔴 **윤리 선:** 키디는 아이의 **비밀 친구**. 부모에겐 **아이가 공유 선택한 것 + 감정 흐름 요약만**. 원본 대화 노출 금지.
- 상세: `UPDATE_1/KidSafe_정체성전환_전략.md`(전략) · `UPDATE_1/KidSafe_ClaudeCode_작업지시서.md`(구현)

## 협업 체제와 기록
- Claude Code 창 3개: **팀장**(전략·아동 카피 게이트) / **컨트롤타워**(설계·브리프·리뷰) / **작업자**(구현). 오너가 중계. 시스템 프롬프트는 **`prompts/README.md`** 부터.
- ⚠️ `prompts/` 는 반드시 커밋 유지 (2026-08-01 유실 직전에 복구한 이력)
- **기록 2종을 섞지 말 것** — `WORKLOG.md` = *"어디까지 했지?"*(진행·지뢰, 작업 주체) / **`DECISIONS.md`** = *"뭘 배웠지?"*(결정·이유·배움, **팀장**이 5항목으로: 결정/왜/배운 것/자산/되돌리려면). 새 안건은 **`DECISIONS.md` 부터 읽고** 시작 — 같은 논의를 반복하지 않기 위해.
- 🔴 **오래 걸리거나 비싼 것(병렬 에이전트·워크플로·대량 실행)은 오너의 "해"를 받고 시작한다.**
  ⚠️ **규모를 알리는 것은 승인이 아니다** — 08-11 에 *"13개 돌립니다"* 만 말하고 시작해 지적받았다(08-02 에 42개·182만 토큰 전력).

## 기술 스택
- 프론트: React 19 · React Router v7 · Tailwind · Axios · Recharts / 백엔드: FastAPI(Python, `server/`)
- AI: Anthropic — 실시간(챗봇·체크인·스케줄)은 `claude-haiku-4-5-20251001`, **리포트·AI 코치는 Sonnet**(`REPORT_MODEL`)
- 영상: YouTube Data API v3 / 아바타: 로컬 PNG(`client/public/images/avatars/`)
- 배포: Vercel(프론트 `kidsafe-eight.vercel.app`) + Railway(백엔드 `kidsafe-production.up.railway.app`)

## 현재 무게중심
- ✅ 검수 고도화(Tier0~2) · 정체성 전환 P0 3건 · **GD-8 그림일기 서버 이전 완주** · **B4 회원 탈퇴**(파일까지 · 2026-08-10 배포)
- 🟢 **서버 저장이 켜져 있다**(2026-08-09 배포) — 아이 일기가 어느 기기에서든 열린다. 게이트는 두 겹(킬스위치 ∧ 보호자 동의).
  되돌리려면 **두 줄 함께**: `diaryStore.DIARY_SERVER` + `Privacy.jsx` 의 `DIARY_SERVER_LIVE`(한쪽만 끄면 검사가 막는다)
- 🔵 **다음** — 지도는 `고도화/README.md`. 당장은 **앨범 내려받기**(🔴 정리의 전제조건 — 없으면 *"아이 그림을 버리세요"* 가 된다).
- 🔴 **출시 전 필수 1건 = 전역 상한** (⚠️ quota 가 메모리라 걸 자리가 없다 — DB 표 먼저). ~~원가 실측~~✅08-10 · ~~업로드 한도·저장 용량~~✅08-11 · [비용 방어 3층](고도화/원가와가격/비용_방어_3층과_남은_구멍.md)

## 🔴 커밋 게이트 (2026-08-04 오너 확정)

**`master` = 배포다.** Vercel·Railway 가 master 를 보고 자동 배포한다 → 검증 안 된 코드를 올리면 **실제 서비스가 깨진다.**

| 대상 | 커밋·푸시 |
|---|---|
| **문서** (md·조사자료·브리프·설계) | ✅ 자유롭게 즉시 |
| **기능 코드** (`client/`·`server/`) | 🔴 **①검증 통과 + ②오너 시범 테스트 완료** 둘 다 끝난 뒤에만 |

- 코드 작업이 끝나도 **스스로 push 하지 않는다.** 검증 결과를 보고하고 **오너의 "올려도 된다"를 받은 뒤** 푸시한다.
- ⚠️ **유실 방지와의 균형:** 7/12 코드 유실 사고(미커밋 상태로 맥 이전)를 반복하지 않는다 → **로컬 커밋은 하되 push 만 보류.**

## 🧪 검증 고정 축 4개 (2026-08-05 오너 확정 — 매 작업 필수)

> **왜 고정인가:** 검증 항목을 매번 즉흥으로 정하면 **축이 통째로 빠진다.** 아래 사고들은 전부 기능도 동작했고 문법도 맞았다 — **아무도 그 질문을 던지지 않았을 뿐이다.**
> **틀린 답은 시끄러워서 결국 잡히고, 안 던진 질문은 조용히 배포된다.**

| # | 질문 | 이 축이 빠져서 난 사고 |
|---|---|---|
| 1 | **기존 동작이 보존됐나?** | `efadd94`(검색 포즈 삭제) · KidHome 검색바 실종 · `videoId` 덮어쓰기 |
| 2 | **무거워지지 않았나?** | 배럴 import — 아이콘 2개 쓰려다 **1,611개** 로드, 테스트 9.45s→**120s** |
| 3 | **데이터가 새지 않나? (윤리선)** | ⚠️ **아직 안 터짐 — 터지면 제일 크다.** 새 기능마다 *"이 데이터가 부모 화면으로 새는 경로가 있나?"* 를 반드시 물을 것 |
| 4 | **사실이 왜곡되지 않나?** | 키디 스케줄 인사(부모 메모 재해석) · 볼 것 "동화 **봤구나**"(미래→과거) |

⚠️ **2번은 "테스트 통과"로 대체되지 않는다.** 실행 시간·번들 크기를 **작업 전후 비교값으로** 확인할 것.

## 🔁 멈추고 **새 각도**를 찾아야 할 신호 (2026-08-05·06 오너 확정)

기계는 **"안 던진 질문"** 을 대신 물어줄 수 없다. 그래서 오너의 말이 그 역할을 한다.

**트리거 ①** — 팀장이 *"검증 완료 · 끝났습니다 · 문제없습니다 · 전부 통과"* 라고 하면 → 오너: **"다시 한번 검증해봐."**
**트리거 ②** 🔴 — **오너의 확인 질문도 같은 트리거다.** *"이게 뭐야?"* · *"이해가 안 가는데?"* · *"이거 ○○ 말하는 거지?"* 는 지적이 아니라 **질문**인데, 실측해보면 **거기서 오류가 나온다.**

| 오너 질문 | 답하려다 발견한 것 |
|---|---|
| *"이 말이 아직 이해가 안 가거든?"* | 분당 3회만으론 **하루 4,320회(월 200만원)** 통과 — 내 제안이 틀렸다 |
| *"제대로 실행이 안되는듯?"* | 코드는 정상. **위기 사전에 칸 자체가 없었다** |
| *"혹시 게임 때문 아니야?"* | 워커 타임아웃은 부하였다. **그 밑에 내 문법 오류가 깔려 있었다**(2026-08-09) |

**대응 규칙**
1. **새 각도를 찾는다.** 답변에 *"이번엔 ○○를 새로 봤습니다"* 를 **반드시 명시.** 같은 검사 반복은 무의미.
2. **방어하지 않는다.** *"이미 다 봤습니다"* 금지. 그 말을 들은 순간 **아직 안 본 게 있다고 가정.**
3. **설명을 요구받으면 기억으로 답하지 말고 근거를 다시 열 것.** 설명하려면 계산해야 하고, 계산하면 드러난다.
4. 정말 없으면 정직하게 — *"더 볼 축이 없다, 이유는 X"*. 억지로 만들지 않는다.

> 🔬 **이 대응의 정식 절차 = 「정밀검수」** (2026-08-11 명명). 오너가 *"정밀검수·다시 검증"* 이라 하면
> → **`.claude/rules/정밀검수.md` 를 먼저 `Read`** (또는 `/정밀검수`). **각도 목록**과 **검사 자신을 의심하는 8항목**이
> 거기 있고 요약만으로는 못 한다. 실적: *"전부 통과"* 뒤에서 **버그 3건 + 검사가 헛돌던 것 6건**.

## 🔴 내 결론을 **더** 의심해야 할 때 (실사고: 2026-08-06)

**가장 위험한 순간은 오너가 바라는 답을 찾아드리려 할 때다.**
사고: 오너가 *"검수는 기본 기능으로 가고 싶다"* → 측정도 *"싸다"* 로 나옴 → 팀장이 **"한도를 풀자"로 먼저 기울고 그 뒤에 근거를 찾았다.**
숫자를 넣어보니 분당 3회만으로 **하루 4,320회 · 월 200만원**. 같은 날 오전 `quota.py` 에 *"분당=얼마나 빨리 / 하루=얼마나 많이"* 라고 **직접 써놓고** 어겼다.

**핵심: 계산을 빠뜨린 게 아니라 계산할 생각이 안 들었다.** 이미 답을 안다고 느꼈기 때문이다 — **다짐으로는 안 막힌다.**

**신호 3개 — 하나라도 켜지면 멈추고 숫자부터 넣을 것**
1. 오너의 희망과 내 결론이 **같은 방향**이다
2. 유리한 측정치가 방금 나왔다 (*"캐시 적중률 97%"*)
3. 내가 만든 안전장치를 **내가 완화하자고** 하고 있다 ← 가장 강한 신호

**⚠️ 평균으로 최악을 재지 말 것.** 적중률 97% 는 정상 사용자 값이다. 비용을 태우려는 쪽은 매번 새 ID 로 **캐시를 일부러 피한다**(0%). *"평소엔 싸다"* 와 *"최악에도 안전하다"* 는 다른 질문이다.

## 🔴 "이미 만들었으니 그거 쓰자"고 할 때 (실사고: 2026-08-07)

**내가 만든 것을 재사용하자는 제안은 가장 편한 결론이라 검증을 건너뛴다.**
사고: *"이사는 재푸시에 이미 있으니 축소하자"* 고 권고했는데, 브리프를 끝까지 읽어보니 **명시적으로 금지한 `Promise.all`** 을 내 구현이 하고 있었고 트리거도 금지된 **아이 화면**에 있었다.

1. **A(재사용)를 제안하기 전에 B(신규 설계) 문서를 끝까지 읽는다.** 요약·소제목만 보고 판단하지 않는다.
2. **"이미 있다"의 근거를 기능 이름이 아니라 *성질*로 댄다** — 동시성·순서·트리거 위치·실패 처리까지 같은가?
3. **기존 설계가 금지한 것이 내 구현에 있는지 먼저 grep 한다.** 설계자는 이유가 있어서 금지했다.
4. 비교표에 **반대안의 장점을 내가 직접 찾아 적는다.** 안 나오면 아직 안 읽은 것이다.

## 🔴 방어를 고칠 때, 그 방어가 **딛고 선 판정**도 실물로 시험할 것 (실사고: 2026-08-07)

GD-A3(위기 발화를 되읽지 않기)를 고쳤다. 테스트 4건·차단 시험·전체 검증 전부 통과. 그런데 오너가 **"아빠가 날 죽인다고 얘기했어"** 라고 말하자 그대로 새어나갔다.
**원인: 테스트 문장을 방어 코드에서 역산했다.** 그러면 *"방어가 옳게 도는가"* 만 검증되고 *"방어가 딛고 선 판정이 옳은가"* 는 한 번도 검증되지 않는다.

1. **조건문을 고쳤으면 그 조건을 만드는 함수도 실물 입력으로 돌려본다.** `if (screenText(t))` 를 고쳤으면 `screenText` 에 **실제 사람이 할 법한 말**을 넣어본다.
2. **테스트 문장을 코드에서 역산하지 않는다.** "실제 상황에서 사람이 뭐라고 말할까"에서 출발한다.
3. **목록류는 '있는 항목 검토'가 아니라 '없는 칸 찾기'.** 분류 축을 그리고 **빈 칸을 센다.**
4. **오탐 케이스를 함께 못박는다.** 정탐만 있는 검사는 방어를 넓히는 쪽으로만 밀린다.
5. 🔴 **한국어 패턴은 어미를 손으로 나열하지 말고 조합으로 전개한다** — `"때리"` 와 `"때린"` 은 컴퓨터에게 다른 글자다. 손으로 적어서 **두 번 빠뜨렸다.**
6. 🔴 **미탐이 오탐보다 훨씬 나쁘다.** 오탐(위로 한 번 더)의 대가와 미탐(학대 신호를 놓침)의 대가는 비교 대상이 아니다 — **오탐이 보인다고 사전을 좁히지 말 것.**
7. 🔴 **위기 발화는 되읽지 않는다.** 아이 발화를 TTS 로 되읽는 경로를 새로 만들면 스크리닝을 **먼저** 돌리고, 위기면 되읽기를 **통째로 건너뛴다**(외부 전송 + 화면 표시를 동시에 막아야 한다).

> ⚠️ 사전을 만질 때는 `.claude/rules/safety-lexicon.md` 를 **직접 열어 읽을 것** — 자동 로드되지 않는다(2026-08-08 확정).

## 코드 규칙 (필수)
- **모든 주석과 답변은 한국어** (thinking 포함)
- 함수형 컴포넌트만 (class 금지) · Axios 만 (fetch 금지) · Tailwind 만 (인라인 style 은 불가피할 때만) · try-catch 필수
- 백엔드는 Python `import/from` (`require()` 없음) · **라우터 추가 시 `server/main.py` 에 import + `include_router` 등록**
- 파일 경로는 항상 명시

## 파일 및 코드 관리
- 🔴 **기존 파일 절대 삭제 금지** (VideoModal.jsx 삭제 사고)
- 🔴 **기존 코드(함수·state·UI 섹션)를 삭제하거나 크게 바꾸기 전에 반드시 오너에게 먼저 물을 것**
- 기능을 끄고 싶으면 삭제 말고 **주석 처리**(복구 가능하게)

## 🔒 자동으로 걸리는 장치들 — 이건 내가 무시할 수 없다

말로 하는 규칙은 안 지킬 수 있지만 **장치는 무시할 수 없다.** 새 규칙을 만들 때 **장치화가 가능한지 먼저 묻는다.**

| 장치 | 막는 것 |
|---|---|
| `scripts/hooks/pre-commit` (자동 설치 — `npm install` 시) | 아래 검사 전부. 문서만 고쳤으면 0.4초에 통과 |
| 🧭 **하네스 무결성** (문서여도 검사) | **200줄 초과** · 하네스가 **없는 파일을 가리키는 것** · 표에 없는 **고아 규칙 파일** |
| `server/_verify_all.py` **11종** | 인증 기준선 · 인가(소유권) · 🐤 윤리선 카나리아 · 💸 비용 상한 · 🚨 위기 사전 · 📓 저장 계약 · 🧹 고아 자산 · 🗑 탈퇴(계약·실행) · 📦 저장 용량 · 가짜 주입 도구 |
| 배럴 import 차단 | `react-icons/fa` 류 통짜 import (테스트 9.45s→120s 사고) |
| `server/quota.py` `_validate_limits()` | **하루 한도 없는 종류를 아예 못 만들게** 한다 |
| `_verify_lexicon.py` | 서버↔클라 위기 사전이 갈라지는 것 (`node` 로 실제 실행해 비교) |

- 긴급 우회 `git commit --no-verify` — ⚠️ **검증 안 된 코드의 푸시는 여전히 금지** · 배경은 `문서/사고기록_디버깅과장치.md`

## 📍 상세 규칙은 어디에 있나

**`.claude/rules/` — 🔴 자동 로드되지 않는다. 필요하면 `Read` 로 직접 연다.**

> 🔴 **2026-08-08 실측 — "해당 파일을 열면 자동 로드된다"는 사실이 아니었다.**
> 그걸 해주는 기제가 어디에도 없었다(`@import` 도, 훅도 아님). 믿음이었을 뿐이다.
> 증거: 같은 날 `frontend.md` 의 **배럴 import 금지**를 어겼고, 커밋 훅이 막아서야 알았다.
> 규칙이 컨텍스트에 있었다면 애초에 안 썼을 것이다.
>
> ⚠️ **하네스에 거짓이 적혀 있는 것이 규칙이 없는 것보다 나쁘다** — 없는 보호를 있다고 믿게 된다.
> ⇒ **진짜 보장은 장치다.** 같은 날 배럴 import 를 막은 것은 규칙이 아니라 pre-commit 훅이었다.
>    규칙은 '왜'를 남기고 **지키게 하는 것은 장치**다. 새 규칙을 만들 때 이 순서를 지킨다.
> - **마이크·위기 판정·아이 발화를 다루는 파일을 새로 만들면** → `safety-lexicon.md` 를 **먼저 읽는다**
> - **새 검증 파일은 반드시 `server/_verify_*.py` 로 이름 짓는다** — 그래야 규칙이 걸리고 `_verify_all.py` 등록도 잊지 않는다

| 파일 | 언제 | 내용 |
|---|---|---|
| `backend.md` | `server/**/*.py` | 🔴 `except Exception` 이 `HTTPException` 삼킴 · FastAPI 주의 8건 · LLM 사용 원칙 · 모델 배치 |
| `frontend.md` | `client/src/**` | 🔴 성능 회귀(배럴 import) · 레이아웃 리라이트 시 동작 보존 · KidHome 구조 · UI/아바타 |
| `verify-and-mocks.md` | `_verify_*.py`·`__tests__/`·훅 | 🔴 가짜 주입 3원칙 · 대조군 필수 · 카나리아 · 차단 시험 |
| **`정밀검수.md`** 🔬 | 오너가 *"정밀검수·다시 검증"* 이라고 할 때 · **"다 끝났다"를 말하기 전** | 🔴 **각도 목록**(안 쓴 것을 고른다) · 검사 자신을 의심하는 8항목 · `/정밀검수` 로도 열림 |
| `safety-lexicon.md` | 사전·체크인·그림일기 | 🔴 아동 안전 사전 (하루에 두 번 뚫린 자리) |

**읽어야 할 때만 여는 문서** — `문서/사고기록_디버깅과장치.md`(디버깅 절차·훅 배경·**팀장이 틀렸던 패턴 모음**) ·
`고도화/README.md`(36항목 지도) · `고도화/기능별/GD-37_위기감지_근본개선.md`(위기 감지의 한계)

## 페르소나 규칙
- 사실만 말한다 · 모르면 모른다고 한다 · 잘못된 방향이면 바로 말한다
- 보고는 **비유 먼저, 용어 나중** (오너 눈높이)
- 실수가 발생하면 **①장치로 만들 수 있나** 먼저 묻고, 안 되면 규칙을 **가장 좁은 자리**에 추가 제안

### 🔴 **저장된 값**에서 **화면 동선**을 추론하지 않는다 (실사고: 2026-08-09 — 하루에 3번)

`imgSource:"mine"` → *"직접 그리기는 무료"* (실은 AI 호출 **뒤**의 선택지 — 🔴 **오너가 믿고 돈을 썼다**)
`imgState:"idle"` → *"그림 없이 저장된다"*(불가) · `tearEntry` → *"확인 없이 찢긴다"*(확인·경고·죄책감방지 전부 있었다)
**데이터에 값이 있다 ≠ 사용자가 그 상태에 도달한다.** 동선은 **버튼 렌더 조건과 `onClick`** 을 열어 본다.
⚠️ **돈이 나가는 안내**는 확인 없이 하지 않는다. 아이디어 승격도 같다 — *"이미 있지 않나"* 를 먼저 실측할 것.

### 🔴 "다 끝났다"를 말하기 전에 **확인하고 말한다** (실사고: 2026-08-09)

*"기록은 다 남겼습니다"* 는 **그때는 사실이었다.** 그 뒤 15분간 기능을 더 만들어 커밋하고, 마무리 인사에서
**같은 말을 그대로 다시 했다** — 그 시점엔 거짓. **앞선 시점의 사실을 기억으로 재사용한 것.**

> ⚠️ *"기억으로 답하지 말고 근거를 열라"* 는 질문받을 때만이 아니다. 스스로 "끝났다"고 선언할 때가
> **더 위험하다**(아무도 안 물으면 그대로 넘어간다). 🔬 그래서 **여기서도 「정밀검수」를 한 번** 돌린다.

**완료·마무리를 말하기 전에 한 줄:**
```bash
git log --oneline $(git log -1 --format=%H --grep='^docs')..HEAD
```
마지막 기록 커밋 **이후의 커밋**이다. 여기 코드 커밋이 남아 있으면 **기록이 밀린 것.**
