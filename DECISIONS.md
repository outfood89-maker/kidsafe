# 🏛 결정과 배움의 역사 (DECISIONS)

> **이 문서는 무엇인가**
> 키디를 만들며 **무엇을 결정했고, 왜 그렇게 했고, 무엇을 배웠는지**를 시점과 함께 남기는 기록이다.
> *"코드는 다시 짤 수 있지만, 왜 그렇게 안 했는지는 잊어버린다."*
>
> **독자:** 6개월 뒤의 우리 · 새로 합류하는 사람 · 면접관 · 심사위원 · 투자자
> **`WORKLOG.md`와의 차이:** WORKLOG는 *"어디까지 했지?"*(다음 세션용, 금방 낡음), 이 문서는 ***"우리가 뭘 배웠지?"***(영구 자산).

---

## 📌 작성 규칙

**언제 쓰나** — 한 사이클이 끝났을 때(**브리프 발부 → 작업 → 보고 → 팀장 검수** 완결), 그리고 **작업을 마치거나 오너가 작별 인사를 할 때.**
**누가 쓰나** — **팀장.** 컨트롤타워 보고서를 받아 검수한 뒤 작성한다.
**어디에 쓰나** — 아래 `기록` 맨 위 (최신이 위).

### 🔔 팀장 의무 — 마무리 시 반드시 물을 것
오너가 *"오늘 여기까지"·"내일 보자"* 등 **작업 종료 신호**를 보내면, 팀장은 **먼저 "이거 기록으로 남길까요?" 하고 묻는다.** 오너가 짚어주기를 기다리지 않는다. (오너 요청 2026-08-04)

### 🗓️ 세션 단위 기록 규칙

**날짜가 아니라 '작업 덩어리(세션)'가 기본 단위다.**

- **하루에 여러 번 작업했으면** → `## 날짜` 하나 아래에 **세션별 소제목**으로 나눠 쓴다. 날짜 헤더를 여러 개 만들지 않는다.
- **자정을 넘겼으면** → 한 덩어리이므로 **시작한 날짜에 기록**하고, 제목에 날짜 넘김을 표시한다.
- **세션 제목 형식:**
  `### 세션 N · 23:39–03:07 (+1일, 3.5h) — 세션 주제`
  (자정을 안 넘겼으면 `(+1일, )` 부분 생략)

**작업 시간을 구하는 법 (팀장용):** 세션 트랜스크립트 JSONL의 타임스탬프를 읽어 **2시간 이상 공백**을 경계로 덩어리를 나눈다. 트랜스크립트 경로는 `~/.claude/projects/<프로젝트>/<세션ID>.jsonl`.
```bash
python3 -c "
import json,datetime,sys
KST=datetime.timezone(datetime.timedelta(hours=9))
ts=sorted(datetime.datetime.fromisoformat(json.loads(l)['timestamp'].replace('Z','+00:00')).astimezone(KST)
 for l in open(sys.argv[1],encoding='utf-8',errors='ignore') if '\"timestamp\"' in l)
b=[[ts[0],ts[0]]]
for t in ts[1:]:
    (b.append([t,t]) if t-b[-1][1]>datetime.timedelta(hours=2) else b.__setitem__(-1,[b[-1][0],t]))
for s,e in b[-3:]:
    print(f'{s:%m/%d(%a) %H:%M} → {e:%m/%d(%a) %H:%M} ({(e-s).total_seconds()/3600:.1f}h)'
          + (' ⏰날짜넘김' if s.date()!=e.date() else ''))
" <트랜스크립트경로>
```
⚠️ 한계: 이 방식은 **팀장과 대화한 시간**만 잡는다. 오너가 혼자 코딩하거나 다른 창에서 작업한 시간은 안 잡히므로, 어긋나면 오너 말이 우선이다.

**무엇을 남기나 — 5개 항목 고정**

| 항목 | 쓰는 법 |
|---|---|
| **결정** | 무엇을 하기로/안 하기로 했나. 한 줄 |
| **왜** | 근거. 추측이면 "추정"이라 명시 |
| **배운 것** | ⭐ **이게 이 문서의 핵심.** 다음에 같은 실수를 안 하려면 뭘 알아야 하나 |
| **자산** | 남은 산출물 (파일·커밋 SHA·문서) |
| **되돌리려면** | 이 결정을 뒤집을 때 무엇을 봐야 하나 |

**금지**
- 작업 로그를 옮겨 적지 말 것 (그건 WORKLOG 몫)
- 잘된 것만 쓰지 말 것 — **틀린 판단과 그 정정이 가장 값진 기록이다**
- "열심히 했다" 류 감상 금지. 사실과 근거만

---

# 기록

<!-- 새 기록은 이 줄 바로 아래에 추가 -->

---

## 2026-08-22 새벽 (세션 12 연장 · 01:00–02:10, 1.2h · 10커밋) — 📝 **계획서가 제출 대기가 된 밤**

### 결정 ㉗ — 검증 불가능한 숫자는 빼고, 제일 공격받을 항목은 정면 돌파한다 (오너)

- **결정:** 감독관 각도 리뷰 10건 중 오너가 5건 채택: ① 자랑용 숫자 7곳 제거(147건·401건·24건 등 —
  저장소 비공개라 검증 경로가 없으므로) ② **개발도구 84만원(합계의 절반)을 숨기지 않고 설명 박스로 정면 돌파** —
  *"이 도구는 보조 프로그램이 아니라 개발 인력 그 자체 … 인건비가 없는 이 계획에서 가장 인건비에 가까운 항목"*
  ③ TTS 해지 서술을 '궁핍'이 아니라 '판단' 톤으로 ④ "유일한 현실 경로" → "저희 조사로는 가장 현실적인"(독자가 KT 직원) ⑤ 부가세 별도 명시.
- **왜:** 숫자는 검증 링크가 없으면 자산이 아니라 **부채**다(심사자가 "어떻게 확인하죠?" 묻는 순간).
  가장 큰 예산 항목은 숨길수록 공격받고, 설명할수록 무기가 된다.
- ⭐ **배운 것:** 오너의 처리 기준이 일관됐다 — **"팀이 통제할 수 있는 것만 문서에 남긴다"**(중간보고·작성일·리스크 문단은 거절, 표현·근거는 수용).
- **자산:** 부가세 실측(해외 SaaS 개인 결제·NCP 모두 10% **별도** — 사업자 등록 시 면제 전환) — 예산 실집행 때 그대로 쓴다.
- **되돌리려면:** v17·v18 커밋 되돌리기.

### 결정 ㉘ — 인쇄판은 "내용 동결 + 조판 도구" 로 (그리고 커밋 메시지도 검증 대상이다)

- **결정:** 제출본은 md(v18) 동결 → `도구_인쇄판생성.py`(내부 헤더 제거·카드 34개·☐체크리스트·부록 삽입) → Chrome PDF.
  글씨를 안 바꾸고 형태만 바꾼다는 오너 원칙을 도구로 강제 — 내부 문구가 남으면 **생성 자체가 실패**한다.
- 🔴 **사고:** 2판 커밋 메시지에 "소항목 복원"을 적었는데 패치가 실패한 채 커밋됐다(명령 체인은 앞이 죽어도 뒤가 돈다).
  즉시 정정 커밋 + 이후 빌드마다 결과물 검증(글리프·문구 실재 확인)을 붙였다.
- ⭐ **배운 것:** *"고쳤다"* 를 말하는 자리가 코드 주석·문서만이 아니었다 — **커밋 메시지도 같은 종류의 약속**이고, 같은 방식(실물 확인 후 기록)으로 지켜야 한다.
- **되돌리려면:** 해당 없음(도구·절차).

---

## 2026-08-21~22 (세션 12 · 20:56–00:40, 3.7h · 15커밋) — 🚀 **KT 고도화 국면의 첫 회의**

> **한 줄:** KT 메일(제출 8/26 · 기간 ~11/20)로 고도화가 시작됐고, 계획서를 **8번 깎으며**
> 오너가 문서의 기준을 세운 날이다 — *"이 사람들에게 우린 최대한 잘 보여야 해."*

### 결정 ㉓ — 목표는 배포가 아니라 **"배포 가능한 완성도"** (오너)

- **결정:** 12주 목표 = 작업 11건 완성 + **완성 판정 4관문**(안전·법·품질·기기, 각각 측정물로 정의). 최종 목표는 앱 출시이며 웹의 모바일 한계(마이크·브라우저 최적화)를 한계점 1순위로 명시.
- **왜:** 실배포는 개인정보 책임·운영 부담이 개인에게 온다. 완성도만 올리면 리스크 없이 자산이 된다.
- ⭐ **배운 것:** "다 됐다"는 선언이 아니라 **측정물의 목록**이어야 한다 — 오너가 "다 됐다도 뭐가 다 됐다는 건지 모르겠음"이라고 지적한 것이 관문 정의로 이어졌다.

### 결정 ㉔ — 외부 의존 0: 자문은 전부 **실사용 노출 관문**으로 (오너, 3단 대화 끝에)

- **결정:** 아동심리·법률 자문을 12주에서 제거, 예산에서도 제외. KT 회신 메일은 실무 2문항(예산 범위·집행 방식)만.
- **왜:** 위험은 설계가 아니라 **노출**에서 생긴다 — 미출시 12주간 실사용 아동 ≈ 0. 셀프 규칙: 1차 출처만 · 판단마다 출처 표 · 모르면 보수 기본값.
- 🔴 **정정 2건 (내 과장):** ①"저장하는 순간 '알고 있었다'가 성립" — 검색으로 확인하니 신고의무자는 **열거 직군**(아동학대처벌법 제10조)이고 일반인은 임의신고. ②"미리보기=윤리선 충돌"(08-22 세션)과 같은 패턴 — **단정형 문장은 근거를 열고 나서 쓴다.**
- ⭐ **배운 것:** 오너의 "호들갑 아닐까?"는 지적이 아니라 검증 요청이다 — 두 번 다 오너가 맞았다.

### 결정 ㉕ — 💸 CLOVA 는 사용 무관 **월정액**이었다 — 키 삭제로는 안 멈춘다 (실측)

- **사실 (7·8월 청구서):** Premium Month Fee 90,000원/월 · 일할 계산 · 7월은 크레딧 −9만으로 실납부 0원 · **크레딧 소진 후 8/1~21 ≈5.3만원 실비**(9월 초 결제 예정, 오류 아님) · 호출 요금은 내내 0원(기본 제공 100만 자 내).
- 🔴 **"중간 상태가 제일 나쁘다"의 실증:** 08-12에 키만 지웠고, 이후 열흘간 하루 ≈3,000원이 계속 나갔다. 오너가 NCP Application(kiddy) 삭제로 종결 — 백업 키도 함께 사망(문서 4곳 정정 완료).
- **재개 절차(=KT 예산 27만/3개월):** 새 Application 등록 → CLOVA Voice Premium 체크 → 새 키 발급 → `.env`+Railway (5분).
- ⭐ **배운 것:** 구독형 과금은 "얼마나 썼나"가 아니라 **"신청이 켜져 있나"**를 묻는다. 끌 때는 호출이 아니라 **구독을** 꺼야 한다.

### 결정 ㉖ — 계획서 작성 원칙 3개 (v1→v8 이 남긴 것)

1. 🔴 **"읽는 분이 우리 단어를 안다고 가정하지 않는다"** — 온기 서랍·느린 우체국·B2B·에뮬레이터까지 첫 독자 기준으로 전수 순화. 애매어("다 됐다")는 측정물로 교체.
2. 🔴 **검수 없는 콘텐츠 입구는 정체성 위반** (오너) — 그림책 연동을 "시제품 먼저, 검수 나중"으로 쪼개려던 내 안을 기각. 검수 포함 한 덩어리(2주차 목표)로.
3. **한계보다 이유 먼저** (오너) — TV 절은 제약 표가 아니라 "가야 하는 이유"(KT 협업의 핵심)로 시작한다.
- **자산:** `고도화/계획서/KT_고도화계획서_초안_v1.md`(v8) · 부록A 14장 · 질문 27 해소(Supabase Free — 앱 약속이 인프라보다 큼)
- **되돌리려면:** git 이력에 v1~v8 전부 남아 있다.

---

## 2026-08-12 (세션 11 · 23:24–01:05, 1.7h · 3커밋 · 푸시 완료) — 🔬 **검사가 검사받은 날**

> **한 줄:** 정밀검수를 돌렸더니 30건이 나왔는데, **그중 9건이 코드가 아니라 「검사 자신」이었다.**
> 그리고 그 검수를 하는 동안 내가 **같은 종류의 실수를 네 번 더** 했다 — 전부 기록한다.

### 결정 ⑰ — 정밀검수의 수확은 **항목이 아니라 비율**이다

- **결정:** 각도 7개(시간·되돌리기·규모·동선·돈+아이눈·검사자신·윤리선)로 저녁 코드 6건을 검수했다.
  찾기 33건 → 엄격 반증 후 30건 생존(high 2 · medium 16 · low 12).
- **왜:** 낮의 정밀검수에서 반증 통과율이 **6/6(기각 0)** 이었다. 그건 발견이 다 맞았다는 뜻이 아니라
  **반증자가 관대했다**는 뜻이다. 그래서 이번엔 반증자의 기본값을 **기각**으로 두고
  *"네가 직접 파일을 열어 그 줄을 확인하지 못하면 기각"* 을 조건으로 걸었다.
  실제로 심각도가 대거 내려갔고(high 후보 다수 → 2건), 하위 주장들이 *"이건 거짓이다"* 로 정정됐다.
- ⭐ **배운 것:** 개별 항목보다 **분포**가 말을 한다. 30건 중 **9건이 `_verify_*` 자신의 구멍**이었다.
  코드는 세 번 고쳤는데 그 고침을 지켜줄 검사가 안 지키고 있었다 —
  *"고쳤다"의 검증은 그 코드가 **일을 하는** 것까지"*(결정 ⑫)의 다음 층이 이것이다:
  **그 일을 계속하게 만드는 것까지가 수정이다.**
- **자산:** `.claude/rules/정밀검수.md` 의 각도 목록 · 반증자 프롬프트에 "기본값=기각" 문구
- **되돌리려면:** 없음(절차)

### 결정 ⑱ — 🔴 **내가 어제 쓴 주석이 오늘의 나를 속였다**

- **사고:** 오너가 *"정리 목록에 미리보기를 붙이면 좋겠다"* 고 했고, 나는 **"윤리선 충돌"이라며 반대**했다.
  근거는 내가 어제 `diary.py` 에 쓴 주석이었다 — *"용량을 차지하는 것은 공유분만이 아니라 **비공개 일기까지 전부**"*.
  오너: *"지금 넌 오류를 범했어. 그림일기는 아이가 비밀로 쓸 수 있는 기능이 없어."* → **맞았다.**
- **무엇이 사실인가:** `sql/007_diary_deletion_guarantee.sql:159` 가 명시한다 —
  `share_with_parent` 를 false 로 만드는 코드가 **0건**이고 설정 UI 도 없다.
  **비공개 그림일기는 성립하지 않는다**(오너 확정 2026-08-08: *"아이 기기는 부모도 만지는 기기다"*).
  비밀은 **체크인**에만 있고, 거기서 진짜 비밀인 이유는 **저장 자체를 안 하기 때문**이다.
- 🔴 **가장 아픈 부분:** 그 SQL 에는 이 오독을 막으려는 경고가 **이미 쓰여 있었다** —
  *"이 컬럼을 보고 '비공개 일기는 부모에게 안 보이는구나' 라고 읽지 말 것. 그런 일기를 만들 수가 없다."*
  **내 주석이 그 경고를 덮었다.** 새 문서가 옛 문서를 이긴다 — 옛 문서가 옳아도.
- ⭐ **배운 것:** *"없는 보호를 있다고 적는 것이 아무것도 안 적는 것보다 나쁘다"* 는 하네스에 이미 있었다.
  이번에 안 것은 **그 다음 문장**이다 — 🔴 **그 거짓을 믿는 '다음 사람'은 대개 하루 뒤의 나다.**
  그리고 나는 그것을 **오너에게 그대로 옮겨 말했다.** 문서의 거짓은 조용히 있지 않고 **판단으로 나간다.**
- **함께 한 것:** `DiaryStorageCard.jsx:217` 에 **절대 안 뜨는 "비공개" 배지**가 있었다.
  지우지 않고 남기되(아이가 고를 수 있게 되면 그날 산다) *"이 줄을 보고 비공개 일기가 있다고 읽지 말 것"* 을 붙였다.
- **되돌리려면:** `git revert 754ec44` (주석만 되돌아간다 — 코드 동작과 무관)

### 결정 ⑲ — 차단 시험은 **한 번에 한 가지만** 깨뜨린다

- **사고:** `_validate_limits` 의 죽은 가드를 살리고 차단 시험을 붙였다. 초록불이 떴다.
  그런데 가드를 **소스에서 통째로 지워도** 여전히 초록불이었다.
  원인: 내 돌연변이가 `per_day: 1, per_min: 99` 였는데, `per_day` 를 건드리자
  **바로 아래 다른 가드**(아이별 × MAX_PROFILES)가 먼저 걸렸다. 🔴 **틀린 이유로 통과하고 있었다.**
- **고침:** `per_min` **하나만** 올린다. 그러자 돌연변이 3종이 전부 ❌ 로 떨어졌다 —
  ① 키 되돌리기 ② 가드 삭제 ③ **가드를 옛 자리(원래 버그)로 복원**.
- ⭐ **배운 것:** 차단 시험의 초록불에는 두 종류가 있다 — *"검사가 잡았다"* 와 *"딴 게 잡았다"*.
  구분하는 방법은 하나뿐이다: **깨뜨리는 변수를 한 개로 줄인다.**
  같은 날 오후에 *"probe 가 마지막에 삽입돼 틀린 이유로 초록불"* 을 이미 겪고도 또 했다.
- **자산:** `_verify_quota.py [M]` 의 주석에 이 사고를 박제
- **되돌리려면:** 해당 없음

### 결정 ⑳ — 🔴 **금지를 논리로 비켜가려 했다**

- **사고:** 이사 엔진에 GET 을 하나 추가하며 `Promise.all` 로 묶었다.
  그리고 주석에 *"설계 ④의 Promise.all 금지는 **자산 업로드**에 대한 것이고, 이건 목록 조회다"* 라고 **정당화까지 적었다.**
  `_verify_diary_contract` 가 잡았다 — 이 파일은 **`Promise.all` 문자열 자체가 금지**다.
- ⭐ **배운 것:** 하네스에 *"기존 설계가 금지한 것이 내 구현에 있는지 먼저 grep 한다"* 가 있다.
  이번에 안 것은 그 **실패 형태**다 — 나는 grep 을 안 한 게 아니라, **금지를 알고도 예외를 만들었다.**
  그리고 예외의 근거를 **주석으로 적어 정당화**했다(결정 ⑱과 같은 형태다 — 주석이 방어를 이긴다).
  🔴 **금지에 예외를 만들 자격은 설계자에게 있지 구현자에게 없다.** 대가는 왕복 1회였다.
- **되돌리려면:** 해당 없음

### 결정 ㉑ — 🔴 **내 심각도 등급이 틀렸다** (오너가 5분 만에 밟았다)

- **사고:** 정밀검수가 *"📦 용량 카드는 마운트 때 한 번만 읽어 이사가 끝나도 갱신되지 않는다"* 를 찾았다.
  나는 **low** 로 등급을 매기고 3차로 미뤘다. 오너가 시범 테스트 **A-1 에서 바로** 밟았다 —
  배너는 *"보관했어요"*, 바로 위 카드는 `0`.
- **더 나쁜 것:** 오너의 추측은 *"단위가 작아서 0으로 보이는 것"* 이었는데, 코드를 여니
  `if (b <= 0) return "0"` — **1바이트라도 있으면 KB 로 뜬다.** 진짜 0이었다.
  그리고 🧹 정리를 열면 그때 새로 읽어와 **거기서만 진짜 용량이 보였다** — 그게 더 헷갈린다.
- ⭐ **배운 것:** 나는 심각도를 **"코드가 얼마나 틀렸나"** 로 매겼는데,
  오너는 **"부모가 무엇을 믿게 되나"** 로 만난다. 🔴 **화면이 사실이 아닌 것을 말하는 버그는
  코드 깊이와 무관하게 크다** — 사흘 연속 같은 자리(배너·카드·삭제 결과)에서 나오고 있다.
  ⇒ 앞으로 **"화면이 거짓을 말하는가"** 는 등급을 코드 기준으로 매기지 않는다.
- **되돌리려면:** `git revert 754ec44`

### 결정 ㉒ — 📏 **처음으로 잼: 그림일기 1편 ≈ 0.8MB** (질문 24 부분 해소)

- **결정:** 저장 용량 상한을 **아이당 300MB** 로 바꾸기로 했다(오너 지시). 계정 전체 1GB 는 **유지하되 화면에 안 보인다.**
- **왜 이제야 쟀나:** 어제까지 `diary_assets` 가 **0행**이었다(08-10 탈퇴 시범의 결과).
  오늘 오너가 실제 일기를 만들면서 처음으로 표본이 생겼다.
  **실측**(읽기 전용 집계): AI 그림 원본+썸네일 **758KB** · 음성 메모 **46KB** · **일기 1편 ≈ 0.8MB**
  → 300MB ≈ **375편**(매일 1편이면 12.5개월, 주 2편이면 3.6년)
  ⚠️ **표본 1장이다.** 그림 상한이 4MB라 최악은 75편(2.5개월)이다 — 이 숫자를 확정으로 쓰지 않는다.
- **왜 계정 상한을 남기나:** 아이별만 두면 **프로필을 늘릴수록 총량이 배가**된다(4명 = 1.2GB).
  `quota.py` 가 이미 같은 이유로 *"아이별 < 계정 < 아이별 × MAX_PROFILES"* 를 강제하고 있고,
  **300 < 1024 < 1200** 이라 현재 값이 그 조건에 정확히 맞는다.
  오너의 *"1GB라고 적지 마"* 는 **표시**에 대한 지시로 읽었다 — 안 보여주되 방어는 남긴다.
- ⭐ **배운 것:** *"추정을 실측인 척하지 않는다"*(질문 24)를 지킨 덕에,
  표본이 생긴 날 **한 번의 조회로** 답이 나왔다. 모른다고 적어두는 것도 자산이다.
- **자산:** 이 수치 · 아이별/계정 두 겹 구조의 근거
- **되돌리려면:** 아직 구현 전(작업 #27~30). 되돌릴 것 없음

---

## 2026-08-11 (세션 10 · 16:17–17:04, 0.8h · 3커밋) — 📦 **돈은 토큰으로만 나가지 않는다**

> **한 줄:** 저장 용량을 막은 날이 아니라, **같은 사각지대를 세 번째로 만난 날**이다.
> 그리고 이번에는 사각지대가 **검사 자신의 정의 안에** 있었다 — *"돈 쓰는 코드 = LLM 호출"*.

### 결정 ① — 🔴 **검사가 "돈"을 토큰으로만 정의하고 있었다** (세 번째 같은 사각지대)

**결정.** `_verify_quota.py` 의 `_SPEND` 에 `sb_storage_upload` 를 추가한다.

**왜.** `POST /diary/assets` 에는 한도가 **0건**이었다. 4MB × 250번이면 1GB 인데 그걸 막는 코드가 없었다.
그런데 어제 만든 [K] 커버리지 검사는 **65/0 초록불**이었다 — 재현으로 확인했다.
뿌리는 이 한 줄이다:

```python
_SPEND = ("AsyncAnthropic", "anthropic.", "openai.com", "services.tts import", "synthesize(")
```

**배운 것.** ⭐ **어제 배운 문장(*"검사가 무엇을 못 보는가를 검사에 적어라"*)을 적어놓고도 또 당했다.
왜냐하면 그때 적은 것은 *면제 목록*이었고, 이번에 빈 것은 **탐지 목록**이었기 때문이다.**
같은 검사에 두 종류의 '못 보는 것'이 있다 — 알면서 뺀 것(면제)과 **애초에 범주에 없던 것**.
전자는 목록을 보면 보이고, **후자는 목록을 아무리 봐도 안 보인다.**
🔴 계보: `care_signals`(SQL 파일에 없어서) → 챗봇(quota 를 안 써서) → 업로드(**LLM 이 아니라서**).
세 번 다 *"기계가 세게 했다"* 는 맞았고 *"기계에게 그것이 보이나"* 가 틀렸다.

**자산.** `55dd993` · `_verify_quota.py:_SPEND` · 재현 기록(어제 상태에서 한도 0건인데 65/0 통과)

**되돌리려면.** 없음. `_SPEND` 는 넓힐수록 좋다 — 오탐(면제 목록에 이유와 함께 추가)의 대가가 훨씬 싸다.

---

### 결정 ② — 두 장치가 **서로 다른 것**을 막는다 (하나로는 안 된다)

**결정.** 용량 상한 1GB(507) **와** 업로드 횟수 한도(`diary_upload` 200/일·30/분·계정 240)를 **둘 다** 건다.

**왜.** 하나만 걸면 반대쪽이 통째로 열린다:

| | 막는 것 | 이것만 있으면 |
|---|---|---|
| 용량 1GB | **누적** | 🔴 `upload_asset` 은 **upsert** 다 — 같은 `client_asset_id` 로 덮어쓰면 **용량이 안 늘면서 쓰기만 무한** |
| 횟수 한도 | **폭주** | 매일 초기화되므로 **매일 조금씩 쌓이는 것**을 못 본다 |

계정 총량 240 은 임의값이 아니라 **역산**이다 — `1GB ÷ 4.2MB ≈ 250` 보다 아래라
**하루 만에 1GB 를 채우는 것이 구조적으로 불가능**해진다. 검사가 이 관계를 못박는다.

**배운 것.** ⭐ **"이미 한도가 있다"는 *무엇에 대한* 한도인지까지 말해야 한다.**
같은 자원에 두 개의 축(속도·총량)이 있으면 장치도 둘이어야 한다.
부수: 원가는 정직하게 **2원**으로 잡았다(4.2MB 1년 보관 1.4원 + egress). LLM 과 규모가 다르다 —
🔴 **1GB 를 꽉 채워도 보관료는 월 29원이다.** 그래서 진짜 방어는 횟수가 아니라 용량이다.

**자산.** `55dd993` · `quota.LIMITS["diary_upload"]` · 계정 최악 2,600 → **3,080원/일**

**되돌리려면.** `diary.ACCOUNT_STORAGE_LIMIT_BYTES` 를 키우거나, `quota` 에서 `diary_upload` 제거
(⚠️ 라우터의 `check_and_consume` 도 함께 — 의도된 두 겹).

---

### 결정 ③ — 🔴 **80% 경고가 이 기능의 본체다** (100% 는 이미 늦다)

**결정.** 부모 화면에 저장 용량 카드. 80% 에서 알리고, 그 자리에서 *"지우기 전에 앨범으로 남길 수 있다"* 를 함께 말한다.

**왜.** 오너가 정리 화면을 고르면서 한 문장을 덧붙였다 —
*"피지컬앨범 또는 디지털앨범으로 추출해놓으면 부모도 버리지 않고 보관 가능하니까"*.
🔴 **그 한 문장이 이 기능의 순서를 바꿨다.** 내려받기가 없으면 *"정리하세요"* 는
**"아이 그림을 버리세요"** 다. 화면은 있는데 아무도 안 누르는 상태가 된다.

**배운 것.** ⭐ **삭제 기능을 만들 때는 "지우는 법"이 아니라 "지울 마음이 드는가"를 먼저 봐야 한다.**
버튼이 있는 것과 눌리는 것은 다르다. 그리고 **버리기 전에 남기는 길**이 그 차이를 만든다.
🔴 그리고 이번에도 **오너가 짚었다** — 나는 "정리 화면을 만들자"까지만 보고 있었다.

**자산.** `495075b` · `DiaryStorageCard.jsx` · DOM 검사 19항목(79%↔81% 경계 포함) ·
서랍 #16(앨범은 상품이 아니라 **삭제의 전제조건**이라는 관점 — GD-39 우선순위 변경)

**되돌리려면.** `ParentDashboard.jsx` 의 `<DiaryStorageCard ... />` 한 줄 주석 처리.

---

### 결정 ④ — 🔴 **아이 화면이 침묵하고 있었다** (설계상 그렇게 만들어져 있었다)

**결정.** `diaryAssets` 의 throw-금지 계약은 그대로 두고, **507 만 이름 붙여** 밖으로 알린다.

**왜.** 그 파일은 *"절대 throw 하지 않는다"* 가 설계다(서버가 실패해도 아이 일기는 로컬에 있으니까).
그래서 업로드 실패는 전부 `catch { return false }` 로 사라지고, 호출부도 반환값을 안 본다.
⇒ **완전한 침묵.** 아이는 아무 일 없던 것처럼 보고, 부모는 영영 모른다.
어제 TTS 에서 배운 문장이 그대로 걸린 자리다 — *"조용히 막히면 조이면 안 되고, 보이게 막히면 조여도 된다."*

⚠️ 통신 실패·413·429 는 **안 알린다.** 재시도로 조용히 나아지는 종류라 전부 알리면 없는 문제를 만든다.

**배운 것.** ⭐ **"실패를 삼키는 설계"에는 반드시 *어떤 실패는 삼키면 안 되는가*를 함께 적어야 한다.**
삼키는 이유(화면을 안 깨려고)는 옳았고, **예외를 안 둔 것**이 틀렸다.
🔴 그리고 그 판단은 **에러 종류마다 다르다** — "재시도로 나아지나"가 가르는 선이다.

**자산.** `495075b` · `onStorageFull` 구독 · `FamilyShelf` 안내(📚 + 키디의 말) ·
DOM 검사 15항목(**507 → 알림 → 아이 화면**을 실물로 잇는다)

**되돌리려면.** `diaryAssets` 의 `noteStorageFull(e)` 두 줄 제거 — 원래의 완전한 침묵으로 돌아간다.

---

### 결정 ⑤ — 🚨 정리 목록은 ~~**비공개 일기를 담되**~~ 내용은 담지 않는다

> 🔴 **정정 2026-08-12 (결정 ⑱ 참조) — 아래 "왜"의 전제가 거짓이다.**
> **비공개 그림일기는 존재하지 않는다.** `share_with_parent` 를 false 로 만드는 코드가 **0건**이고
> 설정 UI 도 없다(`sql/007:159`, 오너 확정 2026-08-08). 비밀은 **체크인**에만 있다.
> **결론(내용을 안 싣는다)은 그대로 옳지만 이유가 다르다** — '비공개 보호'가 아니라
> *"고르는 데 필요 없는 것은 안 보낸다"* 이고, 그래서 **나중에 아이가 고를 수 있게 돼도 이 자리를 안 고쳐도 된다.**
> ⚠️ 이 문단을 지우지 않고 남긴다. 이 거짓이 코드 주석·WORKLOG·이 문서까지 **네 곳**에 퍼져
> 하루 뒤 오너에게 틀린 답을 하게 만들었다 — **그 경로 자체가 기록할 값어치가 있다.**

**결정.** `GET /diary/usage/entries` 는 **날짜 · 용량 · 공유여부 셋만** 보낸다. select 단계에서 못박는다.

**왜.** ~~용량을 차지하는 건 공유분만이 아니라 **비공개 일기까지 전부**다.~~ 부모가 정리하려면 목록에 있어야 한다.
그런데 이건 **부모 화면**이 부르는 API 다 — 내용이 한 글자라도 섞이면 *"아이의 비밀 친구"* 가 무너진다.
🔴 그리고 그 사고는 **에러도 안 나고 화면도 멀쩡하다.**
화면도 *"왜 내용이 안 보이는지"* 를 부모에게 설명한다 — 고장으로 오해하지 않게.

**배운 것.** ⭐ **윤리선은 "보여주냐 마냐"의 이분법이 아니다. *어느 축을 보여주느냐*의 문제다.**
"목록에 없애기"와 "내용을 없애기"는 다른 답이고, 여기서는 후자가 옳았다.

**자산.** `495075b` · 🐤 카나리아 신설 · 차단시험 4종(컬럼 추가 / `**r` 펼치기 / user_id 필터 제거 ×2) 전부 잡힘

**되돌리려면.** 없음. select 를 넓히면 카나리아가 막는다.

---

### 결정 ⑥ — 🔴 **막을 때마다 횟수가 사라지고 있었다** (오너의 *"다시 검증해봐"* 에서)

**결정.** `quota.refund()` 신설. 507·413 처럼 **일을 안 하고 막는** 자리에서는 소비를 되돌린다.

**왜.** `check_and_consume` 은 일을 **하기 전에** 소비한다 — 그게 옳다(나중에 세면 실패한 요청이 공짜가 된다).
그런데 오늘 내가 그 **뒤에** 관문을 하나 더 만들었다. 그러면 이렇게 된다:

> 가득 찬 계정에서 아이가 책장을 열 때마다 hydrate 가 재푸시한다(최대 3편 × 자산 4개 = **12회**).
> 전부 507 인데 **횟수는 12회씩 사라진다.** 스무 번이면 하루 한도(240)가 없어지고,
> ⇒ **그날 부모가 정리해서 공간을 비워도 아무것도 못 올린다.**

**배운 것.** ⭐ **"소비를 먼저 한다"는 옳은 규칙인데, *일을 안 하고 막는 관문*을 뒤에 추가하면 조용히 뒤집힌다.**
새 관문을 넣을 때는 **그 앞에 이미 무엇이 소비됐는지**를 반드시 볼 것.
🔴 그리고 이 실수는 **조용히** 일어난다 — 아이는 그냥 계속 안 되는 것으로 본다.
⚠️ 환불을 아무 데나 쓰면 반대로 무너진다 — **LLM 을 부른 뒤의 실패는 돈이 이미 나갔으므로 환불 금지.**
그래서 대조군을 양쪽으로 뒀다(성공은 반드시 소비 / 계정 총량도 소비).

**자산.** `fbcb9e5` · `quota.refund()` · `[L]` 8항목 · 차단시험 5건(507·413·하루·계정·**반대-과잉환불**)

**되돌리려면.** `diary.py` 의 `refund(...)` 두 줄 제거 — 그러면 막힐 때마다 한도가 닳는 상태로 돌아간다.

---

### 🧪 오후 — **오너 시범 테스트가 내 초록불을 세 번 더 뚫었다** (결정 ⑫~⑯)

> 오전에 정밀검수로 4건을 찾아 고치고 배포했다. 오후에 오너가 **손으로 한 번 해보자** 그 뒤에서
> **3건이 더 나왔다.** 전부 검사가 초록불인 상태였고, 하나는 **아침에 "고쳤다"고 보고한 자리**였다.
> 🔴 오너 시범이 내 초록불을 뚫은 것이 **8/9 · 8/10 · 8/11 — 사흘 연속**이다.

---

### 결정 ⑫ — 🔴 **"고쳤다"의 검증은 그 코드가 *일을 하는* 것까지다**

**결정.** `delete_profile` 이 **삭제 전에** 자산 경로를 걷어온다(①수집 → ②삭제 → ③명세서+`_sweep_one`).

**왜.** 아침에 *"프로필 삭제가 파일을 안 지운다"* 를 고치고 검사·차단시험까지 통과했다.
그런데 오너가 실제로 해보니 **파일 3개가 그대로 남았다**(그림 원본 593KB · 썸네일 · **아이 목소리**).
영수증은 `done · paths=0개` 로 닫혀 있었다 — **명세서가 빈 채로 돌았다.**

009 트리거는 `diary_entries` 삭제 시 `diary_assets` 를 **조인해서** 경로를 찾는다.
· 아이가 일기를 찢을 때 → 자산이 살아 있다 → 잘 돈다
· 🔴 `profiles` 를 지울 때 → 두 표가 **동시에 cascade** → 조인 대상이 이미 없다 → **경로 0개**

⚠️ **답은 어제 내가 직접 쓴 주석에 있었다**(`account.py:85`):
*"diary_assets 는 CASCADE 로 사라진다. **지우기 전에** 걷어와야 한다"*
탈퇴는 그래서 무사했고, 프로필 삭제에 그 한 단계가 없었다. **오늘 그 주석을 안 읽었다.**

**배운 것.** ⭐ **내 검사는 "부르는가"를 봤지 "부른 것이 일을 하는가"를 안 봤다.**
`_sweep_one` 호출이 있으면 통과였다. **빈 명세서로 도는 경우**를 아무도 묻지 않았다.
🔴 그리고 **같은 성질의 구멍은 한 자리만 메우면 반드시 다른 자리에 남는다** — 어제 탈퇴에서 배운 것을
오늘 프로필 삭제에 옮기면서, **그 옆의 전제(경로 수집)는 안 옮겼다.**

**자산.** `26ac406` · `[G-2] profiles 를 지우기 전에 diary_assets 를 조회하는가` · 차단시험 2/2 ·
**오너 실측으로 통과 확인**(파일 3개 → 0개)

**되돌리려면.** `profiles.py` 의 ①경로 수집 블록 제거 — ⚠️ 그러면 파일이 다시 영원히 남는다(법 제21조).

---

### 결정 ⑬ — 🔴 **화면이 부모에게 "보관했어요"라고 거짓말했다**

**결정.** 이사 엔진이 **회차 전체 요약을 마지막에 한 번** 보고하고, 화면은 회차 시작 시 옛 값을 지운다.

**왜.** 아이를 지우고 새로 만든 뒤에도 부모 화면에 *"일기 1편을 안전하게 보관했어요"* 가 떠 있었다.
같은 화면의 저장 공간 카드는 `0 / 1.00GB` 였다 — **두 값이 서로 모순인데 아무도 안 잡았다.**
원인 셋: ①올릴 게 없으면 `onProgress` 를 아예 안 부름 ②여러 프로필 이벤트가 뒤섞임 ③회차 시작 시 초기화 없음

**배운 것.** ⭐ **"완료했다"고 말하는 화면은 그 말의 근거를 매번 새로 만들어야 한다.**
옛 성공을 그대로 두면 그건 **정보가 아니라 잔상**이다.
🔴 이 거짓말의 대가가 크다 — 부모가 안심하고 기기를 바꾸면 **아이 일기가 통째로 사라진다.**
⚠️ 중단(abort)에도 요약을 안 보낸다. **하다 만 값을 "완료"라고 말하는 것도 같은 거짓말**이다.

**자산.** `af43020` · DOM 검사 11항목 · 차단시험 3/3 · 오너 실측(배너 안 뜸 → 올리니 뜸)

**되돌리려면.** `migrateAllProfiles` 끝의 요약 블록 + `setMig(null)` 제거.

---

### 결정 ⑭ — 🔴 **시계가 2초 느려서 로그인이 통째로 죽었다**

**결정.** `jwt.decode(..., leeway=30)` + **401 의 원인을 로그로 남긴다.**

**왜.** 오너 탈퇴가 401 로 막혔다. 화면엔 *"유효하지 않은 인증 토큰입니다"* 뿐이고 **로그에도 이유가 없었다.**
임시 로그를 넣고서야 보였다 — `ImmatureSignatureError: token is not yet valid (iat)`.
Supabase 가 **방금 발급한** 토큰의 `iat` 이 서버 시계보다 미래였다. 실측: 그 기기가 **2초** 느렸다.
탈퇴만이 아니라 `/me/status` 도 401 — **세션 전체가 죽어 있었다.**

**배운 것.** ⭐ **"우리 코드"가 아닌 것이 우리 기능을 죽일 수 있다.** 시계·인증서·DNS 같은 것들이다.
그런 실패는 증상이 *"안 돼요"* 한 줄이라 **원인을 찾는 비용이 코드 버그의 몇 배**다.
⇒ 그래서 **표준 여유(leeway)** 를 두고, **원인을 반드시 로그로 남긴다.**
🔴 그 자리 주석은 *"원인은 서버 로그로만"* 이라 적혀 있었는데 **로그 코드가 없었다** —
지뢰 #8 이 경고한 자리 그대로다. **주석이 약속한 것을 코드가 안 하면 없는 보호를 있다고 믿게 된다.**

**자산.** `896af13` · `_verify_authz.py` 에 leeway(5~120초) + 원인 로깅 검사 · 차단시험 3/3

**되돌리려면.** `leeway=30` 제거 — ⚠️ 그러면 시계가 1초만 밀려도 전 사용자가 로그인 못 한다.

---

### 결정 ⑮ — 🔴 **합계를 맥락 없이 보여준 것이 거짓이 됐다**

**결정.** `check_leftovers.py` 가 **"이건 전체 DB 합계다"** 를 명시하고 **계정별 표**를 함께 낸다.

**왜.** 내가 오너에게 *"탈퇴하면 아이 프로필 15명이 사라진다"* 고 말했다.
오너: *"한 계정에는 프로필 1개뿐이잖아. 무슨말을 하는건지 모르겠어."*
실측하니 **오너가 맞았다** — 15명은 **계정 11개에 흩어진 합계**였고, 오너 계정엔 **1명**뿐이었다.
도구가 service key 로 `profiles` 를 **user_id 필터 없이** 조회했고(RLS 우회), 나는 그걸 *"이 계정 것"* 으로 읽었다.

**배운 것.** ⭐ **숫자가 맞아도 읽는 사람이 틀리게 읽으면 그건 도구의 잘못이다.**
*"무엇의 합계인가"* 를 숫자 **옆에** 써야 한다. 합계는 맥락 없이 놓이면 거짓이 된다.
🔴 그리고 이걸 잡은 것도 **오너의 확인 질문**이다(트리거 ②). 오늘만 네 번째다.

**자산.** `00ac55b` · `3808334`(DB만 보던 것을 **Storage 실물**까지) · `edb80ac`(*"볼 게 없었다"* 를 *"깨끗하다"* 로 말하지 않기)

**되돌리려면.** 해당 없음.

---

### 결정 ⑯ — 🔬 **테스트 가이드도 "실제로 열어보고" 써야 한다**

**결정.** *"AI 코치를 세 번 눌러 429 를 본다"* 를 가이드에서 **뺐다.** 자동 검사 12항목이 대신 지킨다.

**왜.** 오너가 *"사전작업이 많은데?"* 라고 짚어서 코드를 다시 열었더니, 사전작업 이전에 **재현이 불가능**했다:
1번째 생성 → **2·3번째는 캐시 히트**(`reports.py:281`)라 한도를 안 태운다. 429 에 닿으려면 **클릭 사이마다 시청 기록을 바꿔야** 한다.
게다가 시청 기록이 0이면 `totalWatched == 0` 에서 **한도 앞에 먼저 return** 한다.

**배운 것.** ⭐ **나는 한도표만 보고 "2회니까 3번 누르면 걸리겠지"라고 썼다 — 그 사이의 캐시를 안 봤다.**
하네스의 *"저장된 값에서 화면 동선을 추론하지 않는다"* 가 **가이드를 쓸 때도** 그대로 적용된다.
🔴 부수: D·E 의 전제(**'가족과 함께 보기' 동의**)도 빠져 있었다 — **내가 직접 만든 게이트인데** 안 봤다.
가이드는 *"무엇을 누르나"* 만이 아니라 **"그게 동작하려면 무엇이 켜져 있어야 하나"** 까지다.

**자산.** `8363064`·`d78efea`·`0a6168e` · `문서/시범테스트_가이드_2026-08-11.md` ·
`c903ae4` **`check_leftovers.py`**(SQL Editor 없이 잔존물 확인 — 앞으로 계속 쓴다)

**되돌리려면.** 해당 없음.

---

### 결정 ⑨ — 🔬 **정밀검수를 실제로 돌렸더니, 어제 넣은 한도가 한 번도 발동한 적이 없었다**

**결정.** 어제(08-10) 배포분에 정밀검수를 적용하고, 확정된 것부터 고쳤다.

**왜 이런 게 나왔나.** 어제 *"체크인 반응에 한도를 걸었다"* 고 기록했다. 그런데 —

| 앱이 부르는 것 | 어제 상태 |
|---|---|
| `/checkins/react/stream` (DailyCheckin.jsx:399) | 한도 **0건** |
| `/checkins/greet` (DailyCheckin.jsx:166) | 한도 **0건** |
| ~~`/checkins/react`~~ (한도 걸린 곳) | **호출부 0건 — 아무도 안 부른다** |

⇒ Haiku 2경로가 어제부터 무제한이었고, **기록에는 막았다고 적혀 있었다.**

**배운 것.** ⭐ **"방어를 건 자리"와 "앱이 실제로 부르는 자리"가 같은지 물어야 한다.**
서버만 보면 한도가 있고, 클라이언트만 보면 호출이 있다. **둘을 이어봐야** 어긋난 게 보인다.
🔴 그리고 검사도 이걸 못 봤다 — `[K]` 가 **파일 단위**라 파일에 `check_and_consume` 이
하나만 있으면 초록불이었다. **같은 파일 안의 무한도 엔드포인트는 구조적으로 안 보였다.**

**자산.** `ba8e81e` · `[K-2] 엔드포인트 단위 커버리지`(@router 로 함수 본문을 잘라 확인) ·
`checkin_greet` 신설 · 어제 상태 재현(옛 검사 ✅통과 / 새 검사 ❌2건 발견)

**되돌리려면.** `_verify_quota.py` 의 `[K-2]` 블록 제거 → 파일 단위 판정으로 돌아간다.

---

### 결정 ⑩ — 🔴 **부모가 AI 코치를 세 번 누르면 대시보드가 백지가 됐다**

**결정.** `readErrorText(err, fallback)` 신설 — **반환은 항상 문자열**. 화면에 그리는 3곳에 적용.

**왜.** FastAPI 의 `detail` 은 **문자열일 때도 객체일 때도** 있다(`quota.py` 429 · `diary.py` 507).
`setCoachError(err.response?.data?.detail || …)` 로 받아 JSX 에 넣으면 객체일 때 React 가 throw 하고,
이 레포에는 **ErrorBoundary 가 0건**이라 루트까지 올라가 **화면 전체가 사라진다.**
같은 축의 KiddyRoom·ChatWidget 은 `.detail?.message` 로 한 단계 더 들어갔다 — **여기만 빠졌다.**

**배운 것.** ⭐ **한 축을 여러 화면에 붙일 때는 "다 같은 모양으로 받는가"를 마지막에 한 번 훑어야 한다.**
셋 중 둘이 옳으면 나머지 하나는 **더 안 보인다** — 옳은 예시가 옆에 있어서 다 됐다고 느낀다.
🔴 부수: 그 실패가 **에러 메시지 없이 흰 화면**이라 사용자는 원인을 영영 모른다.

**자산.** `ba8e81e` · `readErrorText` · DOM 검사 12항목(소스 전수 + [A] "React 가 정말 던지는가" 실물 확인)

**되돌리려면.** 없음. 다만 ErrorBoundary 를 도입하면 검사 [D] 의 전제 한 줄을 고칠 것(위 [C] 는 유지).

---

### 결정 ⑪ — 🔴 **아이 프로필을 지워도 그림·목소리가 서버에 남고 있었다**

**결정.** `delete_profile` 이 삭제 뒤 그 프로필의 pending 명세서를 읽어 `_sweep_one` 으로 정리한다.

**왜.** 주석은 *"종속 데이터는 DB cascade 가 자동 정리"* 였고 **절반만 맞았다.**
행은 cascade 로 사라지고 트리거가 명세서를 남기지만, **그 명세서를 도는 사람이 없었다**(크론 없음).
🔴 이건 2026-08-10 결정 ③에서 **이미 배운 것**이다 — *"도는 사람이 없으면 큐가 아니라 쓰레기통이다."*
그때 탈퇴는 고쳤는데 **프로필 삭제는 같이 안 고쳤다.**

**배운 것.** ⭐ **같은 성질의 구멍은 한 자리만 메우면 반드시 다른 자리에 남는다.**
고칠 때 *"이 구멍이 있는 다른 자리는?"* 을 물어야 하고, 그 답은 **기억이 아니라 전수 검사**여야 한다.
그래서 `[G]` 는 "탈퇴가 쓸고 가는가"가 아니라 **"지우는 함수마다 쓸고 가는가"** 로 만들었다.

**자산.** `338f4a3` · `[G] 명세서를 만드는 경로마다 도는 사람이 있는가`(3개 함수 전수) · 차단시험 2/2

**되돌리려면.** `profiles.py` 의 Storage 정리 블록 제거 — ⚠️ 그러면 파일이 다시 영원히 남는다(법 제21조).

---

### 결정 ⑦ — 🔬 **「정밀검수」를 이름 붙이고 절차로 만들었다** (오너 지시)

**결정.** `.claude/rules/정밀검수.md`(4단계 + 각도 목록) + `/정밀검수` 슬래시 커맨드 + 하네스 트리거 한 줄.

**왜.** 오너의 *"다시 검증해봐"* 는 지금까지 **효과는 확실한데 절차가 없었다.** 매번 즉흥으로 각도를 찾았고,
그러면 **찾기 쉬운 각도만 반복하게 된다**(하네스가 이미 경고한 것 — *"검증 항목을 즉흥으로 정하면 축이 통째로 빠진다"*).
오늘 이 각도(**사용자 동선**)로 3건이 나왔으니, 그 4문항을 목록으로 남기면 다음 사람이 처음부터 시작하지 않는다.

🔴 **`.claude/rules/` 에만 두면 또 안 읽힌다** — 하네스가 2026-08-08 에 실측으로 못박은 사실이다(자동 로드 안 됨).
그래서 **슬래시 커맨드로 장치화**하고, 오너가 말로 불러도 걸리게 하네스에 트리거를 함께 뒀다.

**배운 것.** ⭐ **절차가 없는 좋은 습관은 사람에 의존한다.** 오너가 매번 말해줘야 도는 것은 장치가 아니다.
그리고 이 문서의 진짜 자산은 4단계가 아니라 **각도 목록**이다 — *"정밀검수에서 뭔가 나오면 그 각도를 추가한다"* 를
문서 안에 규칙으로 박아뒀다. **목록이 자산이고, 빈 칸이 다음 사고다.**

**자산.** `2e7df96` · `.claude/rules/정밀검수.md` · `.claude/commands/정밀검수.md` · CLAUDE.md 트리거

**되돌리려면.** 커맨드 파일 삭제 + 하네스 트리거 3줄 제거(⚠️ 그러면 훅의 고아 검사가 규칙 파일을 지적한다 — 의도된 것).

---

### 결정 ⑧ — 🧭 **하네스를 문서가 아니라 코드처럼 검사한다**

**결정.** pre-commit 에 「하네스 무결성」 3종. **문서만 고쳐도 이건 돈다**(원래 훅은 문서면 즉시 통과했다).

**왜.** 같은 세션에서 두 가지가 실제로 일어났다:
- 규칙을 추가하다 **200줄 한도를 어겼다**(204줄). 내가 손으로 세어서 알았다 — **다음엔 안 셀 수 있다.**
- 장치 표에 `_verify_all.py **8종**` 이라 적혀 있었는데 **실제는 11종**이었다.

하네스는 **매 세션 통째로 로드되는 유일한 보호막**이다. 여기 거짓이 적히면 *"없는 보호를 있다고 믿게 된다"* —
그 문장이 **하네스 본문에 이미 있는데도** 하네스 자신이 그 상태였다.

**배운 것.** ⭐ **"이 문서가 틀렸을 때 누가 아는가"를 문서마다 물어야 한다.**
코드는 테스트가 알려주고 DB 는 검사가 알려주는데, **하네스는 아무도 안 알려줬다** — 제일 중요한 문서인데.
🔴 그리고 검사 방향은 **양쪽**이어야 한다: 하네스가 없는 파일을 가리키는 것(거짓) + 표에 없는 규칙 파일(고아).
후자가 없으면 *"파일은 있는데 아무도 안 여는"* 상태가 조용히 생긴다.

**자산.** `2e7df96` · `scripts/hooks/pre-commit` 「하네스 무결성」 · 차단시험 6/6

**되돌리려면.** 훅의 해당 블록 제거. ⚠️ 그러면 200줄 한도가 다시 **아무도 안 세는 규칙**이 된다.

---

### 부수적으로 배운 것

- 🔴 **"검사가 자기 문서를 읽는다"를 오늘만 세 번 만났다.** 전부 차단 시험이 잡았다.
  | # | 무엇을 찾았나 | 무엇이 걸렸나 |
  |---|---|---|
  | 1 | `ErrorBoundary` 라는 **단어** | 바로 그 사고를 설명하려고 **내가 쓴 주석** |
  | 2 | `[K-2]` 가 `@router` 사이 전부 | 엔드포인트가 아닌 **모듈 레벨 헬퍼** → `GET /insights` 오탐 |
  | 3 | `_sweep_one` 이라는 **단어** | **독스트링**. 실제 호출을 지워도 차단시험이 통과했다 |
  ⭐ **문서를 읽는 검사는 문서를 먼저 벗긴다** — 주석·독스트링 제거 후, **호출형**(`f(`)으로 센다.
  ⚠️ 그리고 이건 *"못 잡았다"* 가 아니라 **"잘못 잡았다"** 다 — 후자가 더 위험하다(초록불이 뜬다).
- 🔴 **정밀검수를 워크플로로 돌린 비용이 예상을 크게 넘었다** — 에이전트 13개(예고대로)인데 **148만 토큰**.
  2026-08-02 사고(42개·182만)에 근접했다. **에이전트 수를 맞춰도 개당 토큰을 못 맞추면 소용없다** —
  서브에이전트는 컨텍스트를 물려받지 못해 각자 레포를 처음부터 읽고, 일부는 파이썬을 직접 실행해 재현까지 했다.
  ⇒ 앞으로 규모는 **에이전트 수가 아니라 토큰**으로 말한다.
- ⚠️ **정밀검수 결과 중 22건은 아직 검증하지 않았다**(내가 확인 상한을 6으로 걸어 잘랐다).
  그중 high 6건이 남아 있다 — 질문 29 에 목록을 남겼다. **"정밀검수했다"가 "다 봤다"는 아니다.**
- 🔴 **하네스 무결성 검사도 첫 구현이 헛돌았다** — `.claude/rules/...` **전체 경로만** 봤는데
  규칙 표는 `backend.md` 처럼 **파일명만** 쓴다. 차단시험 ②가 **통과해버려서** 발견했다.
  ⭐ **문서를 파싱하는 검사는 그 문서가 실제로 쓰는 표기법을 먼저 열어볼 것.** 내가 쓸 표기를 가정하면 빗나간다.
  (오늘 이런 종류가 **일곱 번째**다 — 전부 차단 시험·대조군이 잡았다)
- 🔴 **재검증에서 프론트 2건도 나왔다** — 둘 다 *"코드는 도는데 사용자가 손해를 본다"* 종류다.
  - `<a href="/family-shelf">` 가 **전체 페이지 리로드**를 일으켰다(SPA 붕괴 — 번들 재파싱·세션 복원·대시보드 상태 소실).
    ⚠️ 같은 레포의 `<a href="/privacy">` 를 보고 관례라 여겼는데, 그건 **앱 밖 성격의 문서 링크**라 성질이 다르다.
    ⭐ *"이미 있는 패턴을 따랐다"* 는 근거가 되려면 **성질이 같아야** 한다(2026-08-07 규칙 ②의 프론트판).
  - 정리한 뒤 **아래 책장이 옛 목록에 굳어 있었다** — *"지웠는데 화면엔 있다"* 는 삭제 기능에서 최악의 불일치다.
- 🔴 **`FakeDB` 가 `sb_upsert` 를 아예 안 덮고 있었다.** 그 한 줄만 진짜 DB 로 나가 500 이 났고,
  검사는 *"막혔다"* 를 **틀린 이유로** 읽었다. **대조군(통과해야 하는 경우)이 없었으면**
  차단 검사만 초록불인 채 조용히 넘어갔을 자리다.
  ⭐ *"목록류는 '있는 항목 검토'가 아니라 '없는 칸 찾기'"*(2026-08-07 규칙 ③)를 **도구에도** 적용해야 했다.
  → `db.py` 의 `sb_*` 전수와 대조하는 자체 점검을 넣었다.
- 🔴 **내가 쓴 카나리아가 아무것도 안 막고 있었다.** *"select 를 `*` 로 바꾸면 샌다"* 고 적었는데
  차단시험이 **통과**했다 — 응답을 키 5개로 **직접 조립**하니 애초에 샐 수 없는 경로였다.
  ⭐ **검사에 적은 '이 검사가 막는 것'도 틀릴 수 있다.** 차단 시험은 방어만이 아니라
  **검사 설명문**도 검증한다. 주석을 사실대로 고쳤다(실제로 잡는 것은 `**r` 로 행을 펼치는 순간).
- 🔴 **가짜가 필터를 무시해 대조군이 헛돌았다 — 어제와 정확히 같은 실수를 같은 종류의 파일에서 반복했다.**
  차단시험 ④(`user_id` 필터 제거)가 **통과해버려서** 발견했다.
  ⇒ **가짜를 새로 쓸 때마다 "이 가짜가 필터를 적용하나"를 먼저 물을 것.**
- 🔴 **내 검사 헬퍼가 측정 대상을 스스로 지우고 있었다** — `upload()` 가 매 호출마다 `_reset_all()` 을 해서
  *"성공 3번은 소비된다"* 대조군이 **3회를 1회로** 읽었다. → 초기화하지 않는 `upload_raw()` 를 따로 뒀다.
- 🔴 **계정 총량 환불 누락이 차단시험을 통과했다** — `peek` 이 `min(아이별, 계정)` 이라
  아이별(200)이 계정(240)을 **가린다.** ⇒ 계정 키를 **직접** 읽는 검사를 따로 뒀다.
  ⭐ **집계 함수로 검사하면 그 함수가 감추는 것까지 함께 감춰진다.**
- 🔴 **차단시험 주입이 세 번 연속 엉뚱한 함수에 들어갔다** — `sb_upsert(` 가 파일에 3곳 있는데
  `perl -0p s///`(첫 일치)와 줄번호가 각각 다른 곳을 짚었다. 그래서 *"못 잡음"* 이 **세 번 다 거짓**이었다.
  ⭐ **주입 지점은 함수명·줄번호가 아니라 *그 자리에만 있는 문자열*(여기서는 표 이름 `"diary_assets"`)로 잡을 것.**
  ⚠️ 이건 차단시험 자체의 신뢰도 문제다 — *"못 잡았다"* 를 만나면 **방어보다 주입을 먼저 의심**해야 한다.
- **문서가 실물과 어긋나 있었다** — `git log origin/master..master` 가 0건인데 WORKLOG 두 곳은
  *"아직 푸시 안 함"*, 표 하나는 끝난 원가 실측을 *"남음"* 으로 두고 있었다(같은 문서 88줄은 ✅).
  ⚠️ *"하네스에 거짓이 적혀 있는 것이 규칙이 없는 것보다 나쁘다"* 의 다른 얼굴 —
  **끝난 일이 남아 있다고 적힌 것도 나쁘다.** 다음 세션이 그걸 또 한다.
- **"막다른 길"이라던 전제가 절반만 맞았다** — 미해결 질문 23 은 *"부모가 지울 방법이 없다"* 고 적혀 있었으나,
  삭제는 **원래부터 부모용**이었다(`FamilyShelf.jsx:664` *"부모 전용으로 일원화"*).
  진짜 문제는 **가는 길**이었다 — `/family-shelf` 링크가 전부 아이 화면에만 있고 부모 화면엔 **0개**.
  ⇒ 문제를 옮겨 적을 때 **원문의 근거를 다시 열 것.** "없다"와 "안 보인다"는 다른 문제고 답도 다르다.
- ⚠️ **못 잰 것: 1GB 가 몇 개월인가.** `diary_assets` 가 지금 **0행**이라(어제 탈퇴 시범의 결과) 실측할 수 없다.
  WORKLOG 의 *"4~8개월"* 은 **최대 크기(4.2MB) 가정**이고, 실제 AI 그림이 1.5MB 면 **2년**이다.
  추정을 실측인 척하지 않는다 — 자산이 쌓이면 다시 잰다.

---

## 2026-08-10 (세션 9 · 2커밋) — 🗑 회원 탈퇴 + 🔴 **내 검사가 딛고 선 땅이 꺼졌다**

> **한 줄:** 탈퇴 기능을 만든 날이 아니라, **검사 36개가 전부 초록불인 채로 아이의 위기 신호가 새고 있었다는 걸 안 날**이다.
> 어제 *"방어가 딛고 선 판정도 실물로 시험하라"* 고 써놓고 오늘 그대로 어겼다.

### 결정 ① — 탈퇴는 **순서가 곧 안전**이다

**결정.** ①경로 수집 → ②`report_coach` 삭제 → ③`auth.users` 삭제 → ④명세서 insert → ⑤파일 삭제.
🔴 **④를 ③보다 먼저 두지 않는다.**

**왜.** 명세서를 먼저 넣으면 ③이 실패했을 때 sweeper 가 **살아있는 계정의 그림·음성을 지운다.**
③ 뒤로 미루면 최악이 '고아 파일 잔존'이고, 그건 GD-8b 가 이미 고른 쪽이다 — **유실보다 잔존.**
②만 ③보다 앞이다. `report_coach` 는 캐시라 ③이 실패해도 손해가 작지만, ③ 뒤로 미루면 실패 시 아이 시청 해석이 **영구히** 남는다.

**배운 것.** ⭐ **되돌릴 수 없는 작업에서는 "각 단계가 실패했을 때 무엇이 남는가"를 표로 그린 뒤 순서를 정한다.**
기능이 도는 순서와 안전한 순서는 다르다.

**자산.** `9cb1397` · `routers/account.py` · `_verify_account_delete.py` 44항목 · 차단시험 12/12

**되돌리려면.** `main.py` 의 `account` 라우터 등록 2줄을 주석 처리. 화면은 버튼이 실패 안내만 띄운다.

---

### 결정 ② — 🔴 **표를 기계가 세게 만들었는데, 기계에게 읽힌 원본이 실물이 아니었다**

**결정.** `server/sql/_db_tables.txt` — 실제 DB 의 표 전수 스냅샷을 레포에 두고, 검사가 SQL 파일과 **양방향**으로 대조한다.

**왜.** `care_signals` 는 `server/sql/` 에 정의가 없다(대시보드에서 만들어짐). SQL 파일만 훑던 [C] 검사는
**그 표의 존재조차 몰랐고** *"설명되지 않는 표 0개"* 라고 초록불을 켰다.
실물에서는 어떤 FK 도 없어서 **부모가 아이 프로필을 지워도, 회원이 탈퇴해도 위기 신호가 남았다.**
2026-07-02 에 만들어진 신호가 그 아이 프로필이 사라진 뒤에도 한 달 넘게 있었다(계정은 살아있음).

**배운 것.** ⭐ **"손으로 세지 말고 기계가 읽게 하라"는 절반이다. 나머지 절반은 "기계에게 읽힌 것이 실물의 전부인가"다.**
어제 배운 문장(*"방어가 딛고 선 판정도 실물로 시험하라"*)의 다음 층이 여기 있었다 — 방어가 딛고 선 **데이터**도 의심해야 한다.
🔴 그리고 이걸 찾은 건 검사가 아니라 **오너의 시범 테스트**다. 어제도 그랬고 오늘도 그랬다.

**자산.** `4b4b098` · `010_care_signals_cascade.sql`(FK 2개 + 고아 정리 + 표 정의 문서화) · `_db_tables.txt` ·
차단시험 ⑩ *"DB 에만 표가 생기고 SQL 파일엔 없다"* ← 오늘 사고를 그대로 재현해 잡는다

**되돌리려면.** `010` 파일 하단의 `drop constraint` 2줄. ⚠️ 고아 삭제는 되돌릴 수 없다.

---

### 결정 ③ — **"한 벌만 유지"는 맞았고, 맡긴 곳이 틀렸다**

**결정.** 탈퇴의 파일 삭제를 `sb_storage_remove` 직접 호출이 아니라 GD-8b 의 **`_sweep_one` 에 위임**한다.

**왜.** 처음엔 *"한 벌만 유지하려고"* 상태 갱신을 sweeper 에 맡겼다. 그런데 sweeper 는 **관리자가 손으로
`POST /diary/admin/sweep` 을 불러야만 돈다**(크론 없음). 결과는 **아무도 안 도는 큐**였다 —
실측에서 파일은 지워졌는데(`storage.objects = 0`) 영수증은 `pending` 2건으로 굳어 있었고,
경로 문자열(`{user_id}/{profile_id}/img_2026-08-10_…`)이 계속 남았다.

**배운 것.** ⭐ **"나중에 처리하는 큐"를 만들 때는 *누가 언제 그 큐를 도는가*를 같은 자리에 적을 것.**
도는 사람이 없으면 큐가 아니라 **쓰레기통**이다.
부수: 검사도 위임한 함수 본문을 열어 *"정말 `done` 으로 닫고 `paths` 를 비우는가"* 까지 본다(⑫).

**자산.** `4b4b098` · `account.py` 의 `_sweep_one` 위임 · 차단시험 ⑪⑫

**되돌리려면.** 없음(고칠 이유가 없다). 다만 sweeper 를 자동화하면 이 위임은 선택이 된다.

---

### 결정 ④ — **"남기는 쪽이 안전하다"는 느낌이, 실은 개인정보를 남기는 쪽이었다**

**결정.** `backup_checkins_judgeweek`(38줄) · `backup_history_judgeweek`(14줄) 를 **버렸다.** FK 를 걸어 남기지 않았다.

**왜.** 팀장이 처음엔 *"시상식(8/14)까지 보험으로 남기고 FK 만 걸자"* 고 권했다.
오너가 **"보험이 정말 필요하냐"** 고 되물어 근거를 열어보니 셋 다 무너졌다:
①이 복사본은 **7월 어느 시점에 멈춰 있다**(그 뒤 데이터는 안 들어간다) ②**복구 절차가 없다**
③원본이 망가질 시나리오를 하나도 대지 못했다(시상식은 발표지 데이터 조작이 아니다).
게다가 작업량도 버리는 쪽이 적다 — FK 를 걸면 8/14 이후 같은 일을 또 해야 한다.

**배운 것.** ⭐ **"보수적인 선택"이 항상 안전한 쪽은 아니다.** 개인정보에서는 **남기는 것이 리스크**다.
'안전해 보이는 쪽'을 고를 때도 *"무엇으로부터 안전한가"* 를 한 번 물어야 한다.
🔴 그리고 이번에도 **오너의 되묻기가 갈랐다** — 트리거 ②(*"이게 뭐야?"*)와 같은 종류다.

**자산.** `011_drop_judgeweek_backups.sql` · 실측 3단계(행수 38·14 / 복사본에만 있는 줄 0 / 내용이 다른 줄 0)

**되돌리려면.** 원본에서 다시 뜰 수 있다(`create table ... as select * from ...`).
⚠️ 다시 만들면 같은 문제가 되살아난다 — 그때는 반드시 `on delete cascade` 를 함께 걸 것.

---

### 결정 ⑤ — 지우기 전에 **"줄이 있다"와 "내용이 같다"는 다른 질문**

**결정.** 백업을 버리기 전에 ①행수 ②줄 존재 비교 ③**내용 비교** 세 단계를 전부 봤다.

**왜.** ②만 보고 지우려다 멈췄다. `id` 가 원본에 다 있어도 **내용이 덧칠됐을 수 있다** —
백업의 목적이 *"실수로 지울까 봐"* 가 아니라 *"실수로 바꿀까 봐"* 였다면 ②는 아무것도 증명하지 않는다.
`to_jsonb(b) is distinct from to_jsonb(c)` 로 확인해 0 이 나온 뒤에야 지웠다.

**배운 것.** ⭐ **되돌릴 수 없는 삭제 앞에서는 "무엇을 확인했나"가 아니라 "무엇을 아직 안 봤나"를 묻는다.**
확인 목록이 길수록 안심되지만, 빠진 축은 그 목록에 안 보인다.

**자산.** `011` STEP 0 (재실행 가능한 확인 쿼리로 파일에 박아둠)

**되돌리려면.** 해당 없음.

---

### 결정 ⑥ — 손으로 하던 시범 테스트를 **세 겹의 자동 검사**로 대체했다

**결정.** 오너 질문(*"재시범 테스트를 자체적으로 할 방법은 없나?"*)에서 시작해 검증을 세 겹으로 나눴다.

| 겹 | 무엇 | 보는 것 |
|---|---|---|
| ① 정적 계약 | `_verify_account_delete.py` | 코드가 **그렇게 생겼는가** |
| ② 실물 FK | `sql/_db_tables.txt` | DB 가 **실제로 어떻게 동작하는가**(pg_constraint) |
| ③ 실행 기록 | `_verify_account_delete_run.py` 🆕 | 진짜 돌렸을 때 **뭐가 어떤 순서로 불리는가** |

**왜.** 손으로 하는 테스트는 **내가 만든 데이터가 있는 곳만** 본다.
지난 시범은 `care_signals` 를 검증하지 못했다 — 그 계정이 위기 발화를 한 적이 없어
**데이터가 없어서 0 이 나온 것**을 "지워졌다"로 읽을 뻔했다. 게다가 계정을 만들고 그림을 그려야 해서 돈이 든다.
③은 데이터를 내가 정해서 넣고 **실패까지 만들어본다** — 글자로는 절대 볼 수 없는
*"auth 삭제가 실패하면 명세서를 안 넣는가"* 를 여기서 본다.

**배운 것.** ⭐ **"검증했다"는 세 가지 다른 뜻이다 — 코드가 그렇게 생겼다 / DB 가 그렇게 동작한다 / 실제로 그렇게 돌았다.**
셋을 구분하지 않으면 하나만 하고 다 했다고 믿는다. 오늘 아침의 나는 ①만 하고 있었다.

🔴 **부수: 가짜가 틀려서 검사가 헛돌던 것을 두 번 잡았다.** 3원칙 ③(*"통과가 났다 → 배선부터 의심하라"*)이 두 번 다 맞았다.
- 명세서 insert 가 **고정값**을 돌려줘 청크마다 전체 경로가 왔다 → 파일이 2배로 세어졌다
- select 가 **필터를 무시**해 언제나 내 것만 돌려줬다 → *"남의 파일이 안 섞였다"* 검사가 **아무것도 안 하고 있었다.**
  차단시험 ⑦(`user_id` 필터 제거)이 **통과해버려서** 발견했다 — 차단 시험이 없었으면 영영 몰랐다.

**자산.** `004490c` · 실행검사 54/0 · 차단시험 7/7 · `_verify_fakes` 확장(FakeDB `table_returns`+callable / FakeHTTP `delete`·`status_map`, 자체 점검 11항목 추가)

**되돌리려면.** `_verify_all.py` 의 `CHECKS` 에서 `_verify_account_delete_run.py` 한 줄 제거.
⚠️ 도구 확장은 하위호환이라 기존 검사에 영향 없다.

---

### 결정 ⑦ — **싼 것에 한도가 없는 쪽이 더 위험했다**

**결정.** 챗봇(`/chat`)에 분당 15 · 하루 80 을 걸었다. 이어서 나머지 6종(`tts`·`checkin_react`·`greeting`·`schedule_ai`·`report_coach`·`report_weekly`)도.

**왜.** 오너 질문에서 드러났다 — *"1,020원이 우리 API 전부 + 서버 저장 값이야?"*
아니었다. **그림 생성 2종 + 정밀검수, 그게 전부**였고 나머지는 전부 무제한이었다.

| | 1회 원가 | 하루 한도 |
|---|---|---|
| 그림 생성 | **70원**(비싸 보인다) | 6회로 막혀 있었다 |
| 챗봇 | 2~4원(싸 보인다) | 🔴 **무한** |

1회 5원이라도 **하루 10만 회면 50만원**이다. 인증만 통과하면 초당 몇 번이든 가능했다.

**배운 것.** ⭐ **한도를 걸 우선순위는 "1회 원가"가 아니라 "1회 원가 × 막지 않았을 때의 횟수"다.**
비싼 것은 눈에 띄어 먼저 막게 되고, **싼 것은 무한이어도 넘어간다.**

**자산.** `25ae401`·`e2684cf` · 비용 상한 65/0 · 차단시험 13/13 · 계정 최악 1,020 → 2,600원/일

**되돌리려면.** `quota.LIMITS` 에서 해당 종류를 지우면 검사가 *"선언만 하고 아무도 안 쓰는 종류"* 로 막는다 → 라우터의 `check_and_consume` 도 함께 지워야 한다(의도된 두 겹).

---

### 결정 ⑧ — 🔴 **검사가 자기가 아는 것만 봤다** (오늘 두 번째 같은 사각지대)

**결정.** `_verify_quota.py` 에 **[K] 한도 커버리지** 를 신설했다. 라우터를 **전수**로 훑어 *"돈 쓰는 코드가 있는데 한도가 없는 파일"* 을 찾는다.

**왜.** 챗봇이 무제한인 걸 놓친 뿌리가 여기다 — 검사는 **quota 를 쓰는 경로만** 봤다.
안 쓰는 경로는 **존재조차 몰랐다.**

> 🔴 같은 날 `care_signals` 가 SQL 파일에 없어서 안 보였던 것과 **정확히 같은 종류**다.
> 아침에 결정 ②로 배웠는데, 저녁에 다른 얼굴로 또 만났다.

**배운 것.** ⭐ **"검사가 무엇을 못 보는가"를 검사에 적어라.**
면제 목록에는 **이유를 함께** 적는다(이유 없는 면제는 구멍). 반대 방향도 본다 — 선언만 하고 안 쓰는 종류.
🔴 **그리고 이 검사가 바로 내 실수를 잡았다** — `schedules.py` 를 빠뜨렸는데 커밋 전에 걸렸다.

**자산.** `e2684cf` · `_verify_quota.py [K]` · 차단시험 ⑧~⑬(6종 각각 빼보기)

**되돌리려면.** 없음(고칠 이유가 없다).

---

### 결정 ⑨ — 💰 **원가 실측: 그림 생성을 절반으로 낮춰 잡고 있었다**

**결정.** 오너가 두 콘솔에서 실제 청구액을 꺼냈다. 하네스 「출시 전 필수 3건」의 *"원가 실측"* 완료.

| | 내 추정 | **실측** | |
|---|---:|---:|---|
| `diary_gen` | 40원 | **70원** | 🔴 **낮게** |
| `diary_continue` | 90원 | **180원** | 🔴 **절반으로** |
| `report_coach`·`weekly` | 30원 | **15원** | 3배 과대 |

**왜 정확히 나왔나.** OpenAI 일자별 금액에 **단독 호출일**이 있었다 —
8/1·8/4 는 `mini` 1회뿐($0.05006), 8/10 은 `gpt-image-1` 1회뿐($0.129745).
그리고 혼합일(8/8)이 그 값으로 검산됐다($0.3095 ≈ 실제 $0.310247).

**배운 것.** ⭐ **낮게 잡은 원가는 "안전해 보이는데 실제로는 더 나가는" 방향이다.**
한도표의 `cost_krw` 는 장식이 아니라 **하루 최대 노출액을 계산하는 입력값**이고, 그게 틀리면 판단 전체가 틀린다.
부수: **이어그리기가 비싼 이유가 토큰에 그대로 보였다** — 아이 낙서를 이미지로 함께 올려서(입력 6,531토큰) 출력이 `mini` 의 1/4인데도 3.6배 비싸다.

**자산.** `fd6354f` · 계정 최악 2,300 → **2,600원/일**(월 78,000원)

**되돌리려면.** 해당 없음(실측값이다).

---

### 부수적으로 배운 것

- 🔴 **CLOVA TTS 는 요금 구조가 다르다** — **횟수가 아니라 글자 수**다.
  `월 기본료 90,000원 + 100만 자 무료 + 초과 1,000자당 100원`. 문구 평균 20자, 아이 1명 월 25,261자, **100만 자로 약 39명** 커버.
  ⇒ TTS 한도의 목적은 *"돈 막기"* 가 아니라 **"100만 자를 넘지 않게"** 다. 하루 200회가 그 선과 맞는다.
  ⚠️ **캐싱을 넣을 때: 프론트 캐시여야 이 한도가 여유로워진다.** 서버 캐시는 CLOVA 호출만 줄이고 우리 서버 요청은 그대로다.
- 🔴 **TTS 는 막히는 방식이 최악이었다** — 실패가 전부 `null` 로 뭉개져 **키디가 아무 말 없이 조용해졌다.**
  4~7세는 글을 못 읽으니 아이는 그냥 고장으로 읽는다.
  ⭐ **여기서 원칙 하나가 나왔다: "조용히 막히면 조이면 안 되고, 보이게 막히면 조여도 된다."**
  하네스의 *"조이는 쪽 실수는 조용히 일어난다"* 가 실은 **숫자가 아니라 조용함**의 문제였다.
  ⚠️ 말풍선 표시는 **반쪽**이다(아이는 글을 못 읽는다). 아이용 안내는 따로 설계해야 한다.
- **한도 메시지가 그림일기 전용 하나뿐이었다** — *"오늘은 그림을 많이 그렸어요"*.
  대화를 했는데 그 말이 나가면 **사실 왜곡**(검증 축 4)이다. 종류별 문구를 넣었다.
- **CORS(B6)를 하려다 멈췄다.** 근거를 열어보니 둘 다 무너졌다:
  ①*"비용을 막는다"* → **curl 은 CORS 를 무시한다**(실증: 악성 Origin 을 붙여도 200)
  ②*"미인증 문이 생기면 안전망"* → 같은 이유로 약하다.
  우리는 Bearer 토큰이라 CSRF 도 없다. ⇒ **거의 아무것도 안 막는데 틀리면 서비스가 죽는다.**
- 🔴 **이메일 인증 링크가 `localhost:3000` 으로 가고 있었다.** Supabase **Site URL** 이 그대로였고,
  우리 `signUp` 은 `emailRedirectTo` 를 주지 않아 **무조건 Site URL 로 간다**(`AuthContext.jsx:56`).
  Confirm email 을 켠 2026-08-09 이후 가입 시도자는 **전원 문 앞에서 막혔다.** → Site URL·Redirect URLs 수정(오너).
  ⇒ **기능을 켤 때는 그 기능이 딛고 선 설정도 함께 본다.** 코드는 멀쩡했고 설정이 틀렸다.
- **`004`·`006` 의 `user_id` 에 FK 가 없다는 주석만 보고 "탈퇴해도 남는다"고 판정했다가 정정했다.**
  같은 표의 **다음 줄** `profile_id` 가 `profiles` 로 cascade 라 2단으로 지워지고 있었다.
  한 줄을 보고 표 전체를 판정한 것 — 8/9 의 *"저장된 값에서 동선을 추론하지 말라"* 와 같은 종류다.
- **SQL 파일을 드릴 때 `STEP` 단위로 끊어 실행해달라고 명시할 것.** 오너가 011 을 통째로 실행해
  `drop` 까지 함께 돌았다. 이번엔 무손실이 확인된 뒤라 괜찮았지만, 다음이 위험하다.
- ✅ **재시범 테스트는 결정 ⑥ 으로 해결했다** — 손으로 다시 하는 대신 자동 검사 세 겹으로.
  `care_signals` 는 이제 ②(실물 FK)가 `auth 직접` 으로 확인한다.
- ⚠️ **그래도 못 보는 것 하나:** 실제 Supabase Storage 에서 파일이 사라지는 것.
  8/10 실계정 시범에서 확인됐지만(`storage.objects = 0`), 자동 검사로는 대체되지 않는다.

---

## 2026-08-08 (세션 8 · → 08-09, 7커밋) — 🟢 킬스위치를 켰다

> **한 줄:** 킬스위치를 켠 게 아니라, **켜야만 보이는 것들을 찾아 고친 날**이다.
> 켠 직후 30분에 버그 3건이 쏟아졌고, **전부 오너 시범 테스트가 찾았다. 내 테스트는 초록불이었다.**

### 결정 ① — 자물쇠를 없애지 않고 **지키는 대상을 바꿨다**

**결정.** `DIARY_SERVER = true`. 그 과정에서 *"켜지 마라"* 를 못박던 검사 4건을 지우지 않고 *"켰다면 이것들이 갖춰져야 한다"* 로 승격했다.

**왜.** 자물쇠를 지우면 **왜 잠갔는지도 함께 사라진다.** 6개월 뒤 누가 되돌릴 때 근거가 없다.
승격하면 조건이 그대로 살아 있고, 다시 끄면 검사가 조용히 통과한다 — **되돌리는 길을 막지 않는다.**

| 자물쇠 | 전 | 후 |
|---|---|---|
| `diary-consent` | `DIARY_SERVER === false` | 켠 상태에서도 **동의 없는 아이는 막힌다**(대조군 포함) |
| `diary-server-push` | "플래그 OFF면 네트워크 0" | **"동의가 없으면 네트워크 0"** (더 강한 명제) |
| `privacy-policy` | 빈칸이어야 한다 | **켰다면 보호책임자·시행일이 있어야 한다**(법 제30조) |
| `_verify_diary_contract` | `false` 여야 한다 | **켰다면 지울 방법이 실재해야 한다** |

**배운 것.** ⭐ **자물쇠를 만들 때 "언제 풀리는가"를 함께 적어두면, 풀 때 지우지 않고 승격할 수 있다.**
지우면 지식이 사라지고, 승격하면 지식이 남는다.

**자산.** `fa0a170` · `_verify_diary_contract.py [C]` · 차단시험 6/6

**되돌리려면.** `diaryStore.DIARY_SERVER` 와 `Privacy.jsx` 의 `DIARY_SERVER_LIVE` 를 **둘 다** false 로. 한쪽만 바꾸면 검사가 *"허위 기재"* 라며 막는다(의도된 장치).

---

### 결정 ② — 삭제 전파는 **서버가 명시한 것만** 지운다

**결정.** 다른 기기에서 지운 일기를 반영하되, 판정 근거는 **서버의 삭제 명세서**(`diary_deletions.client_entry_id`, 009 SQL)로 한정한다. *"서버 목록에 없으면 지운다"* 는 **채택하지 않았다.**

**왜.** 후자로 만들면 **서버가 빈 응답을 주는 사고 한 번에 아이 일기가 통째로 날아간다.**
지우는 일은 되돌릴 수 없다. 조회 실패·빈 목록이면 **아무 일도 일어나지 않는** 쪽이 옳다.

**배운 것.** ⭐ **"서버엔 없고 로컬엔 있다"는 한 가지가 아니라 두 가지다** — ①푸시 실패분(다시 올려야) ②다른 기기에서 지운 것(지워야).
구분할 표식이 없으면 코드는 **한쪽으로만 해석**하고, 그 해석이 틀린 절반에서 사고가 난다. 실제로 지운 일기가 화면에 부활하고 **재푸시로 서버에까지 되살아날 수 있었다.**
→ 상태가 겹쳐 보일 때는 **"이 신호가 뜻할 수 있는 다른 상황은 무엇인가"** 를 먼저 세어볼 것.

**자산.** `c97e9d9` · `server/sql/009_diary_deletion_client_id.sql` · `GET /diary/deletions` · `[C-2]` 12건 · 차단시험 5/5

**되돌리려면.** 009 롤백은 **순서가 있다** — ①007 함수 재실행 ②컬럼 삭제. 반대로 하면 트리거가 없는 컬럼에 insert 하다 **삭제가 통째로 실패**한다.

---

### 결정 ③ — 비용 한도에 **계정 총량**을 한 겹 더

**결정.** 아이별 한도 위에 `ACCOUNT_LIMITS` 신설. `analyze_deep` 은 20이 아닌 **30**으로.

**왜.** 한도는 **한 층만으로는 못 막는다.** 아래를 막으면 위로 우회한다.
아이별만 있으니 프로필(최대 4개)을 늘리면 최악이 **4배**였다 — 월 56,100원.
30을 고른 이유: 20으로 조이면 **한 아이가 하루 20편을 보면 나머지 셋이 아예 못 본다.** 아낀 돈은 계정당 월 5,100원인데 대가가 더 확실했다.

**배운 것.** ⭐ **조이는 쪽에도 위험이 있고, 그쪽은 조용히 일어난다.**
*"막으면 안전"* 이 아니다. 아이가 그림을 못 그리는 것도 사고인데 **아무도 신고하지 않는다.**
그래서 한도를 정할 때 **막히는 케이스보다 통과해야 하는 케이스(대조군)를 먼저 쟀다.**

⭐ 그리고 **내가 만든 장치가 내 실수를 잡았다.** 검증 조건을 *"계정 총량이 아이별보다 크면 무의미"* 라고 **반대로** 썼는데(계정 총량의 목적은 여러 아이의 **합**을 막는 것이라 커도 의미가 있다), `_validate_limits()` 가 서버를 아예 못 뜨게 막아서 알았다.
→ **장치는 남을 막으려고 만들지만, 결국 나를 가장 많이 막는다.**

**자산.** `5c397a3` · [비용 방어 3층과 남은 구멍](고도화/원가와가격/비용_방어_3층과_남은_구멍.md) · `[D-2]` 8건

**되돌리려면.** `ACCOUNT_LIMITS` 를 비우면 아이별 한도만 남는다. ⚠️ 그 순간 **아이 4명 기준 월 56,100원**으로 돌아간다.

---

### 결정 ④ — 아이 화면에는 **이유를 설명하지 않는다**

**결정.** 빈 책장이 왜 비었는지(동의 꺼짐/조회 실패/정말 0편)를 **부모 화면에만** 안내한다. 아이 화면 문구는 그대로 뒀다.

**왜.** *"동의가 꺼져 있어요"* 는 아이가 이해할 수도 없고 **이해시켜서도 안 되는 말**이다. 아이는 시스템 사정을 몰라야 한다.
부모는 반대다 — **왜 비었는지 알아야** 다음 행동(토글을 켠다)으로 이어진다.

**배운 것.** ⭐ **같은 사실이라도 아이와 부모에게 말할 것이 다르다.** 이건 카피 문제가 아니라 **윤리선**이다.
그래서 규칙으로 두지 않고 **검사로 못박았다** — 부모용 카피가 아이 화면 3곳으로 새면 테스트가 막는다.

⭐ 그리고 **오너가 헷갈리면 부모는 더 헷갈린다.** 오늘 오너가 같은 화면에서 **두 번** 헷갈렸고(①동의 꺼진 아이 ②캐시가 옛 값), 그게 이 작업을 앞당긴 근거였다.

**자산.** `57c67df` · `BLANK_SHELF_WHY` · `blank-shelf-why.dom.test.jsx` 9건 · 차단시험 3/3

**되돌리려면.** `BLANK_SHELF_WHY` 를 안 쓰면 기존 한 문장으로 돌아간다. 단 [C] 검사(아이 화면 무접촉)는 남겨둘 것.

---

### 결정 ⑤ — 되돌리는 것도 결론이다

**결정.** 아이 책장에서 동의 캐시를 갱신하려던 수정을 **철회**했다.

**왜.** 넣었더니 **테스트 9개가 깨졌고**, 그게 신호였다. `ProfileSelect` 가 **이미** `getProfiles()` 를 부르고 그 안에서 동의 캐시가 교체된다 — **새로고침 한 번이면 풀리는 일**이었다.
아이 화면에 서버 왕복을 하나 더 얹을 이유가 없다.

**배운 것.** ⭐ **테스트가 무더기로 깨지면 "테스트를 고칠 일"이 아니라 "설계를 다시 볼 일"일 수 있다.**
9개 파일을 다 고치는 쪽으로 갔다면 문제는 남고 부채만 늘었다.

⭐ 그리고 **더 단순한 답이 이미 있는지 먼저 볼 것.** 오늘 하루에 같은 종류의 실수를 두 번 했다 —
저장된 데이터의 값 이름(`imgSource: "mine"`, `imgState: "idle"`)에서 **화면 동선을 추론**해 오너에게 잘못 안내했고, 그중 하나는 **오너가 실제로 돈을 썼다.**
→ **저장 결과와 사용자 동선은 다른 것이다.**

**자산.** `57c67df` 안의 `FamilyShelf.jsx` 주석(철회 이유를 코드에 남김)

**되돌리려면.** 다시 넣으려면 FamilyShelf 를 쓰는 테스트 **9개**의 api 모킹에 `getProfiles` 를 추가해야 한다. 그 비용을 감수할 이유가 생겼는지 먼저 확인할 것.

---

### 결정 ⑥ — 되돌리기를 만들지 않고 **실수를 줄인다** (지우기 2초 길게 누르기)

**결정.** 그림일기 지우기를 **클릭 한 번 → 2초 길게 누르기**로 바꿨다. 휴지통·복구는 **만들지 않는다.**

**왜.** 처리방침 제5조에 *"지우면 정말 지워집니다"* 라고 썼다. 휴지통을 만들면 그 약속이 거짓이 된다.
그런데 확인 화면만으로는 부족했다 — **4~7세는 '응'을 습관적으로 누른다.** 탭 두 번이면 일기가 사라졌다.
길게 누르기는 클릭과 달리 **의도를 요구**한다. ⇒ **복구를 만들지 말고 실수를 줄인다.**

**배운 것.**
⭐ **아이 UX 에서 '확인 화면'은 방어가 아니다.** 어른은 확인 화면을 읽지만 아이는 **화면 모양을 기억해서 위치로 누른다.** 한 번 더 묻는 것과 **다르게 조작하게 만드는 것**은 다른 방어다.

⭐ 🔴 **그리고 이 아이디어를 내가 먼저 던졌는데, 원안은 이미 다 있었다.**
서랍에 *"지금은 확인 없이 바로 찢긴다"* 고 썼으나 **사실이 아니었다** — `TEAR` 에 확인 화면·*"되돌릴 수 없어요"*·죄책감 방지(`"응, 지웠어. 괜찮아!"`)까지 전부 있었고, **내가 제안한 문구보다 기존이 나았다.**
⚠️ 어제(08-07) 이 문서에 *"승격 전에 '이미 어딘가에 있지 않나'를 먼저 실측할 것"* 이라고 **직접 써놓고 다음 날 어겼다.**
그리고 오늘만 같은 종류로 **세 번**이었다 — `imgSource: "mine"` → 직접 그리기가 있다고 추정 / `imgState: "idle"` → 그림 없이 저장된다고 추정 / 확인 없이 찢긴다고 추정. **셋 다 코드를 안 열고 값 이름에서 화면 동선을 유추했다.**
→ **저장된 데이터의 모양과 사용자가 밟는 길은 다른 것이다.** 서랍에 던질 때는 실측 없이 던져도 되지만, **오너가 "해보자"고 한 순간부터는 다르다.**

⭐ **그런데 진짜 빈 곳은 오너가 찾았다** — *"확인을 눌러도 결국 클릭 한 번이잖아."*
내 원안은 죽었지만 **그 자리에서 더 나은 것이 나왔다.** 틀린 아이디어도 대화에 올려두면 값을 한다.

**자산.** `dfc2bbc` · `components/HoldToConfirm.jsx` · `hold-to-confirm.dom.test.jsx` 12건 · 차단시험 4/4
설계 판단 4가지를 컴포넌트 주석에 남겼다(차오르는 표시 필수 / 밖으로 나가야 취소 / 뗄 때 즉시 0 / 취소는 클릭 한 번).

**되돌리려면.** `HoldToConfirm` 을 `<button onClick=…>` 으로 되돌리면 된다. ⚠️ 단 그 순간 테스트 `[D]` 가 잡는다 — **의도한 것이면 테스트도 함께 고칠 것.**
⚠️ **'다시 만들기'(`doRemake`)에는 붙이지 않았다.** 삭제가 아니라 교체 흐름이라 2초는 방해다. 되살릴 때 여기까지 넓히지 말 것.

---

### 부수적으로 배운 것

- **부하와 진짜 실패가 섞이면 원인이 흐려진다.** `Failed to start forks worker` 는 코드 문제가 아니다(load 21.58 — 게임+dev서버). 그런데 **그 밑에 진짜 문법 오류가 깔려 있었다.** 오너가 *"게임 때문 아니냐"* 고 물어 갈렸다. → 테스트가 무더기로 깨지면 **`uptime` 을 먼저 보되, 그것만이 원인이라고 단정하지 말 것.**
- **사업자 등록이 처리방침을 막지 않는다.** 어제 GD-40에 *"사업자가 제11조를 막고 있다"* 고 썼는데, 법이 요구하는 것은 **보호책임자의 성명 또는 담당부서 명칭과 연락처**다. 사업자등록번호는 법정 기재사항이 아니다. → **한 덩어리로 묶기 전에 각각이 정말 같은 뿌리인지 확인할 것.**
- **없는 부서 이름을 지어내지 말 것.** 처음 *"개인정보 보호 담당"* 이라 적었더니 오너가 *"그래서 담당이 누군데?"* 라고 물었다. 1인 운영이라 그런 부서가 실재하지 않는다 → **"키디 운영자"**. 실제 구조를 그대로 적는 편이 정확한 고지다.

---

## 2026-08-07 (세션 7 · → 08-08, 46커밋) — 📓 GD-8 그림일기 서버 이전 완주 + 🔐 동의 체계

> **한 줄:** 기능은 어제 다 만들어져 있었다. 오늘 한 일은 **한 번도 안 불려본 코드를 실제로 불러본 것**이고,
> 거기서 버그가 여섯 개 나왔다. **전부 오너 시범테스트에서 나왔고, 내 테스트는 전부 초록불이었다.**

### 결정

1. **GD-8 3단계 완주** — 8a(저장)·8b(삭제 관철)·8c(이사) + SQL 006·007·008 실행 완료.
2. **동의 체계 신설(B13·B14)** — 게이트를 **두 겹**으로. 전역 킬스위치 ∧ 보호자 동의(아이 한 명 단위).
3. **처리방침을 앱에 붙였다(B1)** — `/privacy`. 앱 전체에 방침 경로가 **0건**이었다.
4. **그림을 저장 시점에 압축한다** — 무손실 PNG → WebP. 실측 **3.82MB → 0.68MB (82%↓)**.
5. **🔴 하네스의 거짓을 지웠다** — *"`.claude/rules/` 는 자동 로드된다"* 는 **사실이 아니었다.**

### 왜

- **동의 없이 아동 데이터를 저장하면 무단 수집이다.** 어제 만든 서버 저장은 동의 절차가 없어 영영 켤 수 없는 상태였다.
- 그런데 **프론트 게이트는 보장이 아니다** — 토큰만 있으면 라우터를 직접 부른다.
  그래서 서버가 `get_consented_profile` 로 **한 번 더** 막는다(403).
- **단, 삭제에는 동의를 요구하지 않는다.** 요구하면 *"철회하면 지울 수 없다"* 가 되어 정반대가 된다.

### 🔴 배운 것 ⭐ — 오늘 나온 버그 여섯 개는 **세 가지 뿌리**에서 나왔다

**① 같은 일을 두 번 구현하면 반드시 어긋난다** (오늘만 3회)

| | 무엇이 어긋났나 |
|---|---|
| 이사 엔진이 게이트를 우회 | `diaryStore` 를 안 거치고 `api` 를 직접 불러 **킬스위치·동의를 통과** |
| 이사 엔진이 업로드를 재구현 | **썸네일 누락**(책장에서 3.8MB 원본을 받음) + **role 4종 전부 오류** |
| `diary.py` 의 `sb_update` | `profiles.py` 는 (필터, 패치)인데 **6곳 전부 반대** |

⇒ 새 경로를 만들 때 **"저장 함수에 게이트를 걸었으니 됐다"** 가 아니다.
   **api 를 직접 부르는 곳마다** 다시 확인해야 한다.

**② 플래그가 꺼져 있으면 "동작 안 함"이 아니라 "동작 안 하는 줄도 모름"이다**

`sb_update` 6곳이 뒤집혀 있었는데 **아무도 몰랐다.** `DIARY_SERVER=false` 라 한 번도 안 불렀기 때문이다.
부모 도장·그림 수정·삭제 sweeper 가 **전부 죽어 있었다.**
⇒ 플래그로 잠근 코드는 **잠긴 채로 썩는다.** 켜기 전에 한 번은 실제로 불러봐야 한다.

**③ 검사가 0건을 찾고도 통과할 수 있다** (오늘 2회)

- `body_of` 가 JS 전용이라 파이썬 함수에 **빈 문자열**을 줬다 → `"X" not in ""` 은 **항상 참**
- `sb_update` 검사 정규식이 f-string 중괄호(`f"eq.{cid}"`)에 걸려 **0건** → *"위반 0건"* 으로 통과

⇒ **부정 검사(`not in`) 앞에는 반드시 "대상을 찾았는가"를 먼저 검사한다.**
   두 번째 것은 그 가드를 미리 넣어둔 덕에 잡혔다.

**④ 하네스에 거짓이 적혀 있는 것이 규칙이 없는 것보다 나쁘다**

CLAUDE.md 가 *"rules 는 해당 파일을 열면 자동 로드된다"* 고 적고 있었는데 **그런 기제가 없었다.**
같은 날 `frontend.md` 의 배럴 import 금지를 내가 어겼고, **pre-commit 훅이 막아서야** 알았다.
⇒ **진짜 보장은 장치다.** 규칙은 '왜'를 남기고, **지키게 하는 것은 장치**다.

### 오너가 찾아낸 것 (전부 내 테스트는 통과 상태)

| | |
|---|---|
| 이사 엔진이 킬스위치를 우회 | 부모 화면에 *"일기 0편을 보관했어요"* 배너가 떠서 발견 |
| 카드가 지키지 못할 약속 | 킬스위치 꺼진 채 *"켜면 폰에서 보여요"* 라고 동의를 구함 |
| *"껐어요"* 와 *"켜기 전에"* 가 동시 표시 | 모순 문구 |
| 기기 간 미연동을 **아이폰 문제**로 씀 | 안드로이드 부모가 *"난 해당 없네"* 하고 넘어감 |
| 그림 용량 | → 82% 압축으로 이어짐 |
| sweep 502 | → `sb_update` 6곳 발견 |

### 자산

- 검사 **302건**(프론트) + 계약검사 **8종**(서버). 오늘 **70건 이상 추가**, 전부 **발화 확인**
- 신설 문서 4종 — [왜 서버여야 하는가](고도화/기능별/GD-8_그림일기서버이전/왜_서버여야_하는가.md) · [보유와 파기 설계](고도화/기능별/GD-8_그림일기서버이전/보유와파기_설계.md) · [GD-39 가족 앨범](고도화/기능별/GD-39_가족앨범.md) · [유료플랜 구성과 노출](고도화/원가와가격/유료플랜_구성과_노출.md)
- 고아 자산 청소기 + 판정 검증 24건 (오탐 방지 — 참조 필드 4개를 **각각** 시험)

### 남은 것

🔴 **킬스위치는 아직 `false`** — 켜려면 두 곳(`DIARY_SERVER`·`DIARY_SERVER_LIVE`)을 **함께** 켜야 하고,
한쪽만 켜면 검사가 커밋을 막는다(의도된 장치). 그 전에 **B8 Supabase 리전 · B9 DPA** 확인이 필요하다.

---

## 2026-08-07 (세션 6) — 💎 서버 저장을 유료 플랜에 넣는다 (오너 결정)

> **왜 이 결정이 기록으로 남을 만한가**: 이건 기능 결정이 아니라 **사업 구조 결정**이고,
> 6개월 뒤에 *"왜 그림일기를 유료로 했더라?"* 를 반드시 다시 묻게 된다.

### 결정

1. **그림일기 서버 저장은 유료 플랜에 넣는다.**
2. **iOS·안드로이드 구분 없이 제공한다.** (서버 저장은 플랫폼 무관 — 이미 그렇게 만들어져 있다)
3. **iOS 7일 규칙을 전면에 내세우지 않는다.** 개인정보 처리방침에만 적는다.
   랜딩·요금제·결제 화면에는 넣지 않는다.

> 오너: *"서버에 저장하는 것이 유료 플랜에 들어가야 해. (…) 그래야 우리 서버비용도 낼 수 있다는 걸 명심해.
> (…) 이건 정면으로 우리의 페르소나를 위협할 수도 있지만 우리는 법을 어긴 것도, 규칙을 어긴 것도 사실 없어."*

### 왜

- **서버 비용을 낼 수 있어야 서비스가 계속된다.** `고도화/원가와가격/README.md` 진단 그대로 —
  지금은 무료 사용자가 유료와 거의 같은 원가를 쓴다. **회원이 늘수록 손해**인 구조다.
- 계획서상 프리미엄 1순위인 **가족 앨범 PDF·실물 인쇄**는 서버 저장 없이는 만들 수 없다.
  즉 서버 저장은 이미 유료 기능의 **전제**였고, 이번에 이름을 붙인 것뿐이다.
- 재개 조건(`원가와가격` §4-1 "2층 기능 구현 완료 — GD-8 서버이전 등")이 **오늘 충족**됐다.

### 페르소나와 충돌하는가 — **하지 않는다. 다만 선이 하나 있다**

**말하지 않는 것(omission)과 사실이 아닌 말을 하는 것(misrepresentation)은 다르다.**

| | 판단 |
|---|---|
| 랜딩·요금제에 7일 규칙을 안 쓴다 | ✅ 문제없음. 마케팅은 *무엇을 주는지* 말하는 자리지 한계를 나열하는 자리가 아니다 |
| 처리방침에 정확히 적는다 | ✅ 고지 의무는 여기서 이행된다 |
| 결제 화면을 캐비어 없이 깔끔하게 | ✅ 문제없음 |
| 🔴 *"무료로도 안전하게 보관됩니다"* 류의 문구 | ❌ **이건 omission 이 아니라 거짓이다. 절대 쓰지 않는다** |

**이 한 줄만 지키면 법적으로도 페르소나 상으로도 문제가 없다.**
GD-38(완벽하지 않은 키디)이 요구하는 건 *"모든 한계를 모든 화면에 나열해라"* 가 아니라
*"못 하는 걸 할 수 있다고 말하지 마라"* 이다.

### ⚠️ 붙여둔 조건 — 🔴 결제는 동의를 갈음하지 못한다

돈을 받았다고 개인정보 처리 동의를 받은 것이 되지 않는다. **결제 → 기능 해금 → 그때 별도 동의** 순서.
결제 화면에 *"결제 시 그림일기가 클라우드에 저장됩니다"* 를 끼워 넣어 갈음하는 방식은 쓰지 않는다.
(법 제22조·제28조의8 / 전문가 질문 B1 부록 A #21-1 에 등재)

### 팀장 제안 — 숨기기보다 **제때 알리기가 전환에 더 낫다**

7일 규칙을 결제 화면에 쓰지 않는 것은 맞다. 다만 **아무 데서도 안 알리면 사용자는 그냥 데이터를 잃고 떠난다.**
화난 사용자는 결제하지 않는다.

→ 결제 화면이 아니라 **잃기 직전**에 알린다.
  무료 사용자가 5일째 미접속이면 *"그림일기가 곧 정리될 수 있어요. 안전하게 보관할까요?"* → 요금제로.
  결제 화면은 깨끗하게 두면서 전환 트리거로 작동하고, **아무도 데이터를 잃지 않는다.**
  (미착수 — 오너 판단 대기)

### 배운 것 ⭐

1. **한계를 '어디에' 쓰느냐가 '쓰느냐 마느냐'보다 중요하다.** 처리방침에 있으면 고지는 끝난 것이다.
2. **불편한 사실을 숨기는 것과 제때 알리는 것 중, 전환에 유리한 쪽은 후자다.** 공포는 상실의 순간에 작동한다.
3. **결제와 동의는 다른 관문이다.** 이걸 섞으면 매출이 아니라 위반이 된다.

### 자산
- `고도화/기능별/GD-8_그림일기서버이전/왜_서버여야_하는가.md` §유료 플랜과의 관계
- `고도화/기능별/B1_개인정보처리방침/B1_처리방침_초안.md` 부록 A #21-1

---

## 2026-08-07 (세션 5 · 00:45–04:59, 4.2h) — 🚨 아동 안전 2건 + 🧰 하네스 재구성

> 커밋 6건 전부 푸시 완료. **이 세션의 두 건은 모두 오너가 찾았다** — 팀장이 만든 테스트는 두 번 다 전부 통과 상태였다.

### 결정 20 — 위기 발화는 **되읽지 않는다** (순서 교체가 아니라 단계 생략)

- **결정:** 아이가 마이크로 말한 내용을 되읽어 확인하는 단계(`confirm`)를, **위기로 판정되면 통째로 건너뛴다.** 즉시 확정 경로로 보내 고정 응답·부모 신호가 나가게 한다.
- **왜:** 되읽기는 두 가지를 **동시에** 한다 — ① 아이 말을 원문 그대로 CLOVA(외부)로 합성 전송 ② 화면 말풍선에 그대로 표시. 사전조사 브리프는 *"순서만 바꾸면 해결"* 이라고 적었지만, **순서만 바꾸면 ②가 남는다.**
- **배운 것:** ⭐ **"확인"의 이득과 "되묻기"의 해를 저울에 올려야 했다.** 되읽기의 목적은 인식 정확도 확인인데, 위기 발화에서는 **"죽고 싶다고 했구나! 맞아?"라고 키디가 되묻는 것 자체가 참사**다. 오인식으로 위로를 한 번 더 하는 건 **안전한 실패**지만, 되묻기는 안전한 실패가 아니다.
  - ⚠️ 브리프의 처방(*"순서만 바꾸면"*)을 그대로 따랐으면 절반만 고쳤을 것이다. **브리프도 검증 대상이다.**
- **자산:** `DailyCheckin.jsx`(`acceptSpeech` 분리) · `checkin-speech-crisis.dom.test.jsx`(4건, 대조군 2 포함) · 차단시험 1/1 · 커밋 `58e1217`
- **되돌리려면:** 되읽기 effect 안의 `if (screenText(t)) { acceptSpeech(t); return; }` 한 줄. 되돌릴 성질은 아니다.

### 결정 21 — 🔴 위기 사전에 **'가해 위협' 칸 신설** + 한국어 어미는 **조합으로 전개**

- **결정:** `HIGH_THREAT_PATTERNS`(대상 3 × 동사변화형 13 + 관용 4 = **45개**)를 신설. 반환값은 기존 `high_violence` 로 **통일**. EXCLUDE 는 적용하되 **`'게임'` 만 제외**.
- **왜:** 오너 시범테스트에서 **"아빠가 날 죽인다고 얘기했어"** 가 미탐이었다. 기존 사전은 ①자해(내가 죽고 싶다) ②이미 일어난 폭력(맞았다) **두 칸뿐**이라, **아직 일어나지 않은 위협**이 두 칸 사이로 통째로 빠졌다. 실측 — 살해 위협 **6/6 미탐**, 폭력 예고 4건 중 3건 미탐.
- **배운 것:** ⭐⭐⭐ **테스트 문장을 방어 코드에서 역산하면, 방어가 딛고 선 '판정' 자체는 한 번도 검증되지 않는다.**
  팀장이 쓴 테스트 문장은 `"죽고 싶어"`·`"아빠한테 맞았어"` — 둘 다 **사전에 이미 있는 패턴**이었다. 그래서 4건 전부 통과했고 차단시험도 통과했는데, **사전에 칸이 없다는 가능성은 끝까지 떠오르지 않았다.**
  - 🔴 **같은 자리에서 두 번 뚫렸다.** 1차 수정 후 오너가 **"엄마가 날 때리려고 해서"** 라고 말하자 또 샜다. `"날 때린"`·`"날 때릴"` 은 넣었지만 `"날 때리려고"` 는 `"날때리려"` 라 안 맞았다 — **한국어는 어미가 붙으면 음절 자체가 바뀐다.** `"때리"` 와 `"때린"` 은 컴퓨터에게 다른 글자다. → **손으로 나열하는 방식 자체가 틀렸다.** 조합으로 전개해 빠뜨릴 자리를 없앴다.
  - **왜 새 레벨을 안 만들었나:** 레벨을 늘리면 `fixed_response`·`is_high` 양쪽에 등록해야 하는데, `is_high` 를 빠뜨리면 **탐지는 되는데 부모 신호만 조용히 사라진다.** 처리가 같아야 하는 상황이라 값을 합쳐 그 사고 가능성 자체를 없앴다(소비처 변경 0).
  - **왜 `'게임'` 만 EXCLUDE 에서 뺐나:** 게임은 **허구의 표지가 아니라 싸움의 계기**로 등장한다 — *"게임 때문에 아빠가 날 죽인다고 했어"* 는 학대 상황에서 실제로 나오는 말이다. 반면 좀비·캐릭터·공룡은 허구가 거의 확실하다.
  - **일부러 안 고친 것:** `"때렸어"` 는 주어를 안 봐서 *"축구공을 세게 때렸어"* 도 걸린다. 좁히면 *"형이 때렸어"* 를 놓친다 → **미탐이 오탐보다 훨씬 나쁘므로** 그대로 두고 `KNOWN_FALSE_POSITIVES` 에 못박아 나중에 누가 함부로 좁히지 못하게 했다.
- **자산:** `safety_lexicon.py`·`safetyLexicon.js`(동일) · **`_verify_lexicon.py` 신설**(정탐 34·오탐 25·양쪽 동일성·처리 정합) · `_verify_all.py` 5종→**6종** · **차단시험 5/5** · 커밋 `eca9d68`
- **되돌리려면:** `HIGH_THREAT_PATTERNS` 를 비우면 탐지만 사라진다(구조는 유지). 단 `_verify_lexicon.py` 가 12건으로 막는다.

### 결정 22 — 위기 감지의 **근본 개선은 지금 하지 않는다** (재료만 남긴다)

- **결정:** 오너 지적(*"가짓수가 많고 구멍이 뚫려있는 느낌"*)에 대해, 방식을 바꾸는 대신 **한계와 선택지를 문서로 남기고 착수는 보류**한다. → `고도화/기능별/GD-37`
- **왜:** 지금 방식(문자열 부분일치)은 **뜻을 전혀 모르고 글자만 본다.** 실측한 미탐 — 방임 5/5, 정서학대 4/4, 간접표현 4/4(*"엄마 오는 소리 나면 숨어"*), 구어체 STT(*"때리려구했어"*), 가정폭력 목격. 다만 **없는 칸을 채우는 일은 문장을 상상해서 만들면 안 된다** — 오늘 두 번 그렇게 틀렸다. **아동학대 분류 조사·전문가 자문이 선행**이라 지금 착수할 일이 아니다.
- **배운 것:** ⭐ **땜질이 쌓이면 그게 설계가 된다.** *"게임을 예외에서 빼자"* 는 오늘 옳은 판단이었지만, **왜 그런 예외가 필요한지**(문맥을 못 본다)를 문서로 남기지 않으면 다음 사람은 그 한 줄만 보고 이유를 모른다. 권고는 **하이브리드**(사전이 1차·즉시·공짜, 못 잡은 것만 LLM 2차 약 0.35원) — 사전을 **대체**하지 않고 **덧대는** 이유는 LLM 이 죽어도 1차 방어가 살아야 하기 때문. **아이 안전을 외부 서비스에 인질로 주지 않는다.**
- **자산:** `고도화/기능별/GD-37_위기감지_근본개선.md`(미탐 지도 실측 + 선택지 4개 + 결정 대기 4건) · 커밋 `b2dd1c7`
- **되돌리려면:** 착수 전이므로 되돌릴 것 없음. 결정 대기 4건 중 **"아이 말을 위기 판정 목적으로 한 번 더 LLM 에 보낼 것인가"** 가 처리방침(B1)·「비밀이야」 정책과 직결 — **오너 결정 사항**.

### 결정 23 — 🧰 하네스를 **200줄 미만**으로 재구성 (397 → 153줄)

- **결정:** `CLAUDE.md` 를 148줄(최종 153줄)로 줄이고, 작업별 상세를 `.claude/rules/` 4개로 분리. 사고 기록 상세는 `문서/사고기록_디버깅과장치.md` 로.
- **왜:** 오너 지적(*"하네스가 꽉 찬 느낌이고 기능이 부실해진다"*). 실측하니 3일 만에 158→397줄(2.5배), 매 세션 **17,101 토큰**. 공식 권장은 200줄 미만이고 근거가 명확하다 — *"Longer files consume more context and **reduce adherence**"* (길수록 **지시를 덜 따른다**).
- **배운 것:** ⭐⭐ **분리 기준은 '주제'가 아니라 `/compact` 후 살아남는가'다.**
  루트 `CLAUDE.md` 만 압축 후 재주입된다. `.claude/rules/` 와 중첩 CLAUDE.md 는 **재주입되지 않는다** → 긴 세션에서 압축이 돌면 밖으로 뺀 규칙은 **사라진 채로 작업이 이어진다.** 그래서 안전·게이트·오너 대응 규칙은 하나도 밖으로 빼지 않았다.
  - ⚠️ **`@path` 임포트는 답이 아니다** — 공식 문서 명시: *"imported files still load at launch"*. 보기만 정리되고 토큰은 그대로다. **경로 스코프만이 실제로 컨텍스트를 줄인다.**
  - 🔴 **오너 조건("기능은 더 단단해져야 한다")의 답은 장치화였다.** 공식 문서: 특정 시점에 반드시 실행돼야 하는 지시는 훅으로 쓰라 — *"apply regardless of what Claude decides to do"*. **규칙은 내가 안 지킬 수 있지만 훅은 못 무시한다.** → 배럴 import 차단을 pre-commit 에 신설(기존 17곳은 유산으로 인정하고 **새로 추가되는 줄만** 검사 — 전면 차단하면 그 파일 만질 때마다 커밋이 막혀 `--no-verify` 가 습관이 된다).
  - 🔴 **정리하면서 같은 함정을 또 팠다.** 경로 규칙은 **'기존 파일을 고칠 때'만 걸리고 '새로 만들 때'는 안 걸린다.** 마이크 쓰는 컴포넌트를 새로 만들면 안전 규칙이 안 온다 — **오늘 위기 사전이 뚫린 구조(있는 것만 보이고 없는 칸은 안 보임)와 정확히 같다.** 오너의 확인 질문(*"공통은 남기고 나머지만 각각 맞지?"*)에 답하려고 다시 열어보다 발견했다.
- **자산:** `CLAUDE.md`(153줄/6,205토큰) · `.claude/rules/` 4개(backend·frontend·verify-and-mocks·safety-lexicon) · `문서/사고기록_디버깅과장치.md` · pre-commit 배럴 차단(차단시험 2/2) · 커밋 `0e93267`·`12c4d85`
- **되돌리려면:** rules 4개를 `CLAUDE.md` 에 다시 붙이면 원상복구. **⚠️ 미검증 1건 — 규칙이 실제로 로드되는지는 다음 세션에서 확인해야 한다**(이 세션 도중에 만든 파일이라 안 잡혔다). 안 들어오면 루트로 되돌린다.

### 결정 24 — `.claude` 는 통째로 막지 말고 **개인 파일만** 제외 (기본값을 '공유'로)

- **결정:** `.gitignore` 를 `.claude/` → `.claude/settings.local.json` + `.claude/*.local.*` 로 변경.
- **왜:** 규칙 파일을 `.claude/rules/` 에 만들었는데 **통째로 무시되고 있었다.** 처음엔 화이트리스트(`.claude/*` + `!.claude/rules/`)로 막았으나, 오너 지적(*"빼면 될 일 아닌가"*)으로 방향을 바로잡았다.
  - ⚠️ **원래 설정이 틀린 게 아니었다.** 2026-07-08 `1c6b20c` 커밋 메시지에 *".claude/(로컬 설정) gitignore 추가"* 라고 명시돼 있고, 그 커밋 자체가 **'컴퓨터 교체 대비 백업' — 공유할 것과 머신별인 것을 나누던 작업**이었다. 당시엔 안에 로컬 설정밖에 없었으니 정확한 분류였다. 오늘 전제가 바뀐 것뿐이다.
- **배운 것:** ⭐⭐ **'전부 막고 아는 것만 뚫는' 화이트리스트는 새로 만드는 것이 기본적으로 사라지는 구조다.** `agents/`·`commands/` 를 만드는 순간에 gitignore 를 떠올릴 이유가 없다 — 만들고, 확인하고, 커밋하고, **며칠 뒤 다른 기기에서 없어진 걸 발견**한다. 그때마다 기억해야 하는 건 **'규칙'이지 '장치'가 아니다**(같은 날 하네스에 쓴 원칙과 정면 충돌했다).
  - 오늘 rules 가 안 커밋된 걸 알아챈 것도 `git status` 에 안 보여서였다 — **운에 가까웠다. 운 좋게 걸렸다는 건 다음엔 안 걸린다는 뜻이다.**
  - **개인 것의 표시는 파일명의 `.local.`** — 규약이 이미 있는데 안 쓰고 있었다.
- **자산:** `.gitignore` · 커밋 `564ef86` · 검증(rules·agents·commands·settings.json → 추적 / `*.local.*` → 무시)
- **되돌리려면:** 한 줄. 단 되돌리면 규칙 공유가 다시 끊긴다.

### 결정 25 — 아이디어 서랍 4·5·6 을 **하나의 원칙 문서(GD-38)로 승격**

- **결정:** 세션 끝에 던진 아이디어 3개를 개별 기능으로 두지 않고 **「완벽하지 않은 키디」** 라는 하나의 축으로 묶어 `고도화/기능별/GD-38` 로 승격. 고도화 README 3층에 등재.
- **왜:** 오너가 서랍을 보고 **"이건 여러 기능에 다 적용할 수 있는 그런 기능 같은데? 서랍에 있기엔 아까워"** 라고 짚었다. 묶어보니 셋 다 **"키디를 완벽한 도구가 아니라 불완전한 동행자로 두는" 설계**였다 — 정체성 문서의 *정체성(B)=관계 / 해자(A)=검수* 와 같은 축이다. **완벽한 검수 도구는 A 를 키우지만, 불완전한 친구는 B 를 만든다.**
  - A 실패는 키디 탓이다 / B "모른다"에서 멈추지 않는다 / C 관계는 양방향이다
- **배운 것:** ⭐⭐ **아이디어를 승격시킬 때는 "이미 어딘가에 있지 않나"를 먼저 실측해야 한다. 좋은 판단은 보통 한 번은 이미 내려져 있다 — 안 퍼졌을 뿐이다.**
  - **원칙 A 는 오너가 이미 세웠다.** `diaryCopy.js:122` 주석 — *"오너 개정(7/8): 못 들은 걸 먼저 인정(**아이 탓 아님**)"*. 그런데 `KidHome`·`KiddyRoom` 의 `LINE_NOSPEECH` 는 여전히 *"아무 소리도 안 들렸어요"*(= **아이가 말을 안 한 것**으로 읽힌다). **한 곳의 옳은 판단이 안 퍼진 것**이다.
  - 🔴 **원칙 B 의 근거는 내가 틀리게 썼다.** 서랍에 *"정작 키디에는 그 규칙이 없다"* 라고 했는데, 실측하니 **LLM 5경로 전부에 이미 있었다**(`chat.py:74` 등). **`chat.py` 한 곳만 보고 단정했다.**
    → 그래도 파보니 **진짜 빈 곳은 따로 있었다**: 지금 지침은 전부 *"지어내지 마라"* 는 **금지**고 **그 다음 행동이 없다.** 아이 입장에선 *"키디도 모르겠어"* 로 끝나는 **막다른 길**이다. 우리에겐 이미 안전한 검색이 있다 → *"같이 찾아볼까?"*.
  - 🔴 **오늘 하루에 "있는 것을 없다고 함"을 세 번 했다** — ①그림일기 한도(프론트에 이미 있었음) ②위기 사전(칸이 없다고 본 게 아니라 **있는 칸만 봤다**) ③원칙 B. **패턴이 명확하니 하네스가 아니라 습관으로 잡아야 한다.**
- **자산:** `고도화/기능별/GD-38_완벽하지않은키디.md`(실측 기반 적용 지점 + 착수 순서 + 윤리선) · 서랍 4·5·6 ⭐ 승격 표기 · README 3층 등재 · 커밋 `cd538e3`·`8754831`
- **되돌리려면:** 착수 전이라 되돌릴 것 없음. **§1(문구 2곳)은 논쟁 여지가 없어 바로 착수 가능** — 오너가 이미 내린 판단을 맞추는 일이다.

### 📌 이 세션의 메타 관찰 — **세 번 다 오너가 찾았다**

| | 팀장 상태 | 오너가 한 것 |
|---|---|---|
| 위기 사전 | 테스트 4건 + 차단시험 + 전체 검증 **전부 통과** | 마이크에 대고 **자기 말로** 말했다 → 미탐 (두 번) |
| 하네스 | 재구성 완료, 커밋까지 | *"빼면 될 일 아닌가"* → 방향이 거꾸로였음 |
| 아이디어 | 세션 끝났다고 판단, 마무리 인사까지 | *"잊은 거 하나 더 없누?"* → **서랍 습관을 건너뛰고 있었다** |

⭐ **팀장이 만든 검증은 팀장이 상상한 범위를 못 넘는다.** 오너의 즉흥적인 한 문장이 그 범위 밖을 친다.
→ 하네스 규칙으로 등재: *"테스트 문장을 코드에서 역산하지 않는다"* · *"오너의 확인 질문도 검증 트리거"*
→ ⚠️ **마무리 체크리스트가 없어서 서랍을 빠뜨렸다.** 세션 종료 시 **DECISIONS·WORKLOG·미해결질문·아이디어 서랍 4종**을 훑는 습관이 필요하다.

---

## 2026-08-06 (세션 4, 약 1h) — 💸 비용 상한 (S2·S4)

> 커밋 0건(오너 시범테스트 대기). 어제가 *"아무나 못 들어오게"* 였다면, 오늘은 *"들어온 사람이 무한정 못 쓰게"*.

### 결정 13 — 비용 상한을 **그림일기부터**, 균일하지 않게

- **결정:** 유료 경로 12곳에 균일하게 걸지 않고, **비용 자릿수가 다른 그림일기 2개**에 먼저 건다. 프로필당 **분당3 · 하루 자체5/이어3**.
- **왜:** 회원가입은 누구나 된다 → 인증만으로는 **계정 1개의 무한 호출**을 못 막는다. 그중 `gpt-image-1`(+`input_fidelity:high`)이 텍스트 LLM 과 **자릿수가 다르다.** 균일하게 조이면 아이 경험만 나빠지고 얻는 게 없다.
- **배운 것:** ⭐ **방어의 대부분은 '분당'이 한다.** 하루 한도는 *얼마나 많이* 새는지를 정하고, 분당은 *얼마나 빨리* 새는지를 막는다. **자동 스크립트는 분당에서 죽으므로 하루 한도까지 가지도 못한다.** 그래서 하루 카운터가 배포 때 리셋되는 것도 실질 위험이 아니다.
- **🔴 왜 서버(5/3)가 프론트(3/1)보다 넉넉한가:** 프론트는 이어그리기를 **'간직할 때만'** 깎는다(아이 배려, 팀장 확정 ①). 하지만 **돈은 호출하는 순간 나간다.** 서버까지 간직 기준을 따르면 *"마음에 안 든다"* 를 반복하는 것만으로 무한 호출이 된다 → 서버는 **호출 기준**으로 깎고 차이를 넉넉한 한도로 흡수.
- **자산:** `server/quota.py` · `_verify_quota.py`(29개, 대조군 포함) · **차단시험 4/4**
- **되돌리려면:** `quota.LIMITS` 숫자만 고치면 된다. 한도 자체를 끄려면 라우터의 `check_and_consume` 두 줄.

### 결정 14 — DB 영속화(D안)는 **GD-8과 함께**, 지금은 메모리

- **결정:** `quota_usage` 테이블(범용 `kind` 칼럼 설계)을 만들지 **않는다.** 메모리로 가고 GD-8에서 함께 설계한다.
- **왜:** 오너가 C안(기존 `usage` 확장)을 물었고, 파보니 재활용이 오히려 독이었다 — `deep_count`는 **계정당**, 그림 카운터는 **프로필당**이라 한 테이블에 섞으면 *"이 행이 어느 쪽인지"* 를 매번 신경 써야 하고 **한 번 헷갈리면 한도가 프로필 수만큼 곱해진다**(조용히 틀리는 종류). 그래서 D안(새 범용 테이블)을 제안했다가, **단점을 펴놓고 보니 지금 할 일이 아니었다.**
- **배운 것:** ⭐⭐ **내가 제안한 안의 반대편을 파보는 게 결정을 바꿨다.**
  - D안이 늘리는 것: **고장 날 부품**(DB가 죽으면 그림도 죽는다 → 막을까/통과시킬까 딜레마 = 어제의 '조용히 새는' 함정과 같은 모양), **배포 순서 의존**, **행 무한 증가**(정리 장치를 "나중에" 만들게 된다 = S3a 패턴).
  - 🔴 결정적인 것: `quota_usage` 한 줄의 의미는 카운터가 아니라 **"이 아이가 8월 6일에 그림을 3장 그렸다"는 행동 기록**이다. 처리방침 개정 대상이고, **아이가 일기를 지워도 그 기록은 남는다** → GD-8b「삭제와 비밀보장」과 정면으로 얽힌다.
  - → **지금 따로 만들면 GD-8 때 두 번 일한다.**
- **자산:** `server/quota.py` 머리말에 사유 + `_read_day`/`_bump_day` 2함수로 **갈아끼울 자리 격리** · 미해결질문 19
- **되돌리려면:** D안 설계는 미해결질문 19에 보존. GD-8 착수 시 함께 판단.

### 결정 15 — 🔴 `except Exception` 이 `HTTPException` 을 삼킨다 (하네스 규칙 신설)

- **결정:** 인증·인가·한도 검사는 **`try` 바깥**에 둔다. 부득이하면 `except HTTPException: raise` 를 일반 `except` 보다 먼저.
- **왜:** `diary_image.py` 는 모든 실패를 `{ok: False}` 로 감싸도록 설계돼 있다(500 금지). 그런데 **`HTTPException` 도 `Exception` 의 하위**라, 한도 검사를 `try` 안에 두면 **429가 `{ok:false}` 로 둔갑한다.**
- **배운 것:** ⭐⭐ **에러도 안 나고 화면도 멀쩡하다.** 카나리아가 지키는 것과 **같은 종류의 사고**다 — 아무도 신고하지 않는다. 차단시험 ④에서 실제로 재현됐고(응답이 `{'ok': False}`), 검사가 16건으로 잡았다. **상태코드만 보지 말고 응답 본문에 `ok` 키가 섞였는지까지** 봐야 한다.
- **자산:** `CLAUDE.md`「FastAPI 주의사항」규칙 + `_verify_quota.py` 의 *"429가 {ok:false}로 둔갑하지 않는다"* 검사
- **되돌리려면:** 되돌릴 성질이 아니다.

### 결정 16 — 테스트 무더기 타임아웃은 `--no-file-parallelism` 으로 가른다

- **결정:** 프론트 테스트가 전부 타임아웃이면 순차 실행으로 먼저 갈라본다. (하네스 디버깅 절에 추가)
- **왜:** 31개 실패 → `git stash` 로 내 변경을 치우니 **36개로 더 늘었다.** 코드가 아님이 즉시 판명. 순차 실행하니 **214개 전부 통과**(import 183초 → 7.6초). 병렬 워커가 CPU 를 두고 굶은 것 — `kernel_task` 40%(발열 쓰로틀링) 상태였다.
- **배운 것:** ⭐ **`git stash` 가 이틀 연속 결정적이었다.** 어제는 이 규칙을 늦게 써서 두 번 헛짚었고, 오늘은 **먼저 썼더니 5분 만에 끝났다.** *"가장 확실한 검증을 가장 늦게 하지 말 것."*
  - ⚠️ 어제 *"게임 부하 때문"* 은 **틀린 진단**이었다(게임 켠 채로 199개 통과). 오늘도 게임이 돌고 있었지만 **그것만으로 단정하지 않고** stash + 순차실행이라는 **판별 수단**으로 갈랐다. 같은 증상, 다른 결론 — **증상이 아니라 판별법이 답을 준다.**
- **자산:** `CLAUDE.md` 디버깅 절 3번 · WORKLOG 30초 요약

### 결정 17 — 정밀검수 한도 초과 시 **아이 화면에는 결제 화면을 띄우지 않는다**

- **결정:** 429 → `PaywallModal` 은 **부모 화면(`parentView`)에서만.** 아이 화면은 조용히 Tier 0~1 로 지나간다. 한도(비용 방어) 자체는 그대로 유지.
- **왜:** 공모전 모드를 끄자 paywall 이 **되살아났다** — 그런데 그 화면은 **아이가 영상 카드를 클릭했을 때** 뜬다(`VideoModal` 은 모달이 열리면 자동으로 Tier2 를 부른다).
  - 🔴 **결제 경로가 없다.** `subscriptions` 를 채우는 건 관리자 페이지뿐(`admin_users.py:147`) → 아이가 *"월 4,900원"* 을 보고 눌러도 **갈 곳이 없는 막다른 길**.
  - **안전이 사라지진 않는다.** 검수는 2층이라 키워드 채점은 계속 동작 — 정밀도만 낮아진다.
  - 🔴 아동 안전 기능에서 *"돈 내면 더 안전"* 을 **아이에게** 보이는 구조 자체가 위험하다(KT 상용화 관점에서도).
- **배운 것:** ⭐ **한 기능을 켜면 그 뒤에 잠들어 있던 다른 기능도 같이 깨어난다.** 공모전 모드 해제는 "한도를 되살린다"였는데, 실제로는 **paywall 까지** 되살렸다. 오너가 *"이게 아이들 시청기록 기반이냐"* 고 물어 확인하다 발견했다 — **질문이 코드를 다시 보게 만들었고, 거기서 나왔다.**
  - ⚠️ 내가 오늘 이걸 미리 짚지 못했다. 커밋 전 「검증 축 1(기존 동작 보존)」을 볼 때 *"한도가 켜지면 화면에서 무슨 일이 일어나나"* 까지 따라갔어야 했다. **서버 응답만 보고 화면을 안 봤다.**
- **자산:** `client/src/components/VideoModal.jsx` · **테스트 신설** `videomodal-paywall.dom.test.jsx`(4개) · 차단시험 2/2
- **되돌리려면:** `if (parentView)` 가드 제거. 단 **테스트가 즉시 막는다**(차단시험으로 확인).
- **⏭️ 남은 판단:** 횟수 자체(무료 하루 3회)는 재검토 대상 — 오너 *"영상 검수는 기본 기능으로 가고 싶다"* → **미해결질문 21** 에 판단 재료 정리(⚠️ **1회당 실제 비용 측정이 선행**).

### 결정 18 — 정밀검수 한도 3 → 20회 (측정으로 정하고, **없애지는 않음**)

- **결정:** 무료 하루 **20회**. 분당 3회 신설. 화면 문구는 서버가 준 실제 한도를 그대로 표시.
- **왜:** 1회 **16.4원** 실측 + 분석캐시 적중률 **97.2%** → 정상 사용자에겐 사실상 무료. 3회는 근거 없이 빡빡했다.
- **왜 없애지 않았나:** 캐시 97%는 **정상 사용자** 값이다. 비용을 태우려는 쪽은 매번 새 `video_id` 를 던져 **캐시를 일부러 피한다**(0%). 그때 총액을 막는 건 하루 한도뿐 — 50회는 계정당 월 25,500원(프리미엄가 5배) 노출이라 20으로.
- **배운 것:** ⭐ **"평소엔 싸다"와 "최악에도 안전하다"는 다른 질문이다.** 평균값(97%)으로 최악을 재면 안 된다.
- **자산:** `quota.LIMITS["analyze_deep"]` 단일 정의(analyze.py 가 읽어 씀) · 측정 결과는 미해결질문 21에 보존
- **⏭️ 남은 것:** 프롬프트 캐싱 미적용 — 켜면 1회 16.4 → **2.5원**(시스템 프롬프트 7,139토큰이 비용의 대부분). 계정 생성 제한(질문 22).

### 결정 19 — 🔴 팀장이 틀린 이유를 구조로 막는다 (오너 요청)

- **무슨 일:** 팀장이 *"하루 한도는 풀고 분당만 걸자"* 고 제안. 숫자를 넣어보니 분당 3회만으로 **하루 4,320회 · 계정 1개 월 200만원**이 통과. 같은 날 오전 `quota.py` 에 *"분당=얼마나 빨리 / 하루=얼마나 많이"* 라고 **직접 써놓고** 어겼다.
- **원인 3층:**
  1. 표면 — 계산을 안 했다 (`3 × 60 × 24`, 2초면 됐다)
  2. 중간 — 내가 만든 원칙을 내가 완화했다
  3. 🔴 **뿌리 — 결론이 먼저 있었다.** 오너의 희망(*"검수는 기본 기능"*)과 유리한 측정치(캐시 97%)가 겹치자 **'한도를 풀자'로 먼저 기울고 뒤에 근거를 찾았다.** 계산을 빠뜨린 게 아니라 **계산할 생각이 안 들었다** — 이미 답을 안다고 느꼈으니까.
- **배운 것:** ⭐⭐ **가장 위험한 순간은 오너가 바라는 답을 찾아드리려 할 때다.** 이런 실수는 다짐으로 안 막힌다 — 구조가 같으면 또 난다.
- **장치(다짐 아님):**
  | # | 무엇 | 막는 것 |
  |---|---|---|
  | 1 | `quota._validate_limits()` — `per_day`/`cost_krw` 없으면 **RuntimeError** | *"분당만 걸자"* 가 **코드에서 불가능** |
  | 2 | `_verify_quota` 가 매 실행마다 **하루 최대 노출액**을 원화로 출력 | 숫자를 안 보고 지나갈 수 없음 |
  | 3 | `CLAUDE.md` — **"오너 희망과 같은 방향의 결론일수록 더 의심"** + 신호 3개 | 사고 습관 |
- **즉시 효과:** 2번이 켜지자마자 *"analyze_deep 하루 50회 = 월 25,500원"* 이 눈에 들어왔고 → 오너가 20으로 확정. **장치가 만든 날 바로 일했다.**
- **자산:** `server/quota.py` `_validate_limits()`/`worst_daily_krw()` · `_verify_quota.py` [0]단계(차단시험 3/3) · `CLAUDE.md`

### 이번 세션에서 **틀렸던 판단** (정정 기록)

| 내가 말한 것 | 실제 | 어떻게 잡혔나 |
|---|---|---|
| 🔴 *"그림일기 한도는 정한 적이 없습니다"* | **프론트에 이미 있었다** — `diaryStore.js:174` `REGEN_MAX=2` / `:195` `CONTINUE_MAX=1` | **오너가 기억하고 있었다** (*"이어그리기는 한번, 자체생성은 3번으로?"*) → 서버만 보고 단정한 것. **규칙이 어느 층에 사는지부터 확인할 것 — 없는 게 아니라 다른 층에 있을 수 있다.** |
| *"2~3시간 걸립니다"* | 실제 1시간. 코드는 30분 안쪽이고 나머지는 검증 | 오너가 *"왜 2~3시간까지 걸리는지 궁금해"* 라고 물어 분해했더니 **숫자·화면·검증이 이미 다 있었다** |
| *"훅이 3.2초 → 19.5초가 됐습니다"* | **콜드 스타트 값**이었다. 실제 2.77 → **3.39초** | 같은 명령을 두 번 재서 확인. **첫 실행값을 회귀로 보고할 뻔했다** |
| 🔴 *"하루 한도는 풀고 분당만 걸면 된다"* | 분당 3회만으로 **하루 4,320회(월 200만원)** 통과 | 오너가 *"이 말이 이해가 안 간다"* → 설명하려 계산하다 발견. **결정 19** |
| *"캐시 적중률 40.8%"* | 실제 **97.2%** — Supabase 가 기본 1,000건만 반환해 **캐시가 잘린 값**이었다 | 행 수를 `count=exact` 로 다시 셈(1,968편). **잘린 데이터로 정반대 결론을 낼 뻔했다** |

> 다섯 건의 공통점: **확인할 수 있는 걸 확인하지 않고 말했다.**
> 잡아준 경로는 셋 — 오너의 지적 2건, 다시 재보기 2건, **오너가 "이해가 안 된다"고 물어봐준 것 1건.**
> 마지막이 특히 값졌다: 틀렸다고 지적한 게 아니라 **설명을 요구했고, 설명하려니 계산해야 했고, 계산하니 드러났다.**

---

## 2026-08-05 (세션 3: 18:07 → 08-06 01:02, +1일, 6.9h) — 🔐 보안 축 + 재발방지 장치(포카요케)

> 커밋 13건. 앞부분(결정 1~7)은 보안 강화, 뒷부분(결정 8~12)은 **"같은 실수를 다시 못 하게 만들기"**.

### 결정 8 — 프로덕션 API 문서 비공개 (S3a)

- **결정:** `/docs`·`/redoc`·`/openapi.json` 을 배포에서 닫는다. **기본값을 '닫힘'으로**(`APP_ENV` 미설정 = production).
- **왜:** 문 82개를 잠갔는데 *"어디가 안 잠겼는지 알려주는 안내판"* 이 걸려 있었다(엔드포인트 83개 목록 + 자물쇠 유무 + "Try it out" 버튼).
- **배운 것:** ⭐ **기본값을 어느 쪽에 두느냐가 설계의 핵심이다.** Railway 엔 `.env` 가 없으므로 **기본값이 production 이면 아무 설정 없이 자동으로 닫힌다** → 오너 작업 0. 반대로 기본값이 '열림'이었다면 배포마다 사람이 챙겨야 했다.
- **자산:** `server/main.py` · `_verify_canary`/`verify_s3_docs.py`(21/0) · 커밋 `4932550`
- **되돌리려면:** `server/.env` 에 `APP_ENV=development`. 단 배포에 넣으면 다시 공개된다.
- ⚠️ **부작용을 알고도 안 고쳤다가 걸렸다:** `/openapi.json` 을 닫자 배포 대조형 검증 스크립트 3개가 죽었다. 커밋 메시지에 *"인지 사항"* 으로 적어놓고 **실제로 고치지는 않았다.** → *"알고는 있었는데 안 했다"* 는 그 자체가 사고 패턴이다. `fetch_prod_openapi()` 로 수정.

### 결정 9 — 🔴 가짜(mock) 주입 3원칙 + 공용 도구 (하루에 3번 반복된 사고)

- **결정:** 규칙만 넣지 않고 **도구까지** 만든다(오너 선택 C안). `server/_verify_fakes.py` 신설. 단 **이미 통과한 스크립트 4개는 건드리지 않는다.**
- **왜:** 하루에 세 번 같은 실수를 했다. 처음엔 "두 번"으로 보고했으나 **확인 결과 세 번**이었고 **세 번째가 가장 위험했다.**

  | # | 무엇을 | 방향 | 결과 |
  |---|---|---|---|
  | 1 | `write_audit` 을 **통째로** 교체(내부에서 예외를 삼키도록 설계된 함수) | 거짓 실패 | 없는 버그(500) |
  | 2 | `load_dotenv` 를 안 막음(로컬 `.env` 가 덮어씀) | 거짓 실패 | 배포 상황 재현 실패, 7건 오탐 |
  | 3 | 가짜를 **정의만 하고 안 끼움** | 🔴 **거짓 통과** | 진짜 YouTube 호출, **검증 안 된 채 통과** + 쿼터 300유닛 |

- **배운 것:** ⭐⭐
  - **공통 뿌리는 "가짜가 실제 실행 경로에 제대로 꽂혔는지 확인하지 않았다"** 이다. 1번은 가짜와 고장 지점 사이에 **방어막 층**, 2번은 **덮어쓰는 층**, 3번은 **아무 연결도 없었다.**
  - **🔴 "거짓 통과"가 "거짓 실패"보다 위험하다.** 실패는 시끄러워서 다시 보지만, **통과는 아무도 다시 안 본다.**
  - **파이썬은 오타 난 이름(`search_youtub`)에도 새 속성을 조용히 만들어준다.** 그게 3번을 가능하게 한 언어 특성이다 → `patch()` 가 즉시 `RuntimeError` 를 내게 만들었다.
  - **그 테스트는 호출 기록을 검사하긴 했다** — `anthropic`·`db_insert` 는 확인하고 **`youtube` 만 빠뜨렸다. 한 줄이면 잡혔다.**
- **자산:** `server/_verify_fakes.py` · **재현 검증** `verify_fakes_catches.py`(6/0 — "그 도구가 그 사고를 실제로 잡는가") · `CLAUDE.md` 3원칙 · 커밋 `8b79cdc`
- **되돌리려면:** 되돌릴 성질이 아니다. 경계가 늘면 도구에 추가.

### 결정 10 — pre-commit 훅 + `postinstall` 자동 활성화 (규칙 → 장치)

- **결정:** 커밋할 때 **자동으로** 검사하고, 깨지면 **커밋 자체를 막는다.** 훅 설치는 `npm install` 에 얹어 **자동화**한다.
- **왜:** 방지책 3층 중 **3층(강제)이 거의 비어 있었다.** 실측 — CI 없음 / git hook 없음 / `npm test` 없음 / Vercel 빌드 시 테스트 없음. **검사를 돌리는 건 오직 "팀장이 기억해서"였다.**
- **배운 것:** ⭐
  - **오늘 사고들이 어떻게 잡혔나를 보면 답이 나온다:** 성능 회귀 2건은 **테스트가 있어서** 잡혔고, 테스트가 없는 곳(가짜 주입 3건·SPA 404·하네스 불일치)은 **아무도 못 잡았다.**
  - **포카요케는 다섯 개를 다 맞춰야 작동한다** — 거짓 양성 0 / 빠를 것 / 안전한 기본값 / 우회로는 열되 경고 / 막혔을 때 고치는 법 제시. **하나라도 틀리면 사람들이 꺼버린다.**
  - 🔴 **오너 지적으로 마지막 구멍을 찾았다** — *"하네스에 적어둔 거면 다른 컴퓨터에서는 읽으면 알아서 하는 거지?"* **아니다.** 훅은 `.git/hooks` 에 살아 **git 으로 전파되지 않고**, 없어도 **아무 경고 없이** 커밋된다. CLAUDE.md 에 "설치하세요"라고 적는 건 **또 '규칙'이었다.** → `postinstall` 로 해결.
- **자산:** `scripts/hooks/pre-commit` · `scripts/setup-hooks.mjs` · `client/package.json`(`test`·`postinstall`) · 커밋 `3cec454`·`7daf3a3`
- **되돌리려면:** `git config --unset core.hooksPath`. 단 그 순간 3층이 다시 빈다.

### 결정 11 — 인가(소유권) 검사 + 🐤 윤리선 카나리아 (남은 구멍 A·B·C)

- **결정:** 훅에 **인가 정적 검사**와 **윤리선 카나리아**를 추가한다. **pytest 는 도입하지 않는다.**
- **왜 — 팀장이 한 말 셋 중 둘이 부정확했다(오너 질문으로 확인):**

  | 팀장이 한 말 | 실제 |
  |---|---|
  | "백엔드 테스트 0개" | 형식상 맞지만 **검증 스크립트 4개가 이미 TestClient 로 진짜 요청을 보내는 테스트**였다 |
  | "윤리선 자동검사 불가" | ❌ **틀렸다.** 카나리아로 가능하고, **GiftGate 브리프에 "카나리아 CI"로 이미 계획돼 있었다** |
  | "안 던진 질문은 사람 몫" | 맞다 |

- **🔴 그리고 새 구멍을 발견했다:** 훅이 **인증만** 보고 **인가는 0건** 검사였다. `Depends` 만 있으면 통과 → **아무 회원이나 남의 아이 데이터**를 볼 수 있는 라우트가 통과한다. *(자물쇠는 달렸는데 남의 집 문이 열린다.)*
- **배운 것:** ⭐⭐
  - **🐤 윤리선은 터져도 조용하다.** 에러가 안 나고 화면은 예쁘게 나오고 아무도 신고하지 않는다 → **조용히 배포되고 조용히 신뢰를 깎는다.** 이 앱에서 신뢰는 기능이 아니라 **존재 이유**다. 그래서 **사람이 아니라 기계가 지켜야 한다.**
  - **카나리아엔 대조군이 필수다.** 공유 답변도 함께 넣어 "파이프라인이 살아있는지" 본다. **대조군마저 안 나오면 유출이 없는 게 아니라 검사가 아무것도 안 한 것**이다.
  - **첫 실행이 '가짜 통과'였다** — `/reports/checkins` 가 **502**였다(가짜 응답에 `type="text"` 가 없어 파싱 실패). **엔드포인트가 실행조차 안 됐으니 유출 검사가 무의미했다.** 자기검사("검사가 실제로 동작했는가")가 이걸 잡았다 → **결정 9의 교훈을 같은 날 바로 써먹었다.**
  - **pytest 를 안 넣은 이유:** `requirements.txt` 에 넣으면 **프로덕션 배포에도 설치**되고, 잘 도는 스크립트 4개를 옮기는 건 **오늘 세 번 당한 패턴**이다. 목적("백엔드에도 자동 검사")은 **훅이 돌리는 것**으로 달성된다.
- **자산:** `server/_verify_authz.py`(라우트 81 / 프로필 31 / 누락 0 · 거짓 양성 0) · `server/_verify_canary.py` · `server/_verify_all.py` · 커밋 `b6a9f5d`
- **실증:** `reports.py:397` 의 `share_with_parent` 필터 **한 줄**을 지우자 유출 표식 2개를 지목하며 **즉시 차단**.
- **되돌리려면:** 훅에서 `_verify_all.py` 호출을 빼면 되지만, 그 순간 윤리선을 지키는 게 다시 사람이 된다.

### 결정 12 — "다시 한번 검증해봐" (기계가 못 하는 자리를 오너 한 문장으로)

- **결정:** 팀장이 *"검증 완료"* 류의 말을 하면 오너가 **"다시 한번 검증해봐"** 라고 한다. 팀장은 **새 각도**를 찾는다.
- **왜:** 팀장이 "물어볼 질문 5개"를 제안했으나 오너 판단 — ***"외우는 건 좀 힘들 것 같다."*** 트리거가 명확하고 외울 게 없는 한 문장이 낫다.
- **배운 것:** ⭐
  - **효과는 같은 날 두 번 입증됐다.** 1차는 "토큰 없으면 막히나"만 봤고(절반), 2차에서 **"회원은? 관리자는?"**, 3차에서 **"파라미터 오염? 응답 형태? 삭제된 줄?"** — 매번 새 축이 나왔다.
  - **팀장이 잘못 대응할 위험**을 규칙으로 못박았다: 같은 검사 반복 금지 / **"이미 다 봤습니다" 금지**(오너가 그 말을 한 순간 아직 안 본 게 있다고 가정) / 정말 없으면 정직하게 / 트리거는 "끝났다·완료·문제없다·전부 통과" **전부**.
  - ⚠️ **오늘 오너가 잡아준 것이 여섯 번이다** — CPU 단정, ⭐ 표/문장 모순, "반복되면 최선의 방법을", "하네스에 적으면 자동이냐", "다 완성한 건가", **"포카요케가 표에 없는데?"**. 전부 **코드를 몰라도 되는 질문**이었고 전부 맞았다.
- **자산:** `CLAUDE.md` 「오너의 "다시 한번 검증해봐"」 · 커밋 `2c965a4`

### 난이도 자평 (오너 요청, S~E)

| | 등급 | 근거 |
|---|---|---|
| 코드(타이핑) | **C** | 서버 기능 코드는 **시그니처 12줄**이 전부 |
| **판단(뭘 할지 정하기)** | **A** | 83개 권한 판정 · 포카요케 5조건 설계 · 가짜 주입 경계 판단 |
| S급 | **없음** | 무중단 마이그레이션·분산 트랜잭션·성능 최적화 같은 건 없었다 |

> 서버 840줄 중 **700줄이 검증 도구**였다. 오늘의 결과물은 *"보안을 고친 것"* 이 아니라 ***"같은 실수를 다시 못 하게 만든 것"*** 이다.
> ⚠️ 참고: 앞으로 올 것 중 **GD-8(그림일기 서버이전)이 A~S** 로 오늘보다 어렵다. 오늘은 "있는 것에 자물쇠 달기"였고, GD-8 은 **"있는 데이터를 안 잃고 옮기기"** 다.

---

## 2026-08-05 (세션 3 전반: 18:07–22:44) — 🔐 보안 축 신설 + 검증 고정 축 4개

> 같은 날 세션 1(08:54–09:25, 0.5h)은 아이디어 서랍 3건, 세션 2(18:07 시작 직후)는 CLOVA TTS 중단 — 아래 별도 기록.

### 결정 1 — 보안을 7번째 축으로 승격 (기존 4축 + TV + 수익 → +보안)

- **결정:** `뚫린 걸 발견 → 막는다` 방식을 폐기하고 **① 한 번 전수 청소 ② 그 뒤론 브리프 양식으로 예방**으로 전환. `S0~S7` 신설.
- **왜:** 오너 판단 — *"우린 이제 포트폴리오용 작업이 아닌 KT 안에서 상용화 할 수도 있는 기능이라는 걸 생각해야 해."*
  A1·A2·A3는 **사전조사 중 우연히 걸린 것**이지 찾아서 넣은 게 아니었다. 그 방식은 **찾은 만큼만 안전**하다.
- **배운 것:** ⭐ **"기본이 열림"과 "기본이 잠김"의 차이.** FastAPI 는 `Depends` 를 안 쓰면 그냥 공개다 — 즉 **잊으면 뚫린다.** 개별 구멍을 막는 것으로는 끝나지 않고, **기본값을 바꾸는 규칙**(브리프 「권한 등급」 필수 칸)이 있어야 닫힌다.
- **자산:** `고도화/기능별/GD-S_보안축/GD-S0_전수판정표.md`(정본) · `고도화/README.md` 보안 축 표 · 커밋 `8692cc0`
- **되돌리려면:** 판정표 §2 표를 보면 13개 각각의 등급 판정 근거와 "깨지는 화면 0" 실측이 있다.

### 결정 2 — 미인증 13개 전부 잠금 (`GET /` 하나만 공개 유지)

- **결정:** 관리자 3개(`feedback/pipeline`·`analyze/cache`·`test-env`) / 회원 9개 / **공개 1개**(`GET /`). 최종 🔑16 · 👤66 · 🌐1.
- **왜:** 프론트 호출부 전수 grep 결과 **잠가도 깨지는 화면이 0개**였다 — 13개를 쓰는 화면이 전부 `ProtectedRoute` 뒤이고, 비보호 페이지(Landing·Login)는 **서버를 아예 안 부른다.**
- **배운 것:**
  - **`GET /` 를 잠그면 배포가 깨진다** — Railway 헬스체크가 이 응답으로 서버 생존을 판단한다. **"열어두는 것도 결정"이므로 사유를 문서에 남긴다.**
  - **인증(누구세요)과 인가(당신 것 맞나)는 다르다.** 인가 구멍이 더 위험한데, 다행히 **0건**이었다 — `get_owned_profile(profile_id, user["user_id"])` 헬퍼가 11개 파일에서 이미 쓰이고 있었다. **이 패턴을 깨지 말 것.**
  - 구멍은 **초기 파일 5개에 몰려 있었다**(회원 시스템이 생기기 전에 만든 것들). 설계 실패가 아니라 **시간 순서 문제**였다.
- **자산:** 커밋 `8692cc0`(1~3단계) · `b2b6677`(4~6단계) · 재현 스크립트 `audit_routes.py`·`verify_s1_*.py`
- **되돌리려면:** 각 라우트의 `Depends(...)` 한 줄씩. 본문 로직은 0줄 변경했다.

### 결정 3 — 관리자 페이지 4중 잠금 (오너 요구)

- **결정:** `ProtectedRoute`(로그인) → `AdminRoute`(role=admin) → `AdminGate`(비밀번호 재확인 **30분**) → 서버 `require_admin`. 실패 시 `/profiles` (3회 시도 허용).
- **왜:** 오너 요구 — *"관리자 권한이 있는 아이디로만 들어갈 수 있어야 하고, 들어가면 다시 재로그인을 해야 하고?"*
  자동 로그인이 브라우저에 남으므로(supabase `persistSession` 기본 true) **오너 기기를 남이 만지면 `/admin` 까지 열린다.**
- **배운 것:**
  - **"완전 로그아웃 후 재로그인"은 답이 아니다.** 로그아웃하면 앱 전체 세션이 끊겨 아이 홈·프로필까지 다시 로그인해야 한다. 업계 표준은 **sudo 모드**(GitHub 설정·AWS 결제) — **로그인은 유지한 채 그 화면에서만 재확인.**
  - **메인 클라이언트로 `signInWithPassword` 를 재호출하면 안 된다** — 새 세션이 발급되고 `onAuthStateChange` 가 발화해 `AuthContext` 가 통째로 다시 돈다. → **검증 전용 클라이언트 분리**(`persistSession:false`·`storageKey` 분리).
  - **판정 재료가 이미 있었다** — `AuthContext` 가 `GET /me/status` 로 `isAdmin` 을 이미 들고 있었다(새 API 0개). **만들기 전에 있는 것부터 찾을 것.**
  - ⚠️ **이 게이트는 "기기를 남이 만졌을 때"를 막는다. 작정한 공격자는 못 막는다**(프론트라 우회 가능). **진짜 방어선은 서버 `require_admin`.**
- **자산:** `client/src/components/AdminRoute.jsx`·`AdminGate.jsx` · 회귀 테스트 13개 · 커밋 `43386e1`·`3b0de51`
- **되돌리려면:** `App.jsx` 의 `/admin` 라우트에서 래퍼를 벗기면 된다. 서버 `require_admin` 은 그대로 남는다.

### 결정 4 — 🔴 SPA 라우팅 404 복구 (보안 아님, 실서비스 버그)

- **결정:** `client/vercel.json` 신설 — 모든 경로를 `index.html` 로 rewrite.
- **왜:** 오너 질문(*"웹에서 관리자 페이지를 어떻게 여나?"*)을 확인하다 발견. 배포본에서 **`/` 외 모든 경로가 404**였다(`x-vercel-error: NOT_FOUND`). `vercel.json`·`_redirects` 둘 다 없었다.
- **배운 것:** ⭐ **피해가 관리자 페이지보다 컸다 — 아이가 영상 보다 새로고침하면 앱이 죽었다.** 링크 공유·즐겨찾기도 불가.
  **홈에서 클릭으로만 다니면 정상이라 여태 안 드러났다.** → **"우리가 쓰는 방식"으로만 확인하면 사용자의 실제 경로를 놓친다.** 새로고침·직접 URL·뒤로가기는 별도로 확인할 것.
  ⚠️ 그리고 이 404 덕분에 `/admin` 이 우연히 막혀 있었다 — **버그가 문을 대신 잠그고 있었다.**
- **자산:** `client/vercel.json` · 커밋 `43386e1` · 배포 후 9개 경로 200 실측
- **되돌리려면:** `vercel.json` 삭제. 단 새로고침 404가 즉시 재발한다.

### 결정 5 — 🧪 검증 고정 축 4개를 하네스에 박음 (이 세션의 가장 큰 수확)

- **결정:** 검증 항목을 매번 즉흥으로 정하지 않는다. **① 기존 동작 보존 ② 무게 ③ 데이터 유출(윤리선) ④ 사실 왜곡** 을 고정 축으로 `CLAUDE.md` 에 등재.
- **왜:** 팀장이 이 세션에서 성능 회귀 2건을 만들었고, 원인을 파다 **이 레포의 과거 사고가 전부 같은 패턴**임을 발견했다.
- **배운 것:** ⭐⭐ **"틀린 답"보다 "안 던진 질문"이 위험하다.**

  | | 틀린 답 | 안 던진 질문 |
  |---|---|---|
  | 증상 | **드러난다** — 안 고쳐지니 결국 다시 본다 | **안 드러난다** — 검증 목록에 없으면 통과 |
  | 이번 | 시간은 버렸지만 잡혔다 | **우연히** 타임아웃이 나서 걸렸다 |

  `efadd94`(동작 보존) · KidHome 게이트 · `videoId` 덮어쓰기 · LLM 왜곡 · 시제 오류 · 이번 `react-icons` — **전부 기능은 동작했고 문법도 맞았다.** 누구도 그 질문을 던지지 않았을 뿐이다.
  → 조심해서 될 문제가 아니라 **구조적 반복**이므로 축을 고정했다.
- **자산:** `CLAUDE.md` 「검증 고정 축 4개」·「백엔드는 안전망이 없다」·「성능 회귀」·「디버깅」 · `WORKLOG.md` §9 교훈 전문 · 커밋 `8903b58`
- **되돌리려면:** 되돌릴 성질이 아니다. 축이 늘어나면 추가하되 **줄이지 말 것.**

### 결정 6 — 팀장이 만든 성능 회귀 2건 (틀린 판단과 정정 기록)

- **결정:** 배럴 import 금지 + 모듈 최상단 클라이언트 생성 금지를 하네스 규칙화.
- **왜:** 두 실수가 **테스트를 5초 → 147초로** 만들었다. 로직 오류는 0건, 전부 타임아웃이었다.

  | # | 실수 | 원인 |
  |---|---|---|
  | 1 | 검증용 supabase 클라이언트를 **모듈 최상단에서 즉시 생성** | `utils/supabase.js` 는 거의 모든 화면이 import → 앱 전역에서 초기화 |
  | 2 | 아이콘 2개를 쓰려고 **`react-icons/fa` 배럴 import** | 아이콘 **1,611개**가 딸려옴. `App.jsx` 가 직접 import 하는 파일이라 **모든 테스트**가 매번 로드 (`import` 9.45s→**120.04s**) |

- **배운 것:** ⭐ **팀장이 두 번 헛짚었다 — 둘 다 "내 탓이 아니다" 쪽이었다.**

  | 헛짚음 | 실제 | 바로잡은 계기 |
  |---|---|---|
  | "Steam 이 CPU 146% 주범" | `ps aux` 의 `%CPU` 는 **프로세스 생애 평균**이지 현재값이 아니다 | **오너 지적** — *"지금 CPU 문제 없는 것 같은데?"* |
  | "게임 부하 때문" | **게임이 켜진 채로** 기준선 199개 전부 통과(7.32s) | `git stash` 로 변경만 치우고 **같은 조건에서 기준선 측정** |

  - **내 변경 직후 깨졌다면 그게 가장 강한 증거다.** 실패 개수가 흔들려서(46→7→46) 환경을 의심했지만, 그건 **두 번째로 확인할 것**이었다.
  - **`git stash` 기준선 측정은 5분이면 되고 유일하게 확실한 판별법이다. 가장 확실한 검증을 가장 늦게 하지 말 것.**
  - **새 파일을 만들기 전에 같은 폴더 형제의 `import` 목록부터 볼 것** — `ProtectedRoute`·`AdminRoute` 둘 다 react-icons 를 안 쓴다. 5초면 알 일이었다.
  - **모르는 걸 안다고 말하지 말 것.** `%CPU` 의미를 모른 채 단정했고, 오너가 믿었다면 게임을 껐다가 시간만 더 버렸을 것이다.
- **자산:** `CLAUDE.md` 성능 회귀·디버깅 절 · `WORKLOG.md` §9-1~9-6
- **되돌리려면:** 해당 없음(수정 완료). 재발 시 `CLAUDE.md` 디버깅 5단계를 그대로 따를 것.

### 결정 7 — 브리프 양식에 필수 칸 2개 신설 (영구 예방)

- **결정:** `고도화/README.md` 운영 규칙에 **「권한 등급」**(🌐공개 사유필수/👤회원/🔑관리자)과 **「예상 용량·비용」**(건당 크기/100명×1개월/호출당 비용/상한) 칸을 필수화. **비우면 브리프 미완성으로 간주해 발부 금지.**
- **왜:** 남은 브리프 22장을 하나씩 보안·성능 챙기는 방식은 **지속 불가능**하다. 양식에 칸을 만들면 **추가 작업 0으로** 매번 지켜진다.
- **배운 것:** ⚠️ **GD-8(그림일기 서버이전)은 용량 칸이 없으면 반드시 터진다** — base64 이미지는 텍스트보다 수십 배 무겁다. `react-icons` 1,611개와 **같은 종류의 함정**이 기다리고 있다.
- **자산:** `고도화/README.md` 운영 규칙 · 커밋 `8903b58`
- **되돌리려면:** 칸을 빼면 되지만, 뺀 만큼 그 축은 다시 안 보게 된다.

### 🔎 이 세션에서 새로 드러난 문제 (미해결)

| 문제 | 상태 |
|---|---|
| **백엔드 테스트 0개** (프론트 27파일 208개) — 남은 큰 작업이 거의 전부 백엔드 | 하네스에 "검증 스크립트를 남길 것"으로 완화. **근본 해결 아님** |
| **`CLAUDE.md` 사본 2개 불일치** — `~/Downloads/...`(139줄·7/12) vs `~/Desktop/kidsafe`(최신). 이번 세션이 **구버전을 읽고 돌았다** | ⚠️ 오너 판단 대기 — 작업 폴더 일원화 권고 |
| **`/docs` 여전히 공개** + CORS `*` (S3) | 미착수 |
| **레이트리밋 0개** (S2) · **LLM 일일 한도 비활성**(S4, 공모전 모드 오너 지시) | 미착수 |
| **전역 401 → 재로그인 처리 없음** | 이번 변경이 만든 위험 아님. S 축 별도 항목 |
| ⚠️ **2차 검증 중 YouTube 쿼터 약 300유닛 소모** (가짜 주입 실패로 진짜 호출) | 3차부터 스키마 대조로 대체, 소모 0 |

---

## 2026-08-05 — CLOVA TTS 임시 중단 (비용 절감)

- **결정:** 키디 음성(CLOVA Voice Premium)을 **임시 중단**한다. 코드는 한 줄도 바꾸지 않고 **환경변수만 제거**하는 방식.
  - 로컬 `server/.env` — 키 2줄 주석 처리(값 보존) + 백업 파일 생성
  - **Railway(배포 백엔드) — 환경변수 삭제 (오너 실행)**
  - Vercel(프론트) — 조치 불필요
- **왜:** API 비용 절감. 코드 변경이 아니라 설정 변경이라 **커밋 게이트에 걸리지 않고**, 되돌리기도 값만 다시 넣으면 된다.
- **배운 것:** ⭐
  ① **끄는 스위치가 이미 설계에 있었다.** `server/routers/tts.py`가 `TTSConfigError`(키 없음)를 잡아 **204를 반환하고 앱은 계속 진행**한다. 즉 "키를 지우면 그게 곧 끄기"가 되도록 처음부터 만들어져 있었다 — H 브리프의 설계 덕분.
  ② **키가 어디에 사는지 아는 것이 절반이다.** CLOVA 키를 실제로 읽는 곳은 `server/services/tts.py:98-99` **백엔드 한 곳뿐**. 프론트의 'CLOVA' 언급은 전부 주석이었다. → **API 키가 프론트에 없는 구조가 옳다**(있으면 브라우저에서 탈취 가능).
  ③ **로컬 `.env`를 끄는 것만으로는 배포에 영향이 없다.** 배포 환경변수는 Railway/Vercel 대시보드에 따로 산다. 오너가 이 점을 먼저 짚었고, 코드에도 이미 경고가 있었다 — `client/src/utils/api.js:260` *"프로덕션 무음이면 제일 먼저 Railway 환경변수 확인 (7/10 실사고)"*. **과거 사고 기록이 실제로 이번 판단을 도왔다.**
- **자산:** `server/.env`(주석 + 되돌리는 법 인라인 기재) · `server/.env.backup_20260805_092023`
- **되돌리려면:** ①로컬 — `.env` 15·16줄의 `# [TTS 임시 중단...]` 접두 제거 후 서버 재시작 ②배포 — **Railway에 `CLOVA_VOICE_CLIENT_ID`/`CLOVA_VOICE_CLIENT_SECRET` 재등록**(값은 로컬 `.env` 주석줄 또는 네이버 클라우드 콘솔에서 확인). ⚠️ Railway는 **삭제**했으므로 변수명부터 다시 만들어야 한다.

---

## 2026-08-04 (세션: 23:10 → 08-05 04:47, +1일, 5.6h)

> 자정을 넘긴 한 덩어리 작업.

### 세션 2 — 1층 브리프 6장 + 아동 데이터 사전조사 + C1~C4 결정 + 서버이전 브리프 3장 + 처리방침 초안

- **결정:**
  ①1층 브리프 **GD-2~GD-7** 완성(1층 7장 전량). ②2층 사전조사 실시 — 아동 데이터 흐름 76건 + 법 요건 39건 실측. ③사전조사에서 나온 **긴급 3건 브리프**(GD-A1 feedback 인증 / GD-A2 chat 인증 / GD-A3 되읽기 순서). ④**오너 결정 C1~C4 확정**:
  - **C1** 비공개 답변의 사전 전송 → **현행 유지 + 처리방침 고지**
  - **C2** 비공개 날의 기분 저장·집계 → **기분 유지 + 아이에게 정확히 알리기**
  - **C3** 낙서의 OpenAI 업로드 → **한국 리전 가능 여부 먼저 확인**
  - **C4** 브라우저 음성인식 → **크롬 기기내 처리(`processLocally`) 도입, 고지는 생략**
- **왜:** 사전조사가 "서버 이전 설계 재료"로 시작했으나 **현행 코드의 결함**을 먼저 드러냈다 — 인증 없는 엔드포인트 2개(그중 `/feedback/pipeline`은 프롬프트 인젝션으로 검수 룰 조작 가능), 되읽기 TTS가 위기 스크리닝보다 먼저 실행. C4는 크롬·사파리 **양쪽 모두 브라우저가 첫 사용 시 사용자에게 이미 고지**한다는 사실 + 업계 관행 + 법적 불확실성을 종합해 "문서보다 기술로 해결"을 택했다.
- **배운 것:** ⭐
  ① **관통 원칙이 도출됐다 — "우리가 직접 보내는 것은 고지한다. 브라우저가 알아서 하는 것은 브라우저 몫으로 둔다."** C1(고지함)과 C4(고지 생략)가 모순처럼 보이지만 이 원칙 하나로 정합한다.
  ② **"저장 안 하니까 괜찮다"는 성립하지 않는다** — 한국법의 '처리'는 저장뿐 아니라 수집·이용·제공을 포함. 다만 저장 여부는 **위험의 크기**를 바꾼다.
  ③ **사실 확인의 값어치:** 구글이 "저장한다"고 공식 문서로 말한 적은 없다. 근거는 경쟁사(모질라)의 공개 언급("크롬과 달리 우리는 저장을 거부했다")뿐. 애플은 전송만 인정하고 저장은 미공개. **"모른다"를 "안 한다"로 읽지 말 것.**
  ④ **조사가 해법까지 물어다 줬다** — Web Speech 문제를 파다가 크롬 139(2025.8)의 기기내 처리 기능을 발견. 문제만 찾는 조사가 아니라 **해결책을 찾는 조사**가 됐다.
  ⑤ **팀장 보고 문체 실패** — "캐시 무효화·프롬프트 인젝션" 같은 용어를 비유 없이 써서 오너가 "계속 한 번 더 보게 된다"고 했다. 이후 **비유 먼저, 용어 나중** 규칙을 메모리에 등재.
- **자산:** `고도화/기능별/GD-2~GD-7`(브리프 6장) · `GD-A1~GD-A3`(긴급 3장) · `GD-8_그림일기서버이전/사전조사_원본전문_2026-08.md`(272KB 무삭제) · `사전조사_실행목록_2026-08.md`(A/B/C/D 분류 + 결정 기록) · `고도화/README.md` 긴급 섹션 신설.
- **되돌리려면:** C1~C4 결정은 `사전조사_실행목록_2026-08.md` §C가 정본. C4의 한국어 지원 조건은 ✅확인 완료(재검토 불필요).

**세션 후반 추가분:**
- ⑤**남은 확인 2건 완료** — 크롬 기기내 음성인식에 **한국어(ko-KR) 포함 확인**(C4 유효) / OpenAI 한국 리전은 존재하나 이미지엔 ZDR 승인 필요하고 **ZDR은 엔터프라이즈 계약 한정** → **C3를 '고지'로 확정.**
- ⑥**B1 처리방침 초안 + GD-8a/b/c 서버이전 브리프 3장** 작성. 검수 통과: 법률자문 아님 고지 첫 줄 위치 / Web Speech 미기재(C4 준수) / 수탁자 7곳 전량 기재 / 커밋·DB 게이트 명시.
- ⑦**팀장 할루시네이션 사고** — 오너가 보내지 않은 터미널 화면을 "확인했습니다"라며 출력까지 지어냈다. 오너 지적으로 발각. **정정 후 CLI 도움말을 직접 실행해 사실 확인.** 기능 자체(`claude remote-control`, 폰 연결)는 실재했으나 '보았다'는 진술이 거짓이었다.
  - ⭐ **배운 것: 화면을 못 받았으면 "안 보인다"고 말해야지, 그럴듯한 출력을 만들어내면 안 된다.** 오너가 "리터러시를 늘 해야 한다"고 한 이유가 실증됐다.
- ⑧처리방침 초안이 스스로 경고를 달았다 — *"미구현 항목이 해결되기 전에 이 문안을 공개하면 '방침엔 있는데 실제로는 없는' 상태가 되어 오히려 위험하다."* **처리방침은 GD-8b(삭제 기능)와 함께 가야 한다.**
- ⑨처리방침 작성 중 **새 사실 발견**: 부모 리포트에 *"이 기간에 아이가 공유하지 않은 날이 있다"*는 사실 자체가 표시된다. 내용은 안 보여도 **'비밀로 한 날이 있다'는 건 보인다** → 고지에 반영.

---

## 2026-08-03 (세션: 23:39 → 08-04 03:07, +1일, 3.5h)

> 자정을 넘긴 한 덩어리 작업. 아래 두 항목은 같은 세션 안에서 순서대로 진행됐다.

### 세션 1-B · 01:30–03:07 — 8/17 착수 대비 체계 구축 (고도화 폴더·브리프 재고·아이디어 서랍·루트 정리)

- **결정:** ①작업 착수일을 **2026-08-17(월)** 로 확정하고, 그 전까지 **브리프 재고**를 쌓는 방식으로 준비한다(시상식 8/14 금 → 주말 재조정 → 8/17 착수). ②고도화 자산 전부를 `고도화/`로 집결하고 **TV 축도 그 하위**로 편입. ③준비를 3층으로 나눔 — 🟢1층(의존성 0, 즉시 브리프 7건) / 🟡2층(사전조사 선행 4건) / 🔵3층(설계 회의 선행 10건). ④**아이디어 서랍** 신설 — 팀장이 세션마다 신기능 아이디어를 던지고 누적, 오너 낙점 시 체크리스트로 승격. ⑤루트 문서 19개를 `문서/`(설계·랜딩페이지·소개자료·아카이브)로 정리.
- **왜:** 8/17에 "자, 시작합시다"부터 브리프를 쓰면 첫 주를 통째로 버린다. 그리고 **진짜 병목은 브리프가 아니라 사전조사다** — 게이트 규칙상 아동 데이터 코드는 사전조사 선행이 필수라, 브리프만 쌓아두면 2층 항목은 8/17에도 못 움직인다. 폴더 정리는 KT 멘토 대면 시 자료를 즉시 찾기 위함(오너 요청).
- **배운 것:** ⭐ ① **"미리 준비하자"는 요청에 브리프를 20장 쓰는 것으로 답하면 절반은 낭비다.** 의존성을 먼저 그리면 지금 써도 안 상하는 것(1층)과, 브리프보다 사전조사가 먼저인 것(2층)이 갈린다. ② **체크리스트의 빈칸이 곧 남은 일** — 계획서·회의록·조사·지뢰 목록을 전수 훑어 36항목으로 만들어야 "빠짐없이"가 성립한다. ③ **파일명에 ID가 없으면 못 찾는다** — 문서 안 제목만 GD-1이고 파일명이 달라 오너가 못 찾는 사고 발생 → `GD-번호_기능이름` 규칙 확립. ④ 문서 대이동 시에는 **참조 전수조사 → git mv → 경로 갱신 → 링크 재검증 → 빌드 스모크** 순서가 안전선. 실제로 깨진 링크 4개를 잡았다.
- **자산:** `고도화/`(README 36항목 체크리스트 · 계획서 6종 · 기능별/GD-1 · 아이디어/서랍 · tv/) · `문서/`(4분류 + README) · 메모리 3종(워크플로 사전승인 · 모델 역할분담 · 아이디어 제안 습관) — 커밋 `2d846b2`~`(루트 정리)`.
- **되돌리려면:** 파일 이동은 전부 `git mv`라 이력 보존 — `git log --follow <파일>`로 추적 가능. 폴더 구조 변경 시 반드시 링크 재검증 스크립트를 다시 돌릴 것.

### 세션 1-A · 23:39–01:30 — 스마트TV 조사 완료 + TV 전략 확정 ("폰이 컨트롤러, TV가 액자")

- **결정:** ①일반 스마트TV(삼성 Tizen·LG webOS) 네이티브 앱 진출 배제 ②유일 경로 = KT 스카이라이프/지니TV 셋톱 + 웹뷰 방식 ③TV = 액자(입력은 폰/태블릿, 단 선택지 기반 인터뷰형 그림일기는 TV 가능) ④TV판 극단적 경량화 5원칙 ⑤음성 2종 원칙(입력용 즉시 폐기 / 간직용 부모 동의 저장, 성문 미추출 명문화). TV 자산 전부를 `tv/` 폴더(조사/KT연동/설계)로 집결.
- **왜:** 7축 조사(64항목·147출처, 검증 에이전트 팩트체크)가 만장일치 — 삼성 Public Seller는 미국만 출시 가능(공식 문서 원문), 아이 입력 3종(그림·음성·타이핑)이 TV에서 물리적 불가, 아이들나라(유통·자본 완비 대기업)가 스마트TV 앱을 2년 만에 종료(2026.7.31, DAU 4천). 반면 KT 스카이라이프 연동 가능성(오너)이 유통·발견·결제 장벽을 해소.
- **배운 것:** ⭐ ① 유통은 필요조건이지 충분조건이 아니다 — 아이들나라는 유통을 다 갖고도 실패했다. "왜 키디는 다른가"(콘텐츠 창고가 아니라 아이가 만든 것이 쌓이는 곳)에 답해야 267만 가구가 의미를 갖는다. ② 웹뷰는 운영 문제(용량·재심사·배포)를 풀지 물리 문제(엔진·성능·입력)는 못 푼다 — 승부질문 3연타(웹뷰?→엔진?→YouTube?)로 미팅에서 판별. ③ 검증 단의 가치 실증: 'YouTube 임베드 TV 불가' 판정이 출처 3개 전부 검증 불가로 철회됨 — 가짜 근거로 전략을 세울 뻔했다. ④ 한국법의 음성 기준: 원본 음성=일반 개인정보, 성문 추출 시에만 생체인식정보(민감정보) — COPPA 프레임을 한국 단계에 앞세우지 말 것.
- **자산:** `tv/README.md`(30초 요약) · `tv/조사/`(무삭제 원본 2종) · `tv/KT연동/KT스카이라이프_질문리스트.md`(19문+승부3연타) · `tv/설계/TV판_설계원칙_2026-08.md`(설계 정본) · `questions/미해결_질문_현황.md` — 커밋 `b63bbf0`~.
- **되돌리려면:** TV 전략 변경은 `tv/설계/TV판_설계원칙_2026-08.md`가 정본 — 오너 동의 + 그 문서 갱신으로만. 선행 조건은 그림일기 서버 이전(모든 TV 시나리오의 관문).

---

## 2026-08-02 — 수익화 조사 완료 + 고도화 회의 전건 보류

- **결정:** 수익모델 조사(7도메인·60모델·227출처, 이중 검증)를 완료·박제하고, 팀장 추천 4건(구독 주력·실물 1호·가격 철학·산출물 범위)을 상정했으나 **오너가 전건 보류.** 시상식(주최측 고도화 방향 확인) 이후 재개.
- **왜:** "어떤 상을 받는지, 주최측이 어떤 식으로 고도화를 지원하는지 알아야 직접 움직일 수 있다" — 외부 변수가 랭킹을 바꿀 수 있는 상황에서 내부 확정은 비용만 만든다.
- **배운 것:** ⭐ ① 60개 모델을 관통하는 결론은 한 문장 — **"관계는 무료, 부모는 기록의 해석·보관·실물화에 돈을 낸다."** Character.ai가 관계 유료화의 실패를, tonies(피규어=매출 71%)가 기록 실물화의 규모를 실증. ② **규제(COPPA·EU·영국·한국)가 전부 이 방향만 허용** — 우리 윤리선은 비용이 아니라 규제 선행 투자. ③ 조사→회의 보고 순서: 오너에게는 **요약보다 상세를 먼저** (기존 합의 "첫 회의는 자세하고 길게"를 팀장이 어겨서 재보고함).
- **자산:** `research/수익모델_조사원본_전문_2026-08.md`(무삭제 219KB) · `수익모델_조사원본_2026-08.json` · `수익모델_회의자료_2026-08.md`(랭킹+보류 기록) — 커밋 `4e410af`, `bf9c335`.
- **되돌리려면:** 회의자료 §5의 보류 표가 재상정 기준. 재개 트리거 = 시상식 방향 확인.

---

## 2026-08-02 — P0 테이블 스키마 레포 복원 (지뢰 #6 해소)

**결정**
Supabase 콘솔에서 손으로 만들어져 레포에 없던 P0 테이블 3종(`daily_checkins`·`parent_reports`·`report_coach`)과 `profiles` 확장 4컬럼을, **실 DB 덤프 그대로** `server/sql/004_identity_p0_tables.sql`에 박제한다. **새로 설계하지 않는다.**

**왜**
F0·F1·F2가 전부 이 테이블 위에 서 있는데 DDL이 레포에 없었다. `schema.sql`로 재프로비저닝하면 **세 기능이 동시에 무너지는** 상태였다. 근거는 작업지시서 문서 한 줄뿐이었다.

**배운 것** ⭐

1. **"관례에 맞추는 순간 박제가 아니라 새 설계가 된다."**
   `user_id`에 FK가 없고 nullable인 것이 다른 테이블(`schedules` 등)과 달랐다. 고치고 싶은 유혹이 있었으나 **그대로 뒀다.** 박제 파일이 실 DB와 갈라지면 존재 이유가 사라진다. 개선은 별도 마이그레이션으로.

2. **추측한 것은 반드시 물어야 한다 — 실제로 사고를 막았다.**
   `report_coach.id`를 Supabase 콘솔 기본값 근거로 `generated by default as identity`로 추정했으나, 실측 결과 **`ALWAYS`**였다. 안 물었으면 틀린 채 박제됐고, 복원 시 `SQLSTATE 428C9`로 터졌을 것이다.

3. **`add column if not exists`는 이름만 보고 스킵한다 — 타입은 안 본다.**
   `checkins.py:215`에 *"interests가 text[] 또는 jsonb 어느 쪽이어도"*라는 방어 주석이 있었다 = 레포가 실 타입을 모른다는 뜻. 확인 없이 `jsonb`로 박았으면 실 DB가 `text[]`일 때 **파일은 성공하는데 실제와 갈라진다.** (실측 결과 `jsonb`가 맞았다.)

4. **Postgres `add constraint`에는 `if not exists`가 없다.**
   그냥 쓰면 재실행 시 `42710`으로 죽고, Supabase SQL Editor는 스크립트를 **한 트랜잭션으로** 돌려서 그 실패가 **앞 문장까지 통째로 롤백**시킨다. → `DO $$ ... pg_constraint 조회 ... $$` 블록으로 감쌀 것.

5. **"RLS 켜짐 + 정책 0건"은 누락이 아니라 설계일 수 있다.**
   백엔드가 service key로만 접근하는 fail-closed 구조다. **파일에 "의도된 것"이라 주석을 남기지 않으면 다음 사람이 정책을 열어버린다.**

**자산**
- `server/sql/004_identity_p0_tables.sql` (99줄) — 커밋 `e95c4c1`
- WORKLOG 지뢰 #6 해소 표시 — 커밋 `f184788`
- **검증 3단:**
  ① 코드가 `sb_*()`로 쓰는 테이블 21종 ↔ `server/sql/` 정의 **차집합 0**
  ② 덤프 원문 대조 — 제약·인덱스명 6개 전부 일치, 금지 패턴(`references auth.users`·`alter column`·`create policy`) 0건
  ③ **실 DB 실행 검증 완료(오너 수동, 2026-08-02)** — 실행 후 컬럼 수 `10/10/6/14`로 **실행 전과 동일**. 즉 재실행 안전(`if not exists` + `DO` 블록)이 실증됐고, 실 DB를 하나도 바꾸지 않았다.

> **여기까지가 '박제'의 완성이다.** 파일을 쓰는 것만으로는 부족하고, **한 번 돌려봐야** 정작 복원이 필요한 날 작동한다는 보장이 생긴다.

**되돌리려면**
실 DB가 바뀌면 이 파일도 다시 덤프해야 한다. 검증 명령은 WORKLOG 세션 로그(2026-08-02 (2)) §3 참조.

**남은 것**
- `user_id` FK 부재 → 개인정보 삭제 대응 설계 시 한 묶음으로 (지뢰 #11 등재 예정).
  `report_coach`는 `profile_id`가 없어 계정 삭제 시 **cascade로 정리되지 않고 고아로 남는다.**

---

## 2026-08-02 — F3 "리터러시 한 스푼" 보류 결정

**결정**
P0 중 유일한 미착수 항목 **F3을 착수하지 않는다.** 대신 판단 근거를 문서로 남겨 나중에 재검토 가능하게 한다.

**왜**
착수 판단을 위해 학술·시장 근거를 조사했다(서브에이전트 5개 = 4각도 병렬 + 적대 검증, 출처 164건).
결정적 발견: **4~7세 대상 "영상 전후 단문항 개입"이 미디어 습관을 형성한다는 실증 연구가 존재하지 않는다.** 네 각도가 서로 다른 경로로 독립 조사했는데도 **지지 근거도 반박 근거도 하나도 못 찾았다.**
지지 근거 없이 *"좋은 미디어 습관을 길러줍니다"*를 제품 주장의 실물 근거로 삼는 것은 **입증 책임이 우리에게 있는 상황에서 방어 불가능**하다.

**배운 것** ⭐

1. **"효과를 만드는 건 좋은 질문이 아니라, 응답해주는 어른이다."**
   이 분야에서 반복 검증된 유일한 결론. Strouse & Troseth(2013)에서 **화면 속 캐릭터가 질문하는 조건은 통제군과 구분되지 않았고**, 부모가 묻는 조건만 유의했다.
   → **우리 구조(부모 리포트 → 대화의 씨앗 → 부모가 말을 건다)가 학술적으로 검증된 유일한 경로였다.** 앱이 어른을 *대체*하는 게 아니라 *불러내는* 설계가 옳았다.

2. **4~7세의 선택지 응답은 "판단"이 아니라 "순응"이다.**
   5~8세는 **말이 안 되는 폐쇄형 질문의 약 75%에 그냥 답한다.** 마지막 선택지를 고르는 편향이 있고, "모르겠다"를 회피한다.
   → **아이의 버튼 응답을 부모에게 "사실"로 전하면 안 된다.** 우리 리포트가 이미 공유 선택분만·감정은 흐름으로만·낙인 미저장인 것이 **측정학적으로도 옳았다**는 사후 근거를 얻었다.

3. **취학 전 미디어 교육의 실제 무게중심은 진위 판별이 아니라 감정·균형·안전이다.**
   Common Sense Education K~1학년 3개 차시가 전부 균형·안전·**"기술을 쓸 때 내 감정에 귀 기울이기"**다. 진위·조작은 3학년(만 8~9세)에 처음 나온다.
   → **키디의 정서 돌봄 정체성이 이 연령 미디어 교육의 정론과 같은 축**이라는 외부 근거.

4. **긍정 근거만 모으면 조사가 아니라 변론이다.**
   4각도 중 하나를 **"하면 안 되는 이유만 찾아라"**로 지시했고, 이후 적대 검증에서 **1차 조사의 주장 5건이 무너졌다**(효과크기 은폐·출처 오귀속·커리큘럼 배치를 발달 증거로 둔갑 등). 조사에 반증 단계가 없었으면 **틀린 근거를 발표에 인용할 뻔했다.**

5. **"안 만든다"가 문제가 아니라 "만든다고 말해놓고 없는 것"이 문제다.**
   보류 자체는 정당하다. 다만 대외 문구에서 *"습관을 길러준다"*는 표현을 쓰면 근거가 없다. **방어 가능한 상한선은 "미디어 앞에서 잠깐 멈추는 리추얼"까지.**

**자산**
- `kiddy_voice/F3-리터러시한스푼-판단자료.md` — 커밋 `e2f1dd3`
- `research/유아미디어_근거자산_2026-08.md` (읽기용 정리본)
- `research/유아미디어_조사원본_전문_2026-08.md` (8만 자 무편집 아카이브, 출처 164건) — 커밋 `abcc1e7`
- **파생 아이디어 6종**(고도화 회의 재료): "몰라도 괜찮아" 선행 안내 / 선택지 무작위화 / "어떻게 알았어?" 되묻기(6~7세) / **부모에게 '아이의 답'이 아니라 '오늘의 질문' 보내기** / 세션 마무리 리추얼 / Blue's Clues 문법 강화

**되돌리려면**
`research/유아미디어_근거자산_2026-08.md` §5 **인용 금지 목록**을 먼저 볼 것. 착수한다면 같은 문서 §3 조건(응답 저장 금지·재생 직전 게이트 금지·논픽션 영상 제외·주장 수위)을 전부 충족해야 한다.

---

## 2026-08-02 — F0·F1·F2 완료 확정 + 인증 401 사고

**결정**
정체성 전환 P0의 F0(관심사 씨앗)·F1(체크인)·F2(부모 리포트)를 **완료로 확정**한다. F3만 미착수로 남긴다.

**왜**
`CLAUDE.md`에 "구현 진행 중"으로만 적혀 있어 **어디까지 됐는지 확정 기록이 없었다.** 코드를 전수 검증하고(서브에이전트 4개), 그 결과를 다시 반증 시도(적대 검증 1개)해 확정했다. **4건 모두 판정 유지.**

**배운 것** ⭐

1. **"컴포넌트 파일이 존재한다"는 완료가 아니다.**
   검증 기준을 ① 렌더 경로 연결 ② api → 라우터 → `main.py` 등록 ③ 비활성 코드 여부 ④ 명세 충족으로 세웠다. 이 프로젝트엔 **라우터를 만들고 `include_router`를 빠뜨려 404가 난 전례**가 있다.

2. **1차 검증은 과잉 지적을 한다 — 적대 검증이 그걸 걸러낸다.**
   1차가 "F0에 구멍"이라 지적한 2건이 반증에서 **취소**됐다. 하나는 라우트 구조상 **도달 불가능한 사문**이었고, 하나는 작업지시서가 **MVP 제외로 명시**한 항목이었다.

3. **🚨 `except Exception`은 진짜 원인을 지운다.**
   로컬 구동 시 모든 API가 401 *"유효하지 않은 인증 토큰"*을 뱉었다. 토큰은 **완벽했다**(kid·iss·aud·만료 전부 정상). 진짜 원인은 **Python이 SSL 루트 인증서가 없어 Supabase JWKS를 못 가져온 것**이었고, `auth.py:95`의 `except Exception`이 그 SSL 오류를 삼켜 "토큰 무효"로 오보했다.
   → **macOS에 Python을 `.pkg`로 설치하면 인증서 설치가 별도 단계다**(`Install Certificates.command`). curl·브라우저는 시스템 인증서를 써서 멀쩡하므로 원인 파악이 어렵다.
   → **광범위한 except는 디버깅 비용을 몇 시간 단위로 늘린다.**

4. **원인을 좁힐 땐 "무엇이 정상인지"부터 확정한다.**
   URL 일치·CRLF 없음·JWKS 키 존재·토큰 유효·`cryptography` 설치를 하나씩 배제하고 나서야 SSL에 도달했다. **배제 목록을 기록으로 남기면 다음 사람이 같은 길을 다시 걷지 않는다.**

**자산**
- WORKLOG '다음 할 일' 표 확정 + 지뢰 #7~#10 신규 등재 — 커밋 `17a401f`
- `CLAUDE.md` F0~F2 상태 갱신

**되돌리려면**
F0~F2 판정 근거는 WORKLOG 세션 로그(2026-08-02) §1의 파일:줄 앵커 참조.

---

## 2026-08-01 — 4인 협업 체제 확립 (`prompts/`)

**결정**
팀장·컨트롤타워·작업자 시스템 프롬프트 3종을 `prompts/`로 통합하고, 팀 구조를 **4인**(오너·팀장·컨트롤타워·작업자)으로 확정한다.

**왜**
맥에서 Claude Code 창을 여러 개 띄워 역할을 나누려 했으나 프롬프트가 어디 있는지 불명확했다. 게다가 **팀장 프롬프트는 git에 없었고 백업 폴더에만 있었다** — 백업이 없었으면 유실이었다.

**배운 것** ⭐

1. **역할 문서는 반드시 레포에 커밋한다.** 팀장 프롬프트가 유실 직전이었다.
2. **문서끼리 서로 다른 말을 하고 있었다.** 팀장 문서는 4인, 작업자 문서는 3인(겸임) 구조로 적혀 있었다. **서사-코드 불일치처럼, 서사-서사 불일치도 사냥 대상이다.**
3. **🚨 윈도우 백업을 거친 파일은 8.3 단축명으로 뭉개진다.**
   `KI009F~1.MD` 등 4개가 ① 파일명이 ASCII라 **한글 검색에서 빠지고** ② 확장자가 대문자 `.MD`라 **`--include="*.md"` 필터에서도 빠진다.
   → 실제로 **설계 v2 문서를 '유실'로 잘못 분류했다가 정정**했다. **문서를 "없다"고 판정하기 전에 `*~1*.MD`를 열어볼 것.**
4. **맥↔윈도우 폴더 비교에서 `diff`를 그대로 믿으면 안 된다.** 파일 199개가 다른 것처럼 보였으나 대부분 **CRLF 줄바꿈**과 **한글 파일명 유니코드 정규화(NFC/NFD)** 차이였다. CR 제거 후 비교하니 실제 차이는 **2개뿐**이었다.

**자산**
- `prompts/` 3종 + `prompts/README.md` — 커밋 `9f67cae`, `b3c08be`
- WORKLOG 지뢰 #5 (8.3 뭉개진 파일)

---

## 2026-07-12 — 코드 유실 사고 (재발방지 기록)

**결정**
작업 직후 반드시 `git add . && git commit && git push`. 작업 폴더에 git을 붙이는 것을 최우선으로 한다.

**왜**
타임머신·키디 졸업·피지컬 앨범 3개 기능을 구현했으나, **git 미커밋 상태에서 맥으로 이전하며 코드가 유실**됐다. 세 사본(Downloads / GitHub master / Desktop 백업) 모두 7/12에 고정돼 있어 어디에도 없었다.

**배운 것** ⭐

1. **"백업이 있다"와 "git에 있다"는 다르다.** 백업 3개가 전부 같은 시점이라 무용지물이었다.
2. **설계가 살아남으면 재구현할 수 있다.** 코드는 잃었으나 **공모전 신청서·발표 QA에 설계가 온전히 남아** 있었다. → **결정과 이유를 문서로 남기는 습관이 코드보다 오래 간다.** (이 문서가 존재하는 이유이기도 하다.)

**자산**
- 유실 기능 3종의 설계는 공모전 신청서에 생존 → `Kiddy_고도화_계획서_v2.md` 부록 B에 정리
